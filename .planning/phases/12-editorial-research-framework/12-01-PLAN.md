---
phase: 12-editorial-research-framework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/RESEARCH.md
  - docs/DATA_MODEL.md
autonomous: true
requirements: [FRAME-01, FRAME-04]

must_haves:
  truths:
    - "docs/RESEARCH.md's source hierarchy names journalism and a displayable triangulated-community tier, with a concrete, auditable definition of source independence"
    - "docs/RESEARCH.md documents a qualitative content review cadence that is explicitly distinct from the factual research_status recheck cadence"
    - "docs/DATA_MODEL.md documents the closed v1 tag taxonomy (4 categories, 10 tag IDs) and the qualitativeLastReviewed field's semantics"
  artifacts:
    - path: "docs/RESEARCH.md"
      provides: "Community Source Independence rules and Qualitative Review Cadence policy"
      contains: "## Community Source Independence"
    - path: "docs/DATA_MODEL.md"
      provides: "Tag Taxonomy v1 shape and Qualitative Review Tracking field semantics"
      contains: "## Tag Taxonomy"
  key_links:
    - from: "docs/RESEARCH.md ## Qualitative Review Cadence"
      to: "docs/DATA_MODEL.md ## Qualitative Review Tracking"
      via: "cross-reference to qualitativeLastReviewed field shape"
      pattern: "qualitativeLastReviewed"
---

<objective>
Extend `docs/RESEARCH.md` and `docs/DATA_MODEL.md` with the evidence-model half of the Phase 12 framework: the extended source hierarchy (journalism + triangulated community tiers), a concrete community-source independence definition, the closed tag taxonomy v1 shape, and the qualitative review cadence policy + field semantics — all additive, none of it rewriting existing prose.

Purpose: FRAME-01 requires `docs/RESEARCH.md` to define the official → journalism → triangulated-community source hierarchy with an auditable "independent" definition, and FRAME-04 requires a qualitative review cadence distinct from the existing factual `research_status` recheck. This plan delivers both, plus the `docs/DATA_MODEL.md` half of FRAME-01 (tag taxonomy v1 shape) that Phase 13 will implement in code.
Output: Updated `docs/RESEARCH.md` and `docs/DATA_MODEL.md` with new sections, committed together since they cross-reference each other by field name (`qualitativeLastReviewed`).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-editorial-research-framework/12-CONTEXT.md
@.planning/phases/12-editorial-research-framework/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend docs/RESEARCH.md with journalism/triangulated-community tiers, independence rules, and qualitative review cadence</name>
  <files>docs/RESEARCH.md</files>
  <read_first>
    docs/RESEARCH.md
    docs/DATA_MODEL.md
    .planning/phases/12-editorial-research-framework/12-CONTEXT.md
  </read_first>
  <action>
Make five additive edits to `docs/RESEARCH.md`. Do not reword any part of tiers 1-4 of the Source Hierarchy or any sentence in `## Research Standard`, `## Citation Requirements`, `## Conflicting Information`, `## Inspection Availability`, `## Missing Information`, or `## Correction Workflow`.

Edit 1 - `## Source Hierarchy`: replace the existing 5-item numbered list and its trailing sentence with:

1. Official Berlin Senate, district, or public education sources.
2. Official school inspection or quality reports.
3. Individual school websites.
4. Public statistical datasets.
5. Independent journalism from established news outlets.
6. Triangulated community sources - independently corroborated community observations meeting the Community Source Independence rules below. May currently be cited only for the `active-school-community` tag (see `docs/DATA_MODEL.md`).
7. Parent reviews, forums, Google ratings, and social media.

Then the trailing sentence: "Anecdotal parent signals (tier 7) remain reserved in the data model and are not displayed in V1, except for the triangulated community sources described in tier 6 above."

Edit 2 - `## Canonical Source Types`: insert two new bullets after the existing `public_dataset` bullet and before `anecdotal_reserved` (leave `official_government`, `official_inspection`, `school_website`, `public_dataset`, and `anecdotal_reserved` bullets exactly as they are):

- `journalism`: reporting from an established, independent news outlet.
- `triangulated_community`: a community observation independently corroborated per the Community Source Independence rules below; may currently be cited only for the `active-school-community` tag.

Edit 3 - `## Reliability Levels`: replace the existing 4-bullet list with:

- `primary`: official government, district, inspection, or public education source.
- `secondary`: school-published information, reliable public dataset, or independent journalism.
- `triangulated_community`: independently corroborated community observation meeting the Community Source Independence rules below; may be cited only for the `active-school-community` tag, never as the sole basis for a tag.
- `anecdotal`: subjective parent or community signal that has not been triangulated. Reserved in the data model but not displayed in V1.
- `unknown`: source reliability has not yet been classified.

Edit 4 - new `## Community Source Independence` section: insert immediately after `## Reliability Levels` and before `## Citation Requirements`, containing exactly these bullets (adapt only punctuation/flow, not the substantive content):

- Opening sentence: community-sourced observations may only be used for the `active-school-community` tag (see `docs/DATA_MODEL.md`'s Tag Taxonomy) and never as the sole basis for any tag; official corroboration should be sought where applicable.
- **Independence** means a different publisher or community origin - not merely a different URL or a different individual within the same venue. The guiding question is whether the observations could reasonably have arisen separately, not merely been repeated.
- **Echoes do not count** as independent corroboration: same-thread comments, cross-posts, screenshots or reposts of the same discussion, two Reddit threads about the same original post, or the same article republished elsewhere.
- **Acceptable independence examples:** a Reddit thread and a Facebook parent group; two unrelated Facebook groups with different moderation and membership; a parent blog and a Reddit thread; a local parent forum and a Facebook group.
- **Conflict fails closed:** when independent sources disagree, no tag is published. The research log may record supporting evidence, opposing evidence, and the reason for withholding the tag. Absence of a tag means insufficient evidence, not a negative signal.
- **Minimum source quality:** an active, multi-voice venue with more than one participant, recent enough to reflect the current school, and containing substantive discussion. A single testimonial or an abandoned page with one comment does not qualify.
- **Independence log:** each community source cited for a tag must be logged with venue/publisher, URL or stable identifier, date accessed, a one-line independence rationale versus the other corroborating source(s), and an echo-check note. Longer research reasoning belongs in research notes, not this required log.
- **Recency:** community sources should normally be within approximately 24 months of authoring or review. Older material may be used only when it clearly describes a durable characteristic still confirmed by current official information or other up-to-date evidence.

Edit 5 - new `## Qualitative Review Cadence` section: insert immediately after `## Research Status` and before `## Correction Workflow`:

- Opening paragraph: qualitative content - tags and the "Perfil da escola" narrative - is reviewed on a cadence distinct from the factual `research_status` recheck cycle. Qualitative review tracks its own field, `qualitativeLastReviewed`, defined in `docs/DATA_MODEL.md`, separate from `lastResearched` and `lastSourceChecked`. The cadence is hybrid: periodic and event-driven.
- **Periodic baseline:** all qualitative content is reviewed at least annually. Community-backed `active-school-community` tags must be revalidated at least every ~12 months, and community observations are revalidated on every qualitative review pass.
- **Event-driven triggers:** a qualitative review is also triggered by a major school website update, significant press coverage, a curriculum or program change, repeated correction reports about a school's qualitative content, or a methodology version change.
- **Methodology version changes:** when the methodology version changes (see `docs/DECISIONS.md`), every pilot school's qualitative content - tags and narrative - must be re-reviewed before the new methodology version is declared live.
  </action>
  <verify>
    <automated>grep -q "## Community Source Independence" docs/RESEARCH.md && grep -q "## Qualitative Review Cadence" docs/RESEARCH.md && grep -q "triangulated_community" docs/RESEARCH.md && grep -q "journalism" docs/RESEARCH.md</automated>
  </verify>
  <done>docs/RESEARCH.md contains both new sections, the journalism/triangulated-community tiers, and none of the pre-existing tier-1-4 wording or other section prose was altered.</done>
</task>

<task type="auto">
  <name>Task 2: Extend docs/DATA_MODEL.md with Tag Taxonomy v1 and Qualitative Review Tracking</name>
  <files>docs/DATA_MODEL.md</files>
  <read_first>
    docs/DATA_MODEL.md
    docs/RESEARCH.md
    .planning/phases/12-editorial-research-framework/12-CONTEXT.md
  </read_first>
  <action>
Insert two new `##` sections into `docs/DATA_MODEL.md` immediately after the existing `## Anecdotal Signals` section and before `## Open Day Questions`. Do not modify any existing section's wording.

New section 1 - `## Tag Taxonomy`:

Opening paragraph: tags are a closed, versioned taxonomy - v1 defines exactly 10 tag IDs across 4 categories. A tag describes a sustained, decision-relevant characteristic of a school, not an isolated project, a generic claim, or documentation richness. A tag differs from a factual field: a factual field answers "what does this school have?"; a tag answers "what kind of school is this?" (for example, a verified `ganztag` value does not automatically justify the `all-day-model` tag, and a bilingual/SESB program does not automatically justify `bilingual-program`).

Sub-heading `### v1 Tag Categories` with exactly these bullets:

- **Academic focus:** `stem-focus`, `languages-focus`, `arts-music-focus`.
- **Learning model:** `bilingual-program`, `special-pedagogical-model`, `all-day-model`.
- **Student support:** `inclusion-support`, `transition-support`.
- **School environment:** `structured-learning-environment`, `active-school-community`.

Then three closing paragraphs:

1. Each tag must cite at least one evidence source and carry a qualitative confidence label describing evidence strength, never school quality (see `docs/EDITORIAL_GUIDE.md`).
2. `active-school-community` is the only v1 tag that may be supported by community evidence, and only when corroborated by an independent source per `docs/RESEARCH.md`'s Community Source Independence rules; it may never be the sole basis, and it never implies that the community is "better." All other v1 tags require official and/or journalism sources only.
3. A "High demand" tag (admissions pressure or popularity) is explicitly out of v1 - it measures demand, not school-child match, and cannot be evidenced comparably across schools. Full taxonomy IDs, categories, and evidence rules are structural decisions recorded in `docs/DECISIONS.md`.

New section 2 - `## Qualitative Review Tracking`:

Two paragraphs:

1. Qualitative content (tags and the "Perfil da escola" narrative) is tracked with its own field, `qualitativeLastReviewed`, separate from the factual `research.lastResearched` and `research.lastSourceChecked` fields. `research_status` and its recheck cadence continue to describe only factual field completeness; `qualitativeLastReviewed` describes when a school's tags and narrative were last reviewed under the qualitative review cadence.
2. This section documents the field's intended semantics; Phase 13 implements it on the school schema. `qualitativeLastReviewed` should record an ISO `YYYY-MM-DD` date and be updated whenever a school's qualitative content is reviewed under the cadence documented in `docs/RESEARCH.md`'s Qualitative Review Cadence section - whether the periodic annual baseline or an event-driven trigger.
  </action>
  <verify>
    <automated>grep -q "## Tag Taxonomy" docs/DATA_MODEL.md && grep -q "## Qualitative Review Tracking" docs/DATA_MODEL.md && grep -q "qualitativeLastReviewed" docs/DATA_MODEL.md && for t in stem-focus languages-focus arts-music-focus bilingual-program special-pedagogical-model all-day-model inclusion-support transition-support structured-learning-environment active-school-community; do grep -q "$t" docs/DATA_MODEL.md || exit 1; done</automated>
  </verify>
  <done>docs/DATA_MODEL.md contains both new sections with all 10 tag IDs across 4 categories and the qualitativeLastReviewed field semantics, with no existing section reworded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Internal policy doc -> published pilot content (Phase 14) | Rules written here govern what a human/agent is later allowed to publish as a tag; a gap in the written rule becomes a gap in what gets published |
| Community observation -> triangulated evidence -> published tag | Unverified, subjective community claims cross into structured, citable evidence for exactly one tag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-12-01 | Tampering | docs/RESEARCH.md Community Source Independence section | mitigate | Fail-closed rule: conflicting or non-independent community evidence must never be published as a tag (D-11); independence log required per source (D-13) so every community-backed tag has an auditable trail |
| T-12-02 | Repudiation | docs/RESEARCH.md Qualitative Review Cadence + docs/DATA_MODEL.md qualitativeLastReviewed | mitigate | Distinct, dated field for qualitative review separate from factual research_status prevents stale qualitative claims from silently appearing "current"; hybrid periodic+event-driven cadence documented so staleness can be audited later |

</threat_model>

<verification>
Run `grep -c "^## " docs/RESEARCH.md` and `grep -c "^## " docs/DATA_MODEL.md` before and after the edits to confirm only additive section growth (count increases by exactly 2 in each file, no existing heading text removed). Manually diff-review both files against ROADMAP.md success criteria 1 and 2 (source hierarchy + independence; taxonomy shape).
</verification>

<success_criteria>
- `docs/RESEARCH.md` has `## Community Source Independence` and `## Qualitative Review Cadence` sections with all D-08 through D-14 and D-20 through D-23 content present.
- `docs/DATA_MODEL.md` has `## Tag Taxonomy` (all 10 tag IDs, 4 categories) and `## Qualitative Review Tracking` sections.
- No pre-existing sentence in either file was reworded outside the five specified edits.
</success_criteria>

<output>
After completion, create `.planning/phases/12-editorial-research-framework/12-01-SUMMARY.md`
</output>
