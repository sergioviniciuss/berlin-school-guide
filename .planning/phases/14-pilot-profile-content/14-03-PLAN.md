---
phase: 14-pilot-profile-content
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - src/content/schools/real/lichtenbergPrimarySchools/index.ts
autonomous: true
requirements:
  - PILOT-01
  - PILOT-02
  - NARR-02
must_haves:
  truths:
    - "Seepark-Grundschule has perfilDaEscola + qualitativeLastReviewed (expect zero tags)"
    - "Friedrichsfelder Schule has perfilDaEscola + qualitativeLastReviewed (expect zero tags)"
    - "Bernhard-Grzimek-Schule has perfilDaEscola + qualitativeLastReviewed (sparse tags or zero)"
    - "At least one of Grzimek/Seepark remains directory_only with thin perfil (PILOT-02)"
    - "Non-pilot school objects are unchanged"
    - "pnpm validate:data passes after authoring"
  artifacts:
    - path: "src/content/schools/real/lichtenbergPrimarySchools/index.ts"
      provides: "qualitative content on three thin/mid pilots"
      contains: "perfilDaEscola"
  key_links:
    - from: "primarySchool({ slug: seepark-grundschule })"
      to: "perfilDaEscola + qualitativeLastReviewed"
      via: "factory qualitative fields"
      pattern: "seepark-grundschule"
    - from: "perfil copy"
      to: "existing sources/verified fields only"
      via: "editorial citation discipline D-10"
      pattern: "perfilDaEscola:"
---

<objective>
Author sparse-but-honest PT-BR `perfilDaEscola` (and optional tags) on Seepark-Grundschule, Friedrichsfelder Schule, and Bernhard-Grzimek-Schule — the thin/neighbourhood pilots that prove PILOT-02 and the low-doc half of PILOT-01.

Purpose: Exercise D-05–D-11 on documentation-poor schools before rich pilots and community triangulation.
Output: Three pilots with perfil + qualitativeLastReviewed; non-pilots untouched; validate:data green.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-pilot-profile-content/14-02-SUMMARY.md
@.planning/phases/14-pilot-profile-content/14-CONTEXT.md
@docs/research/PILOT_SELECTION.md
@docs/EDITORIAL_GUIDE.md
@docs/DATA_MODEL.md
@src/content/schools/real/lichtenbergPrimarySchools/index.ts

<interfaces>
Pilot slugs for this plan ONLY:
- `seepark-grundschule`
- `friedrichsfelder-schule`
- `bernhard-grzimek-schule`

Do NOT touch in this plan:
- `richard-wagner-schule`, `lew-tolstoi-schule` (Plan 04)
- Non-pilots: `adam-ries-schule`, `buergermeister-ziethen-schule`, `grundschule-am-traenkegraben`, `schmetterlings-grundschule`, `karlshorster-schule`

Authoring shape via factory (Plan 02):
```typescript
primarySchool({
  // ...existing factual fields UNCHANGED...
  // tags?: [...]  // omit or [] when sparse-honest says zero
  perfilDaEscola: "…", // ~3–5 PT-BR sentences, D-08
  qualitativeLastReviewed: "YYYY-MM-DD", // authoring date, D-15
})
```

Editorial constraints (D-05–D-11, NARR-02):
- Sparse-but-honest; no tag quotas (D-06/D-07)
- Every perfil claim supportable from that school's existing sources/verified fields/tag citations — no new uncited facts (D-10)
- No ranking/superlatives/reputation language (D-11 / EDITORIAL_GUIDE Perfil voice)
- Facts≠tags: verified ganztag ≠ all-day-model; portrait MINT claim alone may not justify stem-focus — prefer zero tags if evidence is thin (esp. Seepark, Friedrichsfelder; Grzimek optional stem only if sustained beyond portrait list)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Seepark and Friedrichsfelder sparse profiles</name>
  <files>src/content/schools/real/lichtenbergPrimarySchools/index.ts</files>
  <read_first>
    - src/content/schools/real/lichtenbergPrimarySchools/index.ts (seepark + friedrichsfelder entries — sources and verified fields)
    - docs/EDITORIAL_GUIDE.md (Perfil da Escola Voice, Facts Versus Tags)
    - docs/research/PILOT_SELECTION.md (Seepark + Friedrichsfelder rationale)
    - .planning/phases/14-pilot-profile-content/14-CONTEXT.md (D-05–D-11, D-15, D-18–D-19)
  </read_first>
  <action>
    For **Seepark-Grundschule** and **Friedrichsfelder Schule** only:

    1. Add `perfilDaEscola`: one concise PT-BR paragraph (~3–5 sentences) synthesizing what families can verify from existing fields/sources and what remains unconfirmed. Expect **zero tags** (omit `tags` or leave absent) — typical neighbourhood / thinnest records (D-06).
    2. Set `qualitativeLastReviewed` to ISO `YYYY-MM-DD` of authoring (D-15).
    3. Do **not** invent tags for taxonomy coverage (D-07). Do **not** change factual FieldValues, sources, researchStatus, or coverageLevel. Do **not** touch non-pilot schools or Wagner/Tolstoi.

    Voice: parent-centered verified vs unconfirmed; no ranking/superlatives (D-11). Cite only evidence already on the record (D-10 / NARR-02).
  </action>
  <verify>
    <automated>pnpm validate:data</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "slug: \"seepark-grundschule\"" -A 80 src/content/schools/real/lichtenbergPrimarySchools/index.ts | rg 'perfilDaEscola:'` matches
    - `rg -n "slug: \"friedrichsfelder-schule\"" -A 80 src/content/schools/real/lichtenbergPrimarySchools/index.ts | rg 'perfilDaEscola:'` matches
    - `rg -n "slug: \"seepark-grundschule\"" -A 80 src/content/schools/real/lichtenbergPrimarySchools/index.ts | rg 'qualitativeLastReviewed:'` matches
    - `rg -n "slug: \"friedrichsfelder-schule\"" -A 80 src/content/schools/real/lichtenbergPrimarySchools/index.ts | rg 'qualitativeLastReviewed:'` matches
    - Prefer no `tags:` on these two (or empty); if tags appear, validate:data must still pass and perfil must be present
    - `git diff -- src/content/schools/real/lichtenbergPrimarySchools/index.ts` hunks limited to these two school objects + no non-pilot slug blocks
    - `pnpm validate:data` green
  </acceptance_criteria>
  <done>Seepark and Friedrichsfelder ship sparse perfil + review date; PILOT-02 thin schools represented.</done>
</task>

<task type="auto">
  <name>Task 2: Author Bernhard-Grzimek sparse profile</name>
  <files>src/content/schools/real/lichtenbergPrimarySchools/index.ts</files>
  <read_first>
    - src/content/schools/real/lichtenbergPrimarySchools/index.ts (bernhard-grzimek entry — portrait MINT claim)
    - docs/EDITORIAL_GUIDE.md (Facts Versus Tags)
    - docs/research/PILOT_SELECTION.md (Grzimek low-doc + STEM pressure-test)
    - src/features/schools/tagTaxonomy/index.ts (closed tag IDs)
  </read_first>
  <action>
    For **Bernhard-Grzimek-Schule** only:

    1. Add `perfilDaEscola` (~3–5 PT-BR sentences) synthesizing directory/portrait evidence — name that MINT/math-science portrait language is present if verified on the record, and clearly mark what is unconfirmed beyond the portrait (sparse-honest, PILOT-02).
    2. Tags: **default to zero**. Only add `stem-focus` (or similar closed ID) if evidence supports a sustained "what kind of school" claim **beyond** listing the portrait offer (D-05 / Facts Versus Tags). If tagging, citations must use existing `sources[]` sourceIds and confidence must match `deriveExpectedConfidence` via validateTagEvidence; always include `perfilDaEscola` + `qualitativeLastReviewed` (D-03/D-15).
    3. Do not invent sources. Do not alter non-pilot or other pilot schools. Preserve `directory_only` / factual coverage unchanged (D-19 / VAL-01 preview).
  </action>
  <verify>
    <automated>pnpm validate:data && pnpm exec jest src/features/evidence/validateTagEvidence --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "slug: \"bernhard-grzimek-schule\"" -A 100 src/content/schools/real/lichtenbergPrimarySchools/index.ts | rg 'perfilDaEscola:'` matches
    - `rg -n "slug: \"bernhard-grzimek-schule\"" -A 100 src/content/schools/real/lichtenbergPrimarySchools/index.ts | rg 'qualitativeLastReviewed:'` matches
    - `researchStatus` / coverage fields for Grzimek unchanged from pre-authoring (still directory_only / directory unless already otherwise)
    - `pnpm validate:data` green
    - Non-pilot slugs still lack `perfilDaEscola` (`rg -n 'perfilDaEscola' src/content/schools/real/lichtenbergPrimarySchools/index.ts` only under pilot objects authored so far)
  </acceptance_criteria>
  <done>Grzimek ships sparse perfil (+ optional honest tag); three thin/mid pilots complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editorial copy → public static site | Uncited or ranking claims in perfil become published parent-facing content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Spoofing / Integrity | perfilDaEscola claims | mitigate | D-10: every claim must map to existing sources/verified fields; human checklist in Plan 05; no new sources invented here |
| T-14-07 | Tampering | Non-pilot / coverage drift | mitigate | Diff discipline — only three pilot objects; do not refresh factual fields; validate:data must stay green without coverage% games |
| T-14-08 | Information disclosure | perfil / notes | mitigate | No parent names or private contact details in perfil text |
</threat_model>

<verification>
Three pilots have perfil + date; validate:data green; non-pilots unchanged; PILOT-02 satisfied via Seepark and/or Grzimek directory_only sparse profiles.
</verification>

<success_criteria>
- Seepark, Friedrichsfelder, Grzimek authored per D-05–D-11, D-15, D-18–D-19
- NARR-02 editorial discipline applied (schema cannot parse sentence→source; executor self-checks against sources)
</success_criteria>

<output>
After completion, create `.planning/phases/14-pilot-profile-content/14-03-SUMMARY.md`
</output>
