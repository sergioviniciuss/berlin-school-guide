---
phase: 14-pilot-profile-content
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - docs/DATA_MODEL.md
  - src/content/schools/real/lichtenbergPrimarySchools/index.ts
autonomous: true
requirements:
  - NARR-02
must_haves:
  truths:
    - "docs/DATA_MODEL.md documents perfilDaEscola as optional plain PT-BR string distinct from factual schoolProfile"
    - "docs/DATA_MODEL.md documents qualitativeResearchNotes and extended qualitativeLastReviewed gate (tags or perfil)"
    - "primarySchool() accepts optional tags, perfilDaEscola, qualitativeLastReviewed, qualitativeResearchNotes and passes them through on the returned School"
  artifacts:
    - path: "docs/DATA_MODEL.md"
      provides: "perfilDaEscola + qualitativeResearchNotes field shapes and cross-field rules"
      contains: "perfilDaEscola"
    - path: "src/content/schools/real/lichtenbergPrimarySchools/index.ts"
      provides: "BaseSchoolInput + primarySchool qualitative passthrough"
      contains: "perfilDaEscola"
  key_links:
    - from: "BaseSchoolInput"
      to: "primarySchool return"
      via: "optional qualitative fields on input spread onto School object"
      pattern: "perfilDaEscola"
    - from: "docs/DATA_MODEL.md"
      to: "schoolSchema"
      via: "documented field names matching D-01/D-02/D-17"
      pattern: "qualitativeResearchNotes"
---

<objective>
Document the Phase 14 qualitative field shapes in `docs/DATA_MODEL.md` and extend `primarySchool()` so pilots can author tags/perfil/notes without post-hoc spreads.

Purpose: Keep docs as source of truth (AGENTS.md) and make five-pilot authoring ergonomic before content plans.
Output: DATA_MODEL update + factory passthrough; still no qualitative copy on pilots.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-pilot-profile-content/14-01-SUMMARY.md
@.planning/phases/14-pilot-profile-content/14-CONTEXT.md
@.planning/phases/14-pilot-profile-content/14-PATTERNS.md
@docs/DATA_MODEL.md
@src/content/schools/real/lichtenbergPrimarySchools/index.ts
@src/features/schools/school/index.ts

<interfaces>
From schoolSchema (Plan 01):
```typescript
perfilDaEscola: z.string().trim().min(1).optional(),
qualitativeResearchNotes: z.string().trim().min(1).optional(),
tags: z.array(schoolTagSchema).optional(),
qualitativeLastReviewed: z.string().date().optional(),
```

Current BaseSchoolInput ends with factual FieldValue overrides only — extend with optional qualitative fields (Claude's discretion Pattern 4 — choose factory passthrough, not post-spread):
```typescript
type BaseSchoolInput = {
  // ...existing...
  tags?: School["tags"];
  perfilDaEscola?: string;
  qualitativeLastReviewed?: string;
  qualitativeResearchNotes?: string;
};
```

Pass through on `return { ... }` after `research` when present (omit undefined so non-pilots stay clean).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document perfilDaEscola and notes in DATA_MODEL</name>
  <files>docs/DATA_MODEL.md</files>
  <read_first>
    - docs/DATA_MODEL.md (Tag Taxonomy + Qualitative Review Tracking)
    - .planning/phases/14-pilot-profile-content/14-CONTEXT.md (D-01–D-04, D-16–D-17)
    - .planning/phases/14-pilot-profile-content/14-PATTERNS.md (DATA_MODEL section)
  </read_first>
  <action>
    Update `## Qualitative Review Tracking` (and add adjacent subsections as needed) to document:

    1. **`perfilDaEscola`** — optional plain PT-BR string on the school record (not MDX/Markdown); family-facing "Perfil da escola" synthesis; **distinct from** factual `schoolProfile` (`FieldValue<string>`, often German portrait/offers text with evidence status).
    2. **`qualitativeResearchNotes`** — optional research/withhold metadata string (not rendered as Perfil); used for durable triangulation withhold summaries per D-16.
    3. **Cross-field rules (Phase 14):** non-empty `tags` ⇒ `perfilDaEscola` required; (`tags` non-empty **or** `perfilDaEscola`) ⇒ `qualitativeLastReviewed` required (`YYYY-MM-DD`); schools with neither remain valid.
    4. Replace "Phase 13 implements" wording to note Phase 13 shipped tags + date, Phase 14 adds perfil/notes and extends the date gate to tags **or** perfil.

    Do not redefine the closed tag taxonomy. Do not invent UI rendering rules (Phase 15).
  </action>
  <verify>
    <automated>rg -n 'perfilDaEscola|qualitativeResearchNotes|schoolProfile' docs/DATA_MODEL.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'perfilDaEscola' docs/DATA_MODEL.md` matches with plain PT-BR / optional semantics
    - `rg -n 'qualitativeResearchNotes' docs/DATA_MODEL.md` matches
    - `rg -n 'schoolProfile' docs/DATA_MODEL.md` distinguishes factual FieldValue from perfilDaEscola
    - `rg -n 'qualitativeLastReviewed' docs/DATA_MODEL.md` mentions tags or perfil (extended gate)
  </acceptance_criteria>
  <done>DATA_MODEL documents perfilDaEscola, notes, and Phase 14 cross-field rules.</done>
</task>

<task type="auto">
  <name>Task 2: Extend primarySchool qualitative passthrough</name>
  <files>src/content/schools/real/lichtenbergPrimarySchools/index.ts</files>
  <read_first>
    - src/content/schools/real/lichtenbergPrimarySchools/index.ts (BaseSchoolInput, primarySchool return)
    - src/features/schools/school/index.ts (School type fields)
    - .planning/phases/14-pilot-profile-content/14-PATTERNS.md (factory section)
  </read_first>
  <action>
    Extend `BaseSchoolInput` with optional:
    - `tags?: School["tags"]`
    - `perfilDaEscola?: string`
    - `qualitativeLastReviewed?: string`
    - `qualitativeResearchNotes?: string`

    On `primarySchool` return object, pass through these fields when provided (only include if `input.* !== undefined`) so non-pilot calls stay identical. Do **not** author any pilot perfil/tags yet — that is Plans 03–04. Do not mutate factual fields or non-pilot entries.

    Discretion locked here: factory passthrough (not post-spread) for all five pilots in later plans.
  </action>
  <verify>
    <automated>pnpm validate:data && pnpm exec jest src/features/schools/school/index.test.ts --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'perfilDaEscola\?:' src/content/schools/real/lichtenbergPrimarySchools/index.ts` matches in BaseSchoolInput
    - `rg -n 'qualitativeResearchNotes\?:' src/content/schools/real/lichtenbergPrimarySchools/index.ts` matches
    - `rg -n 'perfilDaEscola:' src/content/schools/real/lichtenbergPrimarySchools/index.ts` shows passthrough in return (conditional ok)
    - No pilot slug yet has authored `perfilDaEscola:` string content (grep values only on factory type/return wiring)
    - `pnpm validate:data` green
  </acceptance_criteria>
  <done>Factory accepts qualitative fields; real schools still omit them and validate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docs/code contract | Field names in DATA_MODEL must match schema to avoid authoring against wrong shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Elevation of privilege / Integrity | DATA_MODEL vs schema drift | mitigate | Document exact field names `perfilDaEscola` / `qualitativeResearchNotes` matching Plan 01 schema; acceptance greps both |
| T-14-05 | Tampering | primarySchool passthrough | mitigate | Only pass optional fields when present; factual fields unchanged; validate:data still gates all schools |
</threat_model>

<verification>
DATA_MODEL names match schema; factory compiles; validate:data + school Jest green; no pilot qualitative copy yet.
</verification>

<success_criteria>
- Docs describe perfil vs schoolProfile correctly
- primarySchool ready for Plans 03–04 authoring
</success_criteria>

<output>
After completion, create `.planning/phases/14-pilot-profile-content/14-02-SUMMARY.md`
</output>
