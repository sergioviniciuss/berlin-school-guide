---
phase: 13-evidence-tag-schema-layer
plan: 03
type: tdd
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - src/features/evidence/validateTagEvidence/index.ts
  - src/features/evidence/validateTagEvidence/index.test.ts
  - src/features/evidence/validateTagEvidence/fixtures.ts
autonomous: true
requirements:
  - TAG-03
  - TAG-04
must_haves:
  truths:
    - "active-school-community with only community citations is rejected (never sole basis)"
    - "active-school-community requires ≥2 distinct triangulated_community sourceIds each with independenceLog plus ≥1 non-community allowlisted cite"
    - "Non-community tags reject any triangulated_community or anecdotal_reserved citation"
    - "Declared confidence must match derived confidence from citations (bidirectional)"
  artifacts:
    - path: "src/features/evidence/validateTagEvidence/index.ts"
      provides: "validateTagEvidence pure function"
      exports: ["validateTagEvidence"]
    - path: "src/features/evidence/validateTagEvidence/index.test.ts"
      provides: "Valid/invalid matrix for TAG-03/TAG-04"
      min_lines: 80
  key_links:
    - from: "validateTagEvidence"
      to: "school.sources[]"
      via: "Map by sourceId"
      pattern: "sourceById"
    - from: "validateTagEvidence"
      to: "acceptableFactualSourceTypes"
      via: "non-community corroboration set (D-07)"
      pattern: "acceptableFactualSourceTypes|isAcceptableFactualEvidence"
---

<objective>
Implement `validateTagEvidence` enforcing triangulation, independence logs, never-sole-basis, non-community tag restrictions, and bidirectional confidence↔citation consistency.

Purpose: Encode TAG-03/TAG-04 hard rules so invalid community evidence cannot pass schema later.
Output: Pure validator + fixture matrix covering accept/reject cases.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-evidence-tag-schema-layer/13-01-SUMMARY.md
@.planning/phases/13-evidence-tag-schema-layer/13-02-SUMMARY.md
@.planning/phases/13-evidence-tag-schema-layer/13-CONTEXT.md
@.planning/phases/13-evidence-tag-schema-layer/13-RESEARCH.md
@src/features/evidence/validateFieldCitations/index.ts
@src/features/evidence/sourceTypes/index.ts
@src/features/schools/tagTaxonomy/index.ts

<interfaces>
From Plan 01 (must exist before this plan):
```typescript
export const acceptableFactualSourceTypes = [
  "official_government", "official_inspection", "school_website",
  "public_dataset", "journalism",
] as const;
export function isAcceptableFactualEvidence(source: {
  type: SourceType; reliability: ReliabilityLevel;
}): boolean;
```

From Plan 02:
```typescript
export type SchoolTag = z.infer<typeof schoolTagSchema>;
// schoolTagSchema: { id, confidence, citations: TagCitation[] }
// TagCitation may include independenceLog?
```

From validateFieldCitations analog:
```typescript
const sourceById = new Map(sources.map((s) => [s.id, s]));
```

Target API:
```typescript
export function validateTagEvidence(
  tag: SchoolTag,
  sources: Source[],
): { ok: true } | { ok: false; messages: string[] };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing validateTagEvidence test matrix</name>
  <files>src/features/evidence/validateTagEvidence/index.test.ts, src/features/evidence/validateTagEvidence/fixtures.ts, src/features/evidence/validateTagEvidence/index.ts</files>
  <read_first>
    - src/features/evidence/validateFieldCitations/index.ts
    - src/features/evidence/validateFieldCitations/index.test.ts
    - src/features/evidence/source/fixtures.ts
    - src/features/schools/tagTaxonomy/index.ts
    - .planning/phases/13-evidence-tag-schema-layer/13-CONTEXT.md (D-07–D-09, D-20)
    - .planning/phases/13-evidence-tag-schema-layer/13-RESEARCH.md (Pattern 5 confidence predicates)
  </read_first>
  <behavior>
    Cases the suite MUST assert (ok:false unless noted):
    1. Unknown sourceId → reject
    2. active-school-community community-only (≥2 community, zero non-community) → reject (D-08)
    3. active-school-community with 1 community + 1 official → reject (<2 community, D-07)
    4. active-school-community missing independenceLog on a community citation → reject (D-10)
    5. active-school-community with 2 community (logs, distinct sourceIds, distinct venue) + 1 official → accept when confidence matches (ok:true)
    6. Non-community tag (e.g. stem-focus) citing triangulated_community → reject (D-09)
    7. Non-community tag citing anecdotal_reserved → reject
    8. confidence confirmed_official with only journalism cite → reject (D-20/D-18: need official_government|official_inspection|school_website)
    9. confidence confirmed_multi_source with single official cite → reject
    10. confidence partial when multi-source predicate satisfied → reject (bidirectional)
    11. stem-focus with one school_website cite + confirmed_official → accept
  </behavior>
  <action>
    Create `validateTagEvidence/fixtures.ts` composing Plan 01 source fixtures + independence logs. Create `index.test.ts` covering the matrix above.

    Create a minimal `index.ts` stub that always returns `{ ok: false, messages: ["not implemented"] }` so RED fails for intended accepts, OR omit implementation and let import fail — prefer stub export so tests compile and RED is clear.

    Commit style if atomic commits used: `test(13-03): add failing validateTagEvidence matrix`.
  </action>
  <verify>
    <automated>pnpm exec jest src/features/evidence/validateTagEvidence --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists and lists cases covering community-only, <2 community, missing log, valid triangulated, non-community+community reject, confidence mismatches
    - `rg -n 'active-school-community|independenceLog|confirmed_multi_source|confirmed_official' src/features/evidence/validateTagEvidence/index.test.ts` all present
    - Before Task 2 GREEN: at least one formerly-expected-ok case fails OR stub messages include "not implemented" — document RED in SUMMARY
  </acceptance_criteria>
  <done>Failing/RED matrix exists for TAG-03 and TAG-04 rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement validateTagEvidence rules</name>
  <files>src/features/evidence/validateTagEvidence/index.ts</files>
  <read_first>
    - src/features/evidence/validateTagEvidence/index.test.ts
    - src/features/evidence/validateTagEvidence/fixtures.ts
    - src/features/evidence/sourceTypes/index.ts
    - src/features/schools/tagTaxonomy/index.ts
  </read_first>
  <behavior>
    Same matrix as Task 1 — all must pass after implementation.
  </behavior>
  <action>
    Implement `validateTagEvidence(tag, sources)`:

    1. Build `Map` from `sources` by `id`. Unknown `sourceId` → fail message.
    2. For each citation whose resolved `source.type === "triangulated_community"`: require `citation.independenceLog` present and fully populated (already schema-valid if present). Optionally require distinct `independenceLog.venue` across community citations on the same tag (RESEARCH A6 — do enforce distinct venue strings).
    3. If `tag.id === "active-school-community"` (D-07/D-08):
       - Count distinct sourceIds with type `triangulated_community` → must be ≥2
       - Count citations whose type ∈ `acceptableFactualSourceTypes` (same five as D-07 non-community set) → must be ≥1
       - Community-only → reject regardless of confidence (never-sole-basis independent of partial)
    4. Else (D-09): reject if any resolved type is `triangulated_community` or `anecdotal_reserved`.
    5. Confidence consistency (D-20, bidirectional per RESEARCH A2):
       - Derive expected confidence:
         - `confirmed_official` if ≥1 cited source with type ∈ `{official_government, official_inspection, school_website}` AND NOT multi-source (see below)
         - `confirmed_multi_source` if ≥2 distinct sourceIds whose types are in `acceptableFactualSourceTypes` **or** (for community tag only) `triangulated_community`
         - else `partial` (e.g. sole journalism or sole public_dataset)
       - Precedence: if multi-source predicate holds → expected is `confirmed_multi_source` (even if an official cite exists). Else if official/school_website-class predicate holds → `confirmed_official`. Else → `partial`.
       - If `tag.confidence !== expected` → reject with message naming both.
    6. Per D-18: school_website qualifies for `confirmed_official`.
    7. Do not invent a parallel sources registry — resolve against the passed `sources` array only (D-12).

    Return `{ ok: true }` or `{ ok: false, messages: string[] }` with English technical messages (validators, not UI).
  </action>
  <verify>
    <automated>pnpm exec jest src/features/evidence/validateTagEvidence --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - All matrix tests green
    - `rg -n 'export function validateTagEvidence' src/features/evidence/validateTagEvidence/index.ts` present
    - `rg -n 'active-school-community' src/features/evidence/validateTagEvidence/index.ts` present
    - `rg -n 'independenceLog' src/features/evidence/validateTagEvidence/index.ts` present
    - `rg -n 'acceptableFactualSourceTypes|official_government|school_website' src/features/evidence/validateTagEvidence/index.ts` present
    - No high/medium/low confidence strings in the unit
  </acceptance_criteria>
  <done>validateTagEvidence enforces never-sole-basis, independence logs, D-09 restrictions, and bidirectional confidence consistency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tag citations → validateTagEvidence | Community claims must not become sole tag basis |
| Confidence label → parent interpretation | Mis-set confidence could impersonate stronger evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Spoofing | validateTagEvidence community-only path | mitigate | Reject community-only active-school-community; require ≥2 community + ≥1 non-community (D-07/D-08) |
| T-13-10 | Tampering | independenceLog enforcement | mitigate | Require independenceLog on every triangulated_community citation; distinct venue check |
| T-13-11 | Elevation of privilege | confidence consistency | mitigate | Bidirectional derived-vs-declared confidence; ban under/over-claiming multi-source/official |
| T-13-12 | Tampering | non-community tags | mitigate | Reject triangulated_community and anecdotal_reserved cites on all tags except active-school-community community path |
</threat_model>

<verification>
pnpm exec jest src/features/evidence/validateTagEvidence --no-coverage
</verification>

<success_criteria>
- TAG-03: triangulation + independence log + never sole basis enforced in code
- TAG-04: confidence↔citation consistency enforced; vocabulary remains evidence strength
- Fixture unit tests cover valid and invalid combinations
</success_criteria>

<output>
After completion, create `.planning/phases/13-evidence-tag-schema-layer/13-03-SUMMARY.md`
</output>
