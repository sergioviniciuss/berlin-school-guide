---
phase: 13-evidence-tag-schema-layer
plan: 04
type: execute
wave: 3
depends_on:
  - "03"
files_modified:
  - src/features/schools/school/index.ts
  - src/features/schools/school/fixtures.ts
  - src/features/schools/school/index.test.ts
autonomous: true
requirements:
  - TAG-01
  - TAG-03
must_haves:
  truths:
    - "schoolSchema accepts schools with omitted tags and without qualitativeLastReviewed"
    - "schoolSchema accepts a synthetic tagged school with qualitativeLastReviewed YYYY-MM-DD"
    - "schoolSchema rejects non-empty tags without qualitativeLastReviewed"
    - "schoolSchema.superRefine rejects invalid tag evidence via validateTagEvidence"
    - "pnpm validate:data remains green for all 10 real untagged Lichtenberg schools"
  artifacts:
    - path: "src/features/schools/school/index.ts"
      provides: "optional tags + qualitativeLastReviewed + tag superRefine gates"
      contains: "validateTagEvidence"
    - path: "src/features/schools/school/fixtures.ts"
      provides: "valid/invalid tagged school fixtures"
    - path: "src/features/schools/school/index.test.ts"
      provides: "TAG-01 integration tests"
  key_links:
    - from: "schoolSchema.superRefine"
      to: "validateTagEvidence"
      via: "loop over school.tags"
      pattern: "validateTagEvidence"
    - from: "schoolSchema.superRefine"
      to: "qualitativeLastReviewed"
      via: "required when tags.length > 0"
      pattern: "qualitativeLastReviewed"
---

<objective>
Wire optional `tags` and conditional `qualitativeLastReviewed` onto `schoolSchema`, connect `validateTagEvidence` in `superRefine`, and prove real schools stay valid without placeholder dates.

Purpose: Complete TAG-01 end-to-end on the school record while preserving VAL-01 (non-pilots untouched).
Output: Schema wiring + synthetic fixtures/tests; zero edits to real Lichtenberg school content.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-evidence-tag-schema-layer/13-03-SUMMARY.md
@.planning/phases/13-evidence-tag-schema-layer/13-CONTEXT.md
@src/features/schools/school/index.ts
@src/features/schools/school/fixtures.ts
@src/features/schools/researchMetadata/index.ts
@src/features/evidence/validateTagEvidence/index.ts
@src/features/schools/tagTaxonomy/index.ts

<interfaces>
Current schoolSchema object fields end with:
```typescript
sources: z.array(sourceSchema).min(1),
research: researchMetadataSchema,
```

Existing superRefine uses validateFieldCitations for verified fields. Append tag gates after field loop.

From Plan 02/03:
```typescript
import { schoolTagSchema } from "@/features/schools/tagTaxonomy";
import { validateTagEvidence } from "@/features/evidence/validateTagEvidence";
```

Conditional pattern (D-14): trigger is `(school.tags?.length ?? 0) > 0` — omitted tags OR `tags: []` must NOT require qualitativeLastReviewed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add optional tags + qualitativeLastReviewed to schoolSchema</name>
  <files>src/features/schools/school/index.ts, src/features/schools/school/fixtures.ts, src/features/schools/school/index.test.ts</files>
  <read_first>
    - src/features/schools/school/index.ts
    - src/features/schools/school/fixtures.ts
    - src/features/schools/school/index.test.ts
    - src/features/schools/researchMetadata/index.ts
    - src/features/schools/tagTaxonomy/index.ts
    - .planning/phases/13-evidence-tag-schema-layer/13-CONTEXT.md (D-13–D-15)
  </read_first>
  <behavior>
    - schoolSchema.parse(validDirectoryOnlySchool) still succeeds without tags/date
    - schoolSchema.parse({...valid, tags: [], /* no qualitativeLastReviewed */}) succeeds
    - schoolSchema rejects qualitativeLastReviewed: "2026-07-29T12:00:00Z" (must be YYYY-MM-DD via z.string().date())
    - schoolSchema.parse(validTaggedSchool with tags + qualitativeLastReviewed: "2026-07-29") succeeds when tag evidence valid
    - schoolSchema rejects tagged school missing qualitativeLastReviewed
  </behavior>
  <action>
    Add to `schoolSchema` object (both optional at object level — D-13/D-14/D-15):
    ```typescript
    tags: z.array(schoolTagSchema).optional(),
    qualitativeLastReviewed: z.string().date().optional(),
    ```

    In `superRefine`, after existing field citation gates:
    ```typescript
    for (const [index, tag] of (school.tags ?? []).entries()) {
      const result = validateTagEvidence(tag, school.sources);
      if (!result.ok) {
        for (const message of result.messages) {
          context.addIssue({ code: "custom", path: ["tags", index], message });
        }
      }
    }
    if ((school.tags?.length ?? 0) > 0 && !school.qualitativeLastReviewed) {
      context.addIssue({
        code: "custom",
        path: ["qualitativeLastReviewed"],
        message:
          "Schools with tags must include qualitativeLastReviewed (YYYY-MM-DD).",
      });
    }
    ```

    Update verified-field error message that still says "primary or secondary source" to mention factual allowlist / acceptable factual source types (align with Plan 01 D-05) — e.g. "Verified fields must cite at least one acceptable factual source (primary/secondary reliability and an allowlisted source type)."

    Add synthetic fixtures only (do **not** edit `src/content/schools/real/**`):
    - `validTaggedSchool` — spreads valid directory school; adds journalism/community/official sources as needed; one valid non-community tag OR valid active-school-community per Plan 03 rules; `qualitativeLastReviewed: "2026-07-29"`
    - `invalidTaggedWithoutReviewDate` — tags non-empty, omit qualitativeLastReviewed
    - `invalidCommunityOnlyTag` — active-school-community community-only (must throw via superRefine)
    - Keep `validDirectoryOnlySchool` unchanged (no tags)

    Extend `index.test.ts` for accept/reject cases above. Do **not** count tags in calculateEvidenceCoverage. Do **not** expand collectCitedSources for tags.
  </action>
  <verify>
    <automated>pnpm exec jest src/features/schools/school --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'tags:|qualitativeLastReviewed' src/features/schools/school/index.ts` both present
    - `rg -n 'validateTagEvidence' src/features/schools/school/index.ts` present
    - `rg -n 'qualitativeLastReviewed' src/features/schools/school/index.ts` shows conditional required issue when tags non-empty
    - `rg -n 'validTaggedSchool|invalidTaggedWithoutReviewDate|invalidCommunityOnlyTag' src/features/schools/school/fixtures.ts` all present
    - `rg -n 'qualitativeLastReviewed|tags' src/content/schools/real -g '*.ts'` shows no new tag authoring on real schools
    - Jest school suite green
  </acceptance_criteria>
  <done>schoolSchema supports optional cited tags with conditional qualitativeLastReviewed; invalid tag evidence rejected at parse.</done>
</task>

<task type="auto">
  <name>Task 2: Prove validate:data green for 10 real untagged schools</name>
  <files>src/features/schools/school/index.ts</files>
  <read_first>
    - src/features/schools/validateSchools/index.ts
    - package.json (validate:data script)
  </read_first>
  <action>
    Run the production data gate. Confirm all 10 Lichtenberg real schools still parse — tags omitted, no placeholder qualitativeLastReviewed (D-15 / VAL-01 preview).

    If validate:data fails due to Plan 01 allowlist tightening on existing real data, fix only schema/validator bugs or genuine data issues that block the gate — do **not** author tags on real schools. Do not add placeholder dates to real records.

    Also run a broader Jest slice to catch exhaustiveness regressions:
    `pnpm exec jest src/features/evidence src/features/schools --no-coverage`
  </action>
  <verify>
    <automated>pnpm validate:data && pnpm exec jest src/features/evidence src/features/schools --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm validate:data` exits 0
    - Jest evidence + schools suites green
    - Real school files under `src/content/schools/real/` have no `tags:` or `qualitativeLastReviewed` fields added this phase (`rg -L` / `rg -n 'tags:' src/content/schools/real` returns no authored tags)
  </acceptance_criteria>
  <done>Data gate green; non-pilot schools remain untagged; Phase 13 schema layer ready for Phase 14 authoring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Real school TS → schoolSchema.parse / validate:data | Production static data must remain valid without qualitative placeholders |
| Synthetic tagged fixtures → superRefine | Invalid community evidence must fail before Phase 14 authoring |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-13 | Repudiation | qualitativeLastReviewed | mitigate | Required iff non-empty tags; ISO date only; omitted on untagged reals (D-14/D-15) |
| T-13-14 | Tampering | schoolSchema.superRefine tags loop | mitigate | Every tag runs validateTagEvidence; community-only fixtures must throw |
| T-13-15 | Tampering | real Lichtenberg records | mitigate | Plan explicitly forbids tagging real schools; acceptance_criteria greps real/ for tags |
| T-13-16 | Elevation of privilege | confidence on school record | accept | Semantic risk already mitigated in Plan 03 validators; UI chips deferred Phase 15 |
</threat_model>

<verification>
pnpm exec jest src/features/schools/school src/features/evidence/validateTagEvidence --no-coverage
pnpm validate:data
</verification>

<success_criteria>
- TAG-01 fully on schoolSchema (optional tags + citations + confidence + conditional qualitativeLastReviewed)
- TAG-03 wired through school parse path
- Fixture tests valid + invalid; validate:data green for 10 untagged reals
</success_criteria>

<output>
After completion, create `.planning/phases/13-evidence-tag-schema-layer/13-04-SUMMARY.md`
</output>
