---
phase: 13-evidence-tag-schema-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/evidence/sourceTypes/index.ts
  - src/features/evidence/sourceTypes/index.test.ts
  - src/features/evidence/formatSourceType/index.ts
  - src/features/evidence/formatSourceType/index.test.ts
  - src/features/evidence/validateFieldCitations/index.ts
  - src/features/evidence/validateFieldCitations/index.test.ts
  - src/features/evidence/source/fixtures.ts
  - src/features/schools/collectCitedSources/index.ts
  - docs/RESEARCH.md
autonomous: true
requirements:
  - TAG-02
must_haves:
  truths:
    - "sourceTypeSchema accepts journalism and triangulated_community; reliabilityLevelSchema stays primary|secondary|anecdotal|unknown"
    - "Verified field citations reject triangulated_community even when reliability is secondary; accept journalism with secondary reliability"
    - "formatSourceType returns PT-BR for journalism and triangulated_community and null for anecdotal_reserved"
    - "docs/RESEARCH.md Reliability Levels no longer lists triangulated_community as a reliability value"
  artifacts:
    - path: "src/features/evidence/sourceTypes/index.ts"
      provides: "Extended sourceTypeSchema + isAcceptableFactualEvidence allowlist"
      exports:
        - "sourceTypeSchema"
        - "acceptableFactualSourceTypes"
        - "isAcceptableFactualEvidence"
    - path: "src/features/evidence/formatSourceType/index.ts"
      provides: "PT-BR labels for new displayable source types"
    - path: "docs/RESEARCH.md"
      provides: "Corrected Reliability Levels (D-06)"
      contains: "primary | secondary | anecdotal | unknown"
  key_links:
    - from: "src/features/evidence/validateFieldCitations/index.ts"
      to: "src/features/evidence/sourceTypes/index.ts"
      via: "isAcceptableFactualEvidence"
      pattern: "isAcceptableFactualEvidence"
    - from: "src/features/schools/collectCitedSources/index.ts"
      to: "SourceType"
      via: "SOURCE_TYPE_ORDER includes journalism + triangulated_community"
      pattern: "journalism"
---

<objective>
Extend the orthogonal source-type axis with `journalism` and `triangulated_community`, harden factual verification with a sourceType allowlist, ship PT-BR format labels, and correct Reliability Levels doc drift.

Purpose: TAG-02 foundation so community cannot verify factual fields and displayable tiers are distinct from `anecdotal_reserved`.
Output: Updated enums, factual allowlist, formatSourceType labels, validateFieldCitations gate, SOURCE_TYPE_ORDER, docs/RESEARCH.md fix.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-evidence-tag-schema-layer/13-CONTEXT.md
@.planning/phases/13-evidence-tag-schema-layer/13-RESEARCH.md
@.planning/phases/13-evidence-tag-schema-layer/13-PATTERNS.md

<interfaces>
<!-- Contracts executors must preserve / extend. -->

From src/features/evidence/sourceTypes/index.ts:
```typescript
export const sourceTypeSchema = z.enum([
  "official_government",
  "official_inspection",
  "school_website",
  "public_dataset",
  "anecdotal_reserved",
]);
export const reliabilityLevelSchema = z.enum([
  "primary", "secondary", "anecdotal", "unknown",
]);
export function isAcceptableReliabilityLevel(
  reliability: ReliabilityLevel,
): reliability is AcceptableReliabilityLevel;
```

From src/features/evidence/validateFieldCitations/index.ts:
```typescript
export function validateFieldCitations(
  evidence: FieldEvidence,
  sources: Source[],
): { hasUnknownSource: boolean; hasAcceptableSource: boolean };
// Today hasAcceptableSource uses isAcceptableReliabilityLevel only — replace with isAcceptableFactualEvidence.
```

From src/features/evidence/formatSourceType/index.ts:
```typescript
export function formatSourceType(type: SourceType): string | null;
// anecdotal_reserved → null; other types → PT-BR label via Record.
```

Locked PT-BR (methodology / RESEARCH):
- journalism → "Jornalismo independente"
- triangulated_community → "Fontes comunitárias trianguladas"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend sourceTypeSchema + factual allowlist + D-06 doc fix</name>
  <files>src/features/evidence/sourceTypes/index.ts, src/features/evidence/sourceTypes/index.test.ts, src/features/evidence/source/fixtures.ts, docs/RESEARCH.md</files>
  <read_first>
    - src/features/evidence/sourceTypes/index.ts
    - src/features/evidence/sourceTypes/index.test.ts
    - src/features/evidence/source/fixtures.ts
    - docs/RESEARCH.md (## Canonical Source Types and ## Reliability Levels)
    - .planning/phases/13-evidence-tag-schema-layer/13-CONTEXT.md (D-01–D-06)
  </read_first>
  <behavior>
    - sourceTypeSchema.parse("journalism") and parse("triangulated_community") succeed
    - reliabilityLevelSchema.safeParse("triangulated_community").success === false
    - isAcceptableFactualEvidence({ type: "journalism", reliability: "secondary" }) === true
    - isAcceptableFactualEvidence({ type: "triangulated_community", reliability: "secondary" }) === false
    - isAcceptableFactualEvidence({ type: "anecdotal_reserved", reliability: "primary" }) === false
    - isAcceptableFactualEvidence({ type: "official_government", reliability: "primary" }) === true
  </behavior>
  <action>
    Per D-01/D-02/D-03: insert `"journalism"` and `"triangulated_community"` into `sourceTypeSchema` **before** `"anecdotal_reserved"`. Do **not** add either to `reliabilityLevelSchema` (stays `primary | secondary | anecdotal | unknown`).

    Per D-05: add and export:
    ```typescript
    export const acceptableFactualSourceTypes = [
      "official_government",
      "official_inspection",
      "school_website",
      "public_dataset",
      "journalism",
    ] as const;

    export function isAcceptableFactualEvidence(source: {
      type: SourceType;
      reliability: ReliabilityLevel;
    }): boolean {
      return (
        isAcceptableReliabilityLevel(source.reliability) &&
        (acceptableFactualSourceTypes as readonly string[]).includes(source.type)
      );
    }
    ```
    Exclude `triangulated_community` and `anecdotal_reserved` from the allowlist (allowlist-only, not exclusion-only).

    Per D-04 authoring defaults, add fixtures in `source/fixtures.ts`: `journalismSource` (`type: "journalism"`, `reliability: "secondary"`); `communitySourceA` and `communitySourceB` (`type: "triangulated_community"`, `reliability: "anecdotal"`, distinct `id`/`publisher`).

    Per D-06: rewrite `docs/RESEARCH.md` ## Reliability Levels so the ladder is only `primary`, `secondary`, `anecdotal`, `unknown`. Remove the bullet that presents `triangulated_community` as a reliability level. Point community evidence to Canonical Source Types + Community Source Independence. Do not change Canonical Source Types membership.
  </action>
  <verify>
    <automated>pnpm exec jest src/features/evidence/sourceTypes --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'journalism|triangulated_community' src/features/evidence/sourceTypes/index.ts` shows both in sourceTypeSchema
    - `rg -n 'reliabilityLevelSchema' -A6 src/features/evidence/sourceTypes/index.ts` shows only primary|secondary|anecdotal|unknown
    - `rg -n 'isAcceptableFactualEvidence|acceptableFactualSourceTypes' src/features/evidence/sourceTypes/index.ts` both present
    - `rg -n 'triangulated_community' docs/RESEARCH.md` appears under Canonical Source Types / Community sections, NOT as a Reliability Levels bullet (Reliability Levels section lists only the four reliability values)
    - Jest sourceTypes suite green
  </acceptance_criteria>
  <done>Enums, allowlist, fixtures, and D-06 doc correction shipped; reliability remains four-value; community is source type only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire factual allowlist into field citations + format labels + SOURCE_TYPE_ORDER</name>
  <files>src/features/evidence/validateFieldCitations/index.ts, src/features/evidence/validateFieldCitations/index.test.ts, src/features/evidence/formatSourceType/index.ts, src/features/evidence/formatSourceType/index.test.ts, src/features/schools/collectCitedSources/index.ts</files>
  <read_first>
    - src/features/evidence/validateFieldCitations/index.ts
    - src/features/evidence/validateFieldCitations/index.test.ts
    - src/features/evidence/formatSourceType/index.ts
    - src/features/evidence/formatSourceType/index.test.ts
    - src/features/schools/collectCitedSources/index.ts
    - src/features/evidence/source/fixtures.ts
  </read_first>
  <behavior>
    - validateFieldCitations with journalism+secondary → hasAcceptableSource true
    - validateFieldCitations with triangulated_community+secondary → hasAcceptableSource false
    - formatSourceType("journalism") === "Jornalismo independente"
    - formatSourceType("triangulated_community") === "Fontes comunitárias trianguladas"
    - formatSourceType("anecdotal_reserved") === null
  </behavior>
  <action>
    In `validateFieldCitations`, replace `isAcceptableReliabilityLevel(source.reliability)` with `isAcceptableFactualEvidence(source)` (keep return shape `{ hasUnknownSource, hasAcceptableSource }` — per D-05). Extend tests with journalism-pass and community-fail cases using new fixtures.

    Per D-21/D-22: extend `formatSourceType` labels Record:
    - `journalism: "Jornalismo independente"`
    - `triangulated_community: "Fontes comunitárias trianguladas"`
    Keep `anecdotal_reserved` → `null`.

    In `collectCitedSources`, append `"journalism"` and `"triangulated_community"` to `SOURCE_TYPE_ORDER` after `public_dataset`. Do **not** collect tag citations (Phase 15). Do **not** add `anecdotal_reserved` to the order.

    Optionally update the school superRefine error string later in Plan 04; this plan only changes the predicate + format/order units.
  </action>
  <verify>
    <automated>pnpm exec jest src/features/evidence/validateFieldCitations src/features/evidence/formatSourceType --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'isAcceptableFactualEvidence' src/features/evidence/validateFieldCitations/index.ts` matches
    - `rg -n 'isAcceptableReliabilityLevel' src/features/evidence/validateFieldCitations/index.ts` has no remaining use in hasAcceptableSource
    - `rg -n 'Jornalismo independente|Fontes comunitárias trianguladas' src/features/evidence/formatSourceType/index.ts` both present
    - `rg -n 'journalism|triangulated_community' src/features/schools/collectCitedSources/index.ts` both in SOURCE_TYPE_ORDER
    - Jest validateFieldCitations + formatSourceType green
  </acceptance_criteria>
  <done>Factual field gate uses allowlist; displayable source types format correctly; Fontes order includes new displayable types without tag merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author/fixture data → Zod parse | Untrusted static school records cross into validated domain objects |
| Community sourceType → factual field verification | Community must not satisfy verified-field gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | validateFieldCitations | mitigate | isAcceptableFactualEvidence require reliability∈{primary,secondary} AND sourceType∈allowlist excluding triangulated_community/anecdotal_reserved |
| T-13-02 | Spoofing | sourceTypes reliability enum | mitigate | Do not add triangulated_community to reliabilityLevelSchema; D-06 doc fix prevents author mis-encoding |
| T-13-03 | Information disclosure | formatSourceType | mitigate | anecdotal_reserved continues returning null (D-22) |
| T-13-04 | Elevation of privilege | collectCitedSources | accept | Extending SOURCE_TYPE_ORDER without tag merge is low risk; tag→Fontes deferred Phase 15 |
</threat_model>

<verification>
pnpm exec jest src/features/evidence/sourceTypes src/features/evidence/validateFieldCitations src/features/evidence/formatSourceType --no-coverage
rg -n 'triangulated_community' docs/RESEARCH.md
</verification>

<success_criteria>
- TAG-02: journalism + triangulated_community exist as source types; reliability unchanged; anecdotal_reserved non-displayable
- D-05 factual allowlist live in field citation validation
- D-06 Reliability Levels corrected
</success_criteria>

<output>
After completion, create `.planning/phases/13-evidence-tag-schema-layer/13-01-SUMMARY.md`
</output>
