---
phase: 13-evidence-tag-schema-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/evidence/independenceLog/index.ts
  - src/features/evidence/independenceLog/index.test.ts
  - src/features/evidence/tagCitation/index.ts
  - src/features/evidence/tagCitation/index.test.ts
  - src/features/schools/tagTaxonomy/index.ts
  - src/features/schools/tagTaxonomy/index.test.ts
  - src/features/evidence/formatTagConfidence/index.ts
  - src/features/evidence/formatTagConfidence/index.test.ts
autonomous: true
requirements:
  - TAG-01
  - TAG-04
must_haves:
  truths:
    - "Closed 10-id tag taxonomy enum parses all Phase 12 IDs and rejects unknown IDs"
    - "Confidence enum is confirmed_multi_source | confirmed_official | partial with locked methodology PT-BR labels"
    - "Tag citation schema supports optional independenceLog nested on the citation (not on Source)"
    - "schoolTagSchema requires id + confidence + ≥1 citation"
  artifacts:
    - path: "src/features/evidence/independenceLog/index.ts"
      provides: "independenceLogSchema with D-11 fields"
      exports: ["independenceLogSchema", "IndependenceLog"]
    - path: "src/features/evidence/tagCitation/index.ts"
      provides: "tagCitationSchema with optional independenceLog"
      exports: ["tagCitationSchema", "TagCitation"]
    - path: "src/features/schools/tagTaxonomy/index.ts"
      provides: "tagTaxonomyIdSchema, tagConfidenceSchema, schoolTagSchema, formatTagId"
      exports: ["tagTaxonomyIdSchema", "tagConfidenceSchema", "schoolTagSchema", "formatTagId"]
    - path: "src/features/evidence/formatTagConfidence/index.ts"
      provides: "D-17 PT-BR confidence labels"
      exports: ["formatTagConfidence"]
  key_links:
    - from: "src/features/evidence/tagCitation/index.ts"
      to: "src/features/evidence/independenceLog/index.ts"
      via: "independenceLog optional field"
      pattern: "independenceLog"
    - from: "src/features/schools/tagTaxonomy/index.ts"
      to: "src/features/evidence/tagCitation/index.ts"
      via: "citations: z.array(tagCitationSchema).min(1)"
      pattern: "tagCitationSchema"
---

<objective>
Create the closed tag taxonomy, confidence vocabulary, independence-log schema, tag citation shape, and PT-BR format helpers — without wiring schoolSchema yet.

Purpose: Lock TAG-01/TAG-04 contracts and D-17/D-21 label strings before validators and school integration.
Output: New colocated Zod units under evidence/ and schools/tagTaxonomy with Jest coverage.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-evidence-tag-schema-layer/13-CONTEXT.md
@.planning/phases/13-evidence-tag-schema-layer/13-RESEARCH.md
@.planning/phases/13-evidence-tag-schema-layer/13-PATTERNS.md
@docs/DATA_MODEL.md
@src/content/guides/methodology.mdx
@src/features/schools/schoolClassification/index.ts
@src/features/evidence/formatSourceType/index.ts
@src/features/evidence/fieldEvidence/index.ts

<interfaces>
<!-- Patterns to mirror. Plan 01 source types are NOT required for this plan (parallel Wave 1). -->

From src/features/evidence/fieldEvidence/index.ts:
```typescript
export const fieldCitationSchema = z.object({
  sourceId: z.string().min(1),
  quote: z.string().optional(),
  note: z.string().optional(),
});
// Keep fieldCitationSchema lean — do NOT add independenceLog here (D-10).
```

From src/features/evidence/formatSourceType/index.ts:
```typescript
export function formatSourceType(type: SourceType): string | null;
// Mirror Record<Enum, string> map pattern for formatTagConfidence / formatTagId.
```

Locked confidence IDs (D-16) and PT-BR (D-17):
- confirmed_multi_source → "Confirmado por múltiplas fontes"
- confirmed_official → "Confirmado por fonte oficial"
- partial → "Evidência parcial"

Locked taxonomy IDs (Phase 12 / DATA_MODEL):
stem-focus, languages-focus, arts-music-focus, bilingual-program, special-pedagogical-model,
all-day-model, inclusion-support, transition-support, structured-learning-environment,
active-school-community
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Independence log + tag citation schemas</name>
  <files>src/features/evidence/independenceLog/index.ts, src/features/evidence/independenceLog/index.test.ts, src/features/evidence/tagCitation/index.ts, src/features/evidence/tagCitation/index.test.ts</files>
  <read_first>
    - src/features/evidence/fieldEvidence/index.ts
    - .planning/phases/13-evidence-tag-schema-layer/13-CONTEXT.md (D-10–D-12)
    - .planning/phases/13-evidence-tag-schema-layer/13-RESEARCH.md (Pattern 2 independence log keys)
  </read_first>
  <behavior>
    - independenceLogSchema accepts { venue, identifier, dateAccessed: "2026-07-29", independenceRationale, echoCheckNote } all non-empty strings; dateAccessed rejects ISO datetime with time
    - tagCitationSchema accepts { sourceId: "x" } without independenceLog
    - tagCitationSchema accepts citation with full independenceLog nested
    - Do not export or require independenceLog on Source
  </behavior>
  <action>
    Create `src/features/evidence/independenceLog/` per D-10/D-11 with exact property names (Claude discretion locked by RESEARCH A1):
    ```typescript
    export const independenceLogSchema = z.object({
      venue: z.string().min(1),
      identifier: z.string().min(1),
      dateAccessed: z.string().date(),
      independenceRationale: z.string().min(1),
      echoCheckNote: z.string().min(1),
    });
    export type IndependenceLog = z.infer<typeof independenceLogSchema>;
    ```

    Create `src/features/evidence/tagCitation/` per D-12 (citation-by-sourceId, reuse school sources registry later):
    ```typescript
    export const tagCitationSchema = z.object({
      sourceId: z.string().min(1),
      quote: z.string().optional(),
      note: z.string().optional(),
      independenceLog: independenceLogSchema.optional(),
    });
    ```
    independenceLog is optional at Zod parse; Plan 03 `validateTagEvidence` requires it when resolved source type is `triangulated_community`.

    Do **not** modify `sourceSchema` or `fieldCitationSchema`. Colocate `index.test.ts` for both units.
  </action>
  <verify>
    <automated>pnpm exec jest src/features/evidence/independenceLog src/features/evidence/tagCitation --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - Files exist: independenceLog/index.ts, tagCitation/index.ts (+ tests)
    - `rg -n 'venue|identifier|dateAccessed|independenceRationale|echoCheckNote' src/features/evidence/independenceLog/index.ts` all five present
    - `rg -n 'independenceLog' src/features/evidence/tagCitation/index.ts` present
    - `rg -n 'independenceLog' src/features/evidence/source/index.ts` has no matches
    - `rg -n 'independenceLog' src/features/evidence/fieldEvidence/index.ts` has no matches
    - Jest suites green
  </acceptance_criteria>
  <done>Independence log nests on tag citations only; field/source schemas untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tag taxonomy, confidence enum, schoolTagSchema, format helpers</name>
  <files>src/features/schools/tagTaxonomy/index.ts, src/features/schools/tagTaxonomy/index.test.ts, src/features/evidence/formatTagConfidence/index.ts, src/features/evidence/formatTagConfidence/index.test.ts</files>
  <read_first>
    - src/features/schools/schoolClassification/index.ts
    - src/features/evidence/formatSourceType/index.ts
    - docs/DATA_MODEL.md (## Tag Taxonomy)
    - src/content/guides/methodology.mdx (confidence label strings)
    - .planning/phases/13-evidence-tag-schema-layer/13-CONTEXT.md (D-16–D-21)
  </read_first>
  <behavior>
    - tagTaxonomyIdSchema accepts all 10 IDs; rejects "best-school"
    - tagConfidenceSchema accepts only confirmed_multi_source | confirmed_official | partial; rejects "high"|"medium"|"low"
    - schoolTagSchema requires citations.min(1); rejects empty citations
    - formatTagConfidence returns exact D-17 strings
    - formatTagId returns non-empty PT-BR for every taxonomy id; active-school-community → "Comunidade escolar ativa"
  </behavior>
  <action>
    Create `src/features/schools/tagTaxonomy/index.ts` mirroring schoolClassification closed enum:

    ```typescript
    export const tagTaxonomyIdSchema = z.enum([
      "stem-focus",
      "languages-focus",
      "arts-music-focus",
      "bilingual-program",
      "special-pedagogical-model",
      "all-day-model",
      "inclusion-support",
      "transition-support",
      "structured-learning-environment",
      "active-school-community",
    ]);

    export const tagConfidenceSchema = z.enum([
      "confirmed_multi_source",
      "confirmed_official",
      "partial",
    ]);

    export const schoolTagSchema = z.object({
      id: tagTaxonomyIdSchema,
      confidence: tagConfidenceSchema,
      citations: z.array(tagCitationSchema).min(1),
    });
    ```

    Export types via z.infer. Add `formatTagId(id)` with Record of all 10 IDs using RESEARCH recommended PT-BR labels (non-superlative; no "melhor"/"excelente"):
    | ID | Label |
    |----|-------|
    | stem-focus | Foco em STEM |
    | languages-focus | Foco em idiomas |
    | arts-music-focus | Foco em artes e música |
    | bilingual-program | Programa bilíngue |
    | special-pedagogical-model | Modelo pedagógico específico |
    | all-day-model | Modelo de período integral |
    | inclusion-support | Apoio à inclusão |
    | transition-support | Apoio na transição |
    | structured-learning-environment | Ambiente de aprendizagem estruturado |
    | active-school-community | Comunidade escolar ativa |

    Optionally export a category map constant (academic focus / program model / support / school environment) for Phase 15 — not required for validators.

    Create `src/features/evidence/formatTagConfidence/`:
    ```typescript
    export function formatTagConfidence(confidence: TagConfidence): string {
      // confirmed_multi_source → "Confirmado por múltiplas fontes"
      // confirmed_official → "Confirmado por fonte oficial"
      // partial → "Evidência parcial"
    }
    ```
    Per D-19: no high/medium/low anywhere. Per D-21: helpers ship now even without UI.
  </action>
  <verify>
    <automated>pnpm exec jest src/features/schools/tagTaxonomy src/features/evidence/formatTagConfidence --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'stem-focus|active-school-community' src/features/schools/tagTaxonomy/index.ts` both present
    - `rg -n 'confirmed_multi_source|confirmed_official|partial' src/features/schools/tagTaxonomy/index.ts` all present
    - `rg -n 'high|medium|low' src/features/schools/tagTaxonomy/index.ts` no confidence vocabulary matches
    - `rg -n 'Confirmado por múltiplas fontes|Confirmado por fonte oficial|Evidência parcial' src/features/evidence/formatTagConfidence/index.ts` all three exact
    - `rg -n 'Comunidade escolar ativa' src/features/schools/tagTaxonomy/index.ts` present
    - Jest suites green
  </acceptance_criteria>
  <done>Closed taxonomy + confidence enums and format helpers lock TAG-01 shape and TAG-04 vocabulary before school wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author tag payload → schoolTagSchema | Untrusted tag objects must fail closed on unknown IDs / confidence |
| Independence log attachment layer | Wrong layer would weaken triangulation auditability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Elevation of privilege | tagConfidenceSchema | mitigate | Closed enum only D-16 values; tests reject high/medium/low (D-19) — confidence = evidence strength not quality |
| T-13-06 | Tampering | independenceLog placement | mitigate | Log only on tagCitation; forbidden on Source/fieldCitation (D-10) |
| T-13-07 | Spoofing | tagTaxonomyIdSchema | mitigate | z.enum of exact 10 IDs; unknown IDs fail parse |
| T-13-08 | Repudiation | independenceLogSchema | mitigate | Required fields venue/identifier/dateAccessed/rationale/echoCheckNote when later enforced for community cites |
</threat_model>

<verification>
pnpm exec jest src/features/evidence/independenceLog src/features/evidence/tagCitation src/features/schools/tagTaxonomy src/features/evidence/formatTagConfidence --no-coverage
</verification>

<success_criteria>
- TAG-01 schema pieces exist (taxonomy id + confidence + ≥1 citation)
- TAG-04 confidence vocabulary + PT-BR helpers match methodology
- D-10–D-12 independence log on citation; D-16–D-21 labels locked
</success_criteria>

<output>
After completion, create `.planning/phases/13-evidence-tag-schema-layer/13-02-SUMMARY.md`
</output>
