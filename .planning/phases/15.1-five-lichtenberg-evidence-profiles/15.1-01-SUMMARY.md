---
phase: 15.1-five-lichtenberg-evidence-profiles
plan: 01
subsystem: data
tags: [school-factory, evidence, cited-missing, lichtenberg]

requires:
  - phase: 15-profile-ui-tags-narrative
    provides: School records and profile rendering that consume the Lichtenberg primary factory
provides:
  - Optional primarySchool overrides for classification, level, grades, portrait date, and a null website
  - citedMissing helper for checked-but-silent fields
affects:
  - Plans 15.1-02 through 15.1-06 school records
  - Plan 15.1-07 directory length tests

tech-stack:
  added: []
  patterns:
    - "Omitted factory arguments keep public, primary, grades 1–6, and the spike portrait date"
    - "A null website is missing evidence cited to the portrait, not an invented URL"
    - "citedMissing cites the sources that were read; missing() stays citation-empty"

key-files:
  created: []
  modified:
    - src/content/schools/real/lichtenbergPrimarySchools/index.ts

key-decisions:
  - "portraitDateAccessed is passed only when it is a string, so the officialPortraitSource default parameter still applies to the existing ten"
  - "Task 2 did not get a second commit because it confirmed the Task 1 diff and added no hunks"

patterns-established:
  - "New Lichtenberg records opt into private classification, portrait grades, and a real portrait access date through BaseSchoolInput"

requirements-completed:
  - "Existing Phase 14–15 pilot schools remain valid; no schema or UI regressions"

duration: 1min
completed: 2026-09-23
---

# Phase 15.1 Plan 01: Factory Overrides Summary

**primarySchool() can record a private school, portrait grades, a real portrait date, and a missing website without changing the existing ten**

## Performance

- **Duration:** 1 min
- **Started:** 2026-09-23T19:26:59Z
- **Completed:** 2026-09-23T19:28:25Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments
- Optional `classification`, `level`, `primarySectionDescription`, `gradesServed`, and `portraitDateAccessed` on `BaseSchoolInput`
- A `null` website stores cited missing evidence instead of a directory URL
- `citedMissing()` cites the sources that were checked and stayed silent
- Ganztag and offers inference fallbacks are unchanged for callers that omit `afterSchoolCare`, `schoolProfile`, and `pedagogyFocus`

## Task Commits

Each task was committed atomically:

1. **Task 1: Add optional factory overrides and citedMissing** - `120a0d8` (feat)
2. **Task 2: Confirm the ten call sites and inference fallback are untouched** - verified against `120a0d8`; the diff contained only the allowed hunks, so there was no second commit

**Plan metadata:** pending docs commit

## Files Created/Modified
- `src/content/schools/real/lichtenbergPrimarySchools/index.ts` - optional factory overrides and `citedMissing`

## Decisions Made
- Call `officialPortraitSource` with four arguments only when `portraitDateAccessed` is a string. Passing `undefined` would skip the default `2026-07-10` date.
- Leave `missing()` citation-empty. Later records use `citedMissing` when a page was read and was silent.

## Deviations from Plan

None - plan executed exactly as written.

Task 2 is a confirmation of the Task 1 diff. Acceptance greps, the ten-slug diff check, `pnpm validate:data`, and the directory and filter Jest suites passed. No additional lines were required, so Task 2 did not create an empty commit.

## Issues Encountered
None

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
Ready for 15.1-02. The factory can accept the five new records. The directory still has 10 schools.

## Self-Check: PASSED

- FOUND: `src/content/schools/real/lichtenbergPrimarySchools/index.ts`
- FOUND commits: `120a0d8`
- `corepack pnpm validate:data` exited 0 (10 real records)
- Jest `schoolDirectoryData` and `filterSchools` passed (13 tests)

---
*Phase: 15.1-five-lichtenberg-evidence-profiles*
*Completed: 2026-09-23*
