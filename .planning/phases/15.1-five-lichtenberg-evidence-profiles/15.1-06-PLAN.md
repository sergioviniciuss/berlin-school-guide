---
phase: 15.1-five-lichtenberg-evidence-profiles
plan: 06
type: execute
wave: 6
depends_on:
  - "05"
files_modified:
  - src/content/schools/real/lichtenbergPrimarySchools/index.ts
autonomous: true
requirements:
  - "All five schools appear in the static directory and have /schools/[slug]/ profiles"
  - "Each school has field-level evidence status and Fontes citations for authored factual fields"
  - "Each school has a short Perfil da escola, and tags only where gathered evidence earns them, that passes validate:data / tag evidence rules"
  - "User-facing copy for new qualitative content is Brazilian Portuguese (editorial voice)"
must_haves:
  truths:
    - "Evangelische Schule Lichtenberg is a profile_ready detailed record at slug evangelische-schule-lichtenberg with classification private"
    - "Grades come from the portrait, and a field that does not apply is not_applicable while a field that was not found is missing"
    - "The record has a plain Brazilian Portuguese perfilDaEscola that repeats an evangelical or church-run fact when the portrait or website states it"
    - "afterSchoolCare, schoolProfile, and pedagogyFocus on this record are not status not_confirmed"
  artifacts:
    - path: "src/content/schools/real/lichtenbergPrimarySchools/index.ts"
      provides: "Appended Evangelische Schule Lichtenberg primarySchool record"
      contains: "evangelische-schule-lichtenberg"
  key_links:
    - from: "evangelische-schule-lichtenberg"
      to: "perfilDaEscola"
      via: "primarySchool input"
      pattern: "slug: \"evangelische-schule-lichtenberg\""
    - from: "school website source id"
      to: "field evidence citations"
      via: "sourceId on a field or tag"
      pattern: "evangelische-schule-lichtenberg"
---

<objective>
Append Evangelische Schule Lichtenberg as a private portrait-plus-website evidence record with a short Brazilian Portuguese Perfil da escola (D-01 through D-17).

Purpose: This is the fifth of the five locked schools, and the first real private profile. Append it after `schlaufuchs-schule`. The directory publishes it because `generateStaticParams` already maps the school array. This plan does not add a route or a profile component.
Output: One appended `primarySchool()` record. The existing ten call sites stay unchanged.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15.1-five-lichtenberg-evidence-profiles/15.1-05-SUMMARY.md
@src/content/schools/real/lichtenbergPrimarySchools/index.ts
@docs/research/SCHOOL_RESEARCH_WORKFLOW.md
@docs/EDITORIAL_GUIDE.md

<interfaces>
Locked identity for this plan:

```typescript
slug: "evangelische-schule-lichtenberg"
name: "Evangelische Schule Lichtenberg" // replace only if the Schulportrait heading is a different official name
```

`id` is `schoolNumber.toLowerCase()`. Forbidden ids already in the file: `11g06`, `11g11`, `11g09`, `11g23`, `11g35`, `11g31`, `11g13`, `11g12`, `11g14`, `11g37`.

Portrait URL shape: `https://www.bildung.berlin.de/Schulverzeichnis/Schulportrait.aspx?IDSchulzweig=${portraitId}`.

`citedMissing(sourceIds, note, lastChecked)` was added in plan 01. `missing()` still stores `citations: []` — do not use it for a page that was read and was silent.

Closed tag ids, only if evidence earns one: `stem-focus`, `languages-focus`, `arts-music-focus`, `bilingual-program`, `special-pedagogical-model`, `all-day-model`, `inclusion-support`, `transition-support`, `structured-learning-environment`, `active-school-community`.

Jest files still expect 10 schools until plan 07. Do not edit them here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Research the portrait and website and append the factual record</name>
  <files>src/content/schools/real/lichtenbergPrimarySchools/index.ts</files>
  <read_first>
    - src/content/schools/real/lichtenbergPrimarySchools/index.ts (factory, Richard-Wagner record, and the ten call sites so they are not edited)
    - docs/research/SCHOOL_RESEARCH_WORKFLOW.md (missing versus not_applicable, per-field trees)
    - .planning/phases/15.1-five-lichtenberg-evidence-profiles/15.1-PATTERNS.md (Pattern 2 and Pattern 4)
  </read_first>
  <action>
    Fetch the official portrait before writing any fact. Search `https://www.bildung.berlin.de/Schulverzeichnis/` for `Evangelische Schule Lichtenberg`. Open the Schulportrait. Copy `IDSchulzweig` into `portraitId` and the Schulnummer into `schoolNumber`. Do not invent either value. If the search does not return this school, stop and report that blocker. Do not create a stub record.

    Append one `primarySchool({...})` immediately before `] satisfies School[];`. Do not reorder existing records. Do not edit the ten call sites.

    Use the research-session calendar date as `YYYY-MM-DD` for `portraitDateAccessed`, every website source `dateAccessed`, `lastResearched`, and `lastSourceChecked`. Do not use `2026-07-10` or `2026-07-29`. Set `lastSourceChecked` to that same date, which must be greater than or equal to every source `dateAccessed` on the record.

    Set `researchStatus: "profile_ready"` and `coverageLevel: "detailed"`.

    Copy these from the portrait, using the printed values:
    - `name` (locked default `Evangelische Schule Lichtenberg`)
    - `address` (street only) and `postCodeAndCity` (the portrait PLZ, a space, then `Berlin`)
    - `neighbourhood` as the portrait Ortsteil string, untranslated
    - `website` as the portrait URL, or `null` if the portrait has no website
    - `languages` as the printed names, or `[]` if none are listed
    - `ganztag` as the printed model string, or omit the key if the portrait has no Ganztag model
    - `gradesServed` as the printed grades. Pass the key even when the portrait says 1–6. Do not omit it and do not copy the public-school default when the portrait lists different grades (D-16).
    - `classification: "private"`. D-13 locks this school as private from the official portrait. If the portrait you opened is not a private or Ersatzschule record for Evangelische Schule Lichtenberg, stop and report that blocker instead of writing `public`.
    - `level: "primary"` unless the portrait also serves secondary grades; then `level: "mixed_with_primary"` and `primarySectionDescription` set to a short factual sentence citing the portrait

    Private-school evidence (D-13, D-14). Use `notApplicable(note, lastChecked)` only when `docs/research/SCHOOL_RESEARCH_WORKFLOW.md` says the field does not apply to this private school. A field that applies and was not found stays `citedMissing`, not `not_applicable`. Do not mark a missing inspection `not_applicable` to raise coverage. When the portrait or website states that the school is evangelical or church-run, set `schoolProfile` to a verified factual sentence that states that, citing that source. Do not add a religious tag and do not add fee or admissions fields.

    Then open the website from the portrait. Add one `schoolWebsiteSource` per page that exists and that you actually read, with ids `{slug}-website`, and extra ids `{slug}-ganztag`, `{slug}-welcome-classes`, `{slug}-inspection`, or `{slug}-profile` only for those pages. Pass the real access date as the fourth argument. Homepage plus the page for welcome classes, Ganztag or Hort, language program, or inspection, when that page exists (D-02).

    If an important field is still empty after the portrait and the website, search the open web for the school name plus that field. Store a hit only when it is a journalism article with a publisher and a `z.string().url()` URL that states the field. Shape:

    ```typescript
    source({
      id: "evangelische-schule-lichtenberg-journalism-<topic>",
      title: "<article title>",
      url: "<article url>",
      type: "journalism",
      reliability: "secondary",
      publisher: "<publisher>",
      dateAccessed: "<YYYY-MM-DD>",
    })
    ```

    Do not search journalism for a field the portrait or website already states (D-03). Do not search Reddit, Facebook, or parent forums (D-10).

    Always pass these four fields. Do not pass `offers`. Do not call `inferredFrom`.
    - `afterSchoolCare`: verified only when the portrait or website directly describes Hort, eFöB, or after-school care. A Ganztag string is not after-school care (D-04). Otherwise `field(null, citedMissing([...checked source ids], "<Portuguese note>", "<date>"))`.
    - `schoolProfile`: verified only from a direct profile sentence. Do not join an offers list.
    - `pedagogyFocus`: verified only from a direct focus statement. Otherwise cited missing.
    - `inspectionAvailability`: if an inspection page states availability, `field("available"` or `"unavailable"`, `evidence(...))`. If you checked and found no report, cited missing with value `null`. Do not omit this key. Omitting it makes the factory store `not_confirmed` because this record is `profile_ready`.

    Also pass `welcomeClasses`, `bilingualPrograms`, `internationalPrograms`, `inclusionSupport`, `familyCommunication`, `inspectionData`, and `facilities` when a source states them. When the Perfil will name one of those as unconfirmed, pass `citedMissing` with the portrait id `` `${portraitId}-official-portrait` `` and each website source id you read. Every `school_website`, `journalism`, and `official_inspection` source id on this record must appear in at least one `sourceId` on a field or, in Task 2, on a tag. The portrait id is already cited by the factory on name and address.

    Do not set `perfilDaEscola`, `tags`, or `qualitativeLastReviewed` in this task. Do not edit Jest, Playwright, schema, or profile UI. `corepack pnpm exec jest` length tests that expect 10 schools will fail until plan 07. Leave those files alone.
  </action>
  <verify>
    <automated>corepack pnpm validate:data</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'slug: "evangelische-schule-lichtenberg"' src/content/schools/real/lichtenbergPrimarySchools/index.ts` matches
    - `rg -n 'researchStatus: "profile_ready"' src/content/schools/real/lichtenbergPrimarySchools/index.ts` matches the new record as well as the three existing detailed schools
    - The new call includes `classification: "private"`, `afterSchoolCare:`, `schoolProfile:`, `pedagogyFocus:`, `inspectionAvailability:`, `gradesServed:`, and `portraitDateAccessed:`
    - The new call does not contain `inferredFrom` or `not_confirmed`
    - `rg -c 'primarySchool\(\{' src/content/schools/real/lichtenbergPrimarySchools/index.ts` prints 15
    - `corepack pnpm validate:data` exits 0
    - `git diff -U0 -- src/content/schools/real/lichtenbergPrimarySchools/index.ts | rg 'slug: "(adam-ries-schule|seepark-grundschule|lew-tolstoi-schule|richard-wagner-schule)"'` prints nothing
  </acceptance_criteria>
  <done>Evangelische Schule Lichtenberg parses as a detailed factual record with direct or cited-missing profile fields.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Perfil and any tag the sources already earn</name>
  <files>src/content/schools/real/lichtenbergPrimarySchools/index.ts, docs/research/PHASE15_1_COMMUNITY_WITHHOLD.md</files>
  <read_first>
    - src/content/schools/real/lichtenbergPrimarySchools/index.ts (the new record and the Seepark perfilDaEscola string)
    - docs/EDITORIAL_GUIDE.md (Perfil da escola voice)
    - docs/DATA_MODEL.md (tag taxonomy v1, facts versus tags)
    - src/features/evidence/validateTagEvidence/index.ts (derived confidence and active-school-community rules)
  </read_first>
  <action>
    On the `evangelische-schule-lichtenberg` record only, add a plain-text `perfilDaEscola` and `qualitativeLastReviewed`.

    `qualitativeLastReviewed` is the authoring day `YYYY-MM-DD`, the same day as `lastResearched` unless the prose was written on a later day. It must not be `2026-07-29`. The schema is `z.string().date()`. Do not add a time.

    The Perfil is one short Brazilian Portuguese paragraph (D-05, D-06):
    - Name what the portrait and website confirm, including neighbourhood, languages, and Ganztag when those fields are verified.
    - When a decision-relevant point was checked and not stated (after-school care, language support, inclusion, or inspection), include the sentence fragment `Até o momento, não houve confirmação independente`. That means the sources checked do not say so. It does not say the school lacks the thing.
    - No HTML, Markdown, MDX, ranking, or superlatives. `EditorialNarrative` renders the string as a React text child.
    - The official German name stays German inside the sentence.
    - State that the school is private. When `schoolProfile` says the school is evangelical or church-run, repeat that fact in Brazilian Portuguese in this paragraph and cite the same source on `schoolProfile` (D-14). Do not add a religious tag.
    - When the portrait or website states fees or the admission path, add at most one sentence about that fact (D-15). Do not add fee or admissions fields. If neither source states it, omit it.
    - Each unconfirmed point named in the Perfil must already cite, on that field's `evidence.citations`, the sources that were checked (D-08). If Task 1 left a named gap on `missing()` with `citations: []`, replace that evidence with `citedMissing` before finishing.

    Tags (D-07). Apply a closed id only when the sources already gathered state that characteristic. Zero tags is valid: omit the `tags` key. Do not add an eleventh id.
    - A verified Ganztag string is not `all-day-model`.
    - A `languages` list is not `languages-focus` or `bilingual-program`.
    - An offers line alone is not `stem-focus` or `arts-music-focus`.
    - Confidence is derived, not chosen: two or more distinct qualifying source ids → `confirmed_multi_source`; otherwise if any citation type is `official_government`, `official_inspection`, or `school_website` → `confirmed_official`; otherwise `partial`.
    - Any tag other than `active-school-community` is rejected if it cites `triangulated_community` or `anecdotal_reserved`.
    - `active-school-community` only if an incidental venue turned up while reading the school website or a journalism article already used, and the tag has at least two `triangulated_community` citations with distinct `independenceLog.venue` values plus at least one non-community allowlisted citation. `independenceLog` is `{ venue, identifier, dateAccessed, independenceRationale, echoCheckNote }` on the tag citation. If that bar fails, omit the tag. Write `qualitativeResearchNotes` and create `docs/research/PHASE15_1_COMMUNITY_WITHHOLD.md` only in that withheld case (D-11, D-12). Copy the section order from `docs/research/PHASE14_COMMUNITY_TRIANGULATION.md`: research date, school identity, tag, decision withhold, venues table, threshold check, school-record outcome. Failed venues stay in that note and that file. Do not put them in `sources[]`, Fontes, or the Perfil. If no venue turned up, write neither the note nor the file.

    Do not edit the original ten records or `grundschule-am-neuen-tor`, `robinson-schule`, `schule-auf-dem-lichtenberg`, or `schlaufuchs-schule`. Do not edit tests.
  </action>
  <verify>
    <automated>corepack pnpm validate:data</automated>
  </verify>
  <acceptance_criteria>
    - The `evangelische-schule-lichtenberg` call contains `perfilDaEscola:` and `qualitativeLastReviewed:` with a `YYYY-MM-DD` value other than `2026-07-29`
    - `rg -c 'qualitativeLastReviewed: "2026-07-29"' src/content/schools/real/lichtenbergPrimarySchools/index.ts` prints 5
    - The Perfil string contains no `<` character
    - If the Perfil contains `Até o momento, não houve confirmação independente`, the record also contains `citedMissing` or a `status: "missing"` evidence object whose `citations` array is non-empty
    - `corepack pnpm validate:data` exits 0
    - `rg -n 'best-school|confessional|religious' src/content/schools/real/lichtenbergPrimarySchools/index.ts` prints nothing
  </acceptance_criteria>
  <done>Evangelische Schule Lichtenberg has a Brazilian Portuguese Perfil, and any tag on it passes validateTagEvidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live portrait and school website → committed TypeScript | Facts copied into the static array become public profile text |
| `perfilDaEscola` string → `EditorialNarrative` | The string is rendered as a React text child |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-16 | Tampering | `perfilDaEscola` | mitigate | Keep the string plain. Do not edit `EditorialNarrative` and do not introduce `dangerouslySetInnerHTML` or MDX. `schoolSchema` trims the string. `pnpm validate:data` parses the record. |
| T-15.1-17 | Repudiation | Field and tag citations | mitigate | Verified fields go through `validateFieldCitations` inside `schoolSchema`. Tags go through `validateTagEvidence`, which rejects a confidence value other than the derived one. Website, journalism, and inspection source ids must be cited on a field or tag so `collectCitedSources` lists them in Fontes. |
| T-15.1-18 | Repudiation | `active-school-community` | mitigate | Publish the tag only with two independence logs and one non-community citation. Otherwise omit it. A withhold note is not added to `sources[]`, so Fontes cannot show the failed venues. |
| T-15.1-19 | Tampering | `classification` and `not_applicable` | mitigate | The record passes `classification: "private"` and portrait `gradesServed`. `notApplicable()` is only for a field the workflow says does not apply. A missing fact uses `citedMissing`, so coverage cannot be raised by relabeling a gap. No religious tag id and no fee field are added. |
</threat_model>

<verification>
`corepack pnpm validate:data` exits 0. Directory Jest length tests stay red on purpose until plan 07.
</verification>

<success_criteria>
- Slug `evangelische-schule-lichtenberg` is appended after `schlaufuchs-schule`
- Factual fields are verified or cited-missing, never inferred
- A Brazilian Portuguese Perfil and `qualitativeLastReviewed` are present
- The ten call sites are absent from the diff
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-five-lichtenberg-evidence-profiles/15.1-06-SUMMARY.md`
</output>
