---
phase: 15-profile-ui-tags-narrative
plan: 03
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/features/schools/EditorialNarrative/index.tsx
  - src/features/schools/EditorialNarrative/index.test.tsx
  - src/features/schools/TagsSection/index.tsx
  - src/features/schools/TagsSection/index.test.tsx
  - src/features/schools/TagsSection/utils.ts
autonomous: true
requirements:
  - NARR-01
  - NARR-03
  - NARR-04
must_haves:
  truths:
    - "EditorialNarrative renders Perfil da escola H2 + body and returns null when text absent"
    - "TagsSection groups tags by category H3s, omits empty categories and omits section when tags empty"
    - "Collapsed tag shows label + confidence only; expand shows formatSourceType chips + #source-{id} links"
    - "No collapsed Comunidade badge; community distinction only via formatSourceType label when expanded"
  artifacts:
    - path: "src/features/schools/EditorialNarrative/index.tsx"
      provides: "Server editorial narrative block"
      contains: "Perfil da escola"
    - path: "src/features/schools/TagsSection/index.tsx"
      provides: "Client category-grouped tags with per-tag evidence toggle"
      contains: "Ver evidência"
    - path: "src/features/schools/TagsSection/utils.ts"
      provides: "groupTagsByCategory using TAG_CATEGORY_ORDER"
      contains: "groupTagsByCategory"
  key_links:
    - from: "TagsSection toggle"
      to: "expanded evidence region"
      via: "aria-expanded + aria-controls + useId"
      pattern: "aria-expanded|aria-controls"
    - from: "expanded citation"
      to: "#source-{id}"
      via: "anchor href"
      pattern: "#source-"
---

<objective>
Implement EditorialNarrative and client TagsSection with category grouping and ComparisonEvidenceNote-style expand (D-01–D-15, NARR-01/03/04).

Purpose: Deliver the scannable qualitative UI blocks SchoolProfile will compose.
Output: Green component suites; no SchoolProfile wiring yet (Plan 05).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-profile-ui-tags-narrative/15-CONTEXT.md
@.planning/phases/15-profile-ui-tags-narrative/15-UI-SPEC.md
@.planning/phases/15-profile-ui-tags-narrative/15-PATTERNS.md
@.planning/phases/15-profile-ui-tags-narrative/15-01-SUMMARY.md
@src/features/schools/ComparisonEvidenceNote/index.tsx
@src/features/schools/ProfileFieldRow/index.tsx
@src/features/schools/SourcesSection/index.tsx
@src/features/schools/tagTaxonomy/index.ts
@src/features/evidence/formatTagConfidence/index.ts
@src/features/evidence/formatSourceType/index.ts

<interfaces>
```typescript
// EditorialNarrative — server component (no "use client")
type EditorialNarrativeProps = { text: string };
export function EditorialNarrative({ text }: EditorialNarrativeProps): JSX.Element;

// TagsSection — client
type TagsSectionProps = {
  tags: SchoolTag[];
  sources: Source[];
};
export function TagsSection({ tags, sources }: TagsSectionProps): JSX.Element | null;

// utils
export function groupTagsByCategory(tags: SchoolTag[]): Array<{
  categoryId: TagCategoryId;
  label: string;
  tags: SchoolTag[];
}>; // only non-empty categories, order = TAG_CATEGORY_ORDER
```

Locked copy (UI-SPEC):
- H2 narrative: `Perfil da escola`
- H2 tags: `Características da escola`
- Toggle: `Ver evidência` / `Ocultar evidência`
- Confidence chip classes: `bg-neutral-100 text-neutral-800 rounded-full px-2 py-1 text-sm font-semibold`
- Source-type chip classes: `inline-flex rounded-full bg-blue-50 px-2 py-1 text-sm font-semibold text-blue-800`
- Toggle button: include `min-h-11` and `text-blue-700`
- Category H3: `text-sm font-semibold text-neutral-700`
- Section H2: `text-xl font-semibold text-neutral-950`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement EditorialNarrative</name>
  <files>src/features/schools/EditorialNarrative/index.tsx</files>
  <read_first>
    - src/features/schools/EditorialNarrative/index.tsx
    - src/features/schools/EditorialNarrative/index.test.tsx
    - src/features/schools/ProfileSection/index.tsx
    - .planning/phases/15-profile-ui-tags-narrative/15-UI-SPEC.md
  </read_first>
  <behavior>
    - Renders section with H2 "Perfil da escola" and paragraph with provided text
    - No official/community chips (D-15)
    - No card chrome wrapper
  </behavior>
  <action>
    Replace the null stub with a server component:
    ```tsx
    export function EditorialNarrative({ text }: { text: string }) {
      return (
        <section>
          <h2 className="text-xl font-semibold text-neutral-950">Perfil da escola</h2>
          <p className="mt-4 text-base text-neutral-900">{text}</p>
        </section>
      );
    }
    ```
    Parent gates absence (Plan 05). If tests require null when `!text`, return null for falsy text. Never render `qualitativeResearchNotes`. Do not put tags inside the narrative (D-11).
  </action>
  <verify>
    <automated>pnpm test -- EditorialNarrative</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Perfil da escola' src/features/schools/EditorialNarrative/index.tsx` matches
    - `rg -n 'text-base text-neutral-900' src/features/schools/EditorialNarrative/index.tsx` matches
    - `rg -n "use client" src/features/schools/EditorialNarrative/index.tsx` returns no matches
    - `pnpm test -- EditorialNarrative` exits 0
  </acceptance_criteria>
  <done>EditorialNarrative greens Wave 0 narrative tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TagsSection with category groups and evidence expand</name>
  <files>src/features/schools/TagsSection/utils.ts, src/features/schools/TagsSection/index.tsx, src/features/schools/TagsSection/index.test.tsx</files>
  <read_first>
    - src/features/schools/TagsSection/index.tsx
    - src/features/schools/TagsSection/index.test.tsx
    - src/features/schools/ComparisonEvidenceNote/index.tsx
    - src/features/schools/ProfileFieldRow/index.tsx
    - src/features/schools/tagTaxonomy/index.ts
    - src/features/evidence/formatTagConfidence/index.ts
    - src/features/evidence/formatSourceType/index.ts
    - .planning/phases/15-profile-ui-tags-narrative/15-UI-SPEC.md
  </read_first>
  <behavior>
    - Empty tags → null (D-08)
    - Non-empty → H2 Características + H3 per non-empty category in TAG_CATEGORY_ORDER
    - Collapsed: formatTagId + formatTagConfidence chip only; no formatSourceType / Comunidade
    - Expand: Ver evidência → Ocultar evidência; aria-expanded true; show formatSourceType chip(s) + link href `#source-{sourceId}` (+ optional Abrir original external URL like ProfileFieldRow)
    - Independent per-tag useState (NARR-04 / D-10)
  </behavior>
  <action>
    1. Create `TagsSection/utils.ts` with `groupTagsByCategory(tags)` iterating `TAG_CATEGORY_ORDER`, collecting tags where `getTagCategory(tag.id) === categoryId`, skipping empty groups, labeling via `formatTagCategory`.

    2. Replace stub `TagsSection/index.tsx` with `"use client"` component:
       - Early `if (!tags.length) return null;`
       - Build `sourceById = new Map(sources.map(...))`
       - Render section H2 `Características da escola`
       - For each group: H3 + `space-y-6` stack of tag rows
       - Each tag row: extract a small inner `TagRow` (same file OK — avoid new TagEvidence directory unless file exceeds readability) with `useState(false)`, `useId()`, button `type="button"` `aria-expanded` `aria-controls` `className` including `min-h-11 text-sm text-blue-700 hover:underline`, labels `Ver evidência` / `Ocultar evidência`
       - Confidence: `<span className="bg-neutral-100 text-neutral-800 rounded-full px-2 py-1 text-sm font-semibold">{formatTagConfidence(tag.confidence)}</span>`
       - Expanded panel: for each citation resolve source; skip missing; render `formatSourceType(source.type)` in blue chip (skip if null); `#source-${id}` link text `Ver fonte`; optional external `Abrir original` + ExternalLink
       - Do NOT show community stigma colors or collapsed Comunidade badge (D-12–D-14)

    3. Ensure Wave 0 tests pass; add coverage if missing for multi-category grouping (two tags in different categories → two H3s) and empty omit.
  </action>
  <verify>
    <automated>pnpm test -- TagsSection</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'Características da escola|Ver evidência|Ocultar evidência|min-h-11' src/features/schools/TagsSection/index.tsx` matches all
    - `rg -n "formatSourceType|formatTagConfidence|formatTagId" src/features/schools/TagsSection/index.tsx` matches
    - `rg -n "groupTagsByCategory|TAG_CATEGORY_ORDER" src/features/schools/TagsSection/utils.ts` matches
    - `rg -n "Comunidade" src/features/schools/TagsSection/index.tsx` returns no matches in collapsed chrome (string must not appear as a static badge label in the file)
    - `pnpm test -- TagsSection` exits 0
  </acceptance_criteria>
  <done>TagsSection greens NARR-01/03/04 component tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static school tags/sources → client DOM | Build-time content; expand toggles local UI state only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | XSS | EditorialNarrative / TagsSection | mitigate | Render `text` and source titles via React text; no dangerouslySetInnerHTML |
| T-15-03-02 | T | citation sourceId | mitigate | Resolve via sourceById Map; skip missing ids (ProfileFieldRow pattern) |
| T-15-03-03 | I | External source URLs | mitigate | `rel="noopener noreferrer"` + `target="_blank"` on external links |
</threat_model>

<verification>
`pnpm test -- EditorialNarrative TagsSection` green.
</verification>

<success_criteria>
- Category-grouped tags with confidence + expandable evidence
- Official vs community distinction only via formatSourceType when expanded
- Empty tags / missing narrative do not render chrome
</success_criteria>

<output>
After completion, create `.planning/phases/15-profile-ui-tags-narrative/15-03-SUMMARY.md`
</output>
