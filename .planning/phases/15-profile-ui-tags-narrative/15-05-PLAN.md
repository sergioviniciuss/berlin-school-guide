---
phase: 15-profile-ui-tags-narrative
plan: 05
type: execute
wave: 3
depends_on:
  - "02"
  - "03"
  - "04"
files_modified:
  - src/features/schools/SchoolProfile/constants.ts
  - src/features/schools/SchoolProfile/index.tsx
  - src/features/schools/SchoolProfile/index.test.tsx
  - .planning/phases/15-profile-ui-tags-narrative/15-VALIDATION.md
autonomous: true
requirements:
  - NARR-01
  - NARR-03
  - NARR-04
  - NARR-05
  - NARR-06
must_haves:
  truths:
    - "Pilot profiles show editorial zone after header then factual sections then Fontes then correction link"
    - "Factual schoolProfile label is Perfil oficial; editorial H2 remains Perfil da escola"
    - "Non-pilot profiles omit empty qualitative chrome but still show correction link"
    - "qualitativeResearchNotes never renders"
    - "Fontes on tagged schools includes tag citations via collectCitedSources"
  artifacts:
    - path: "src/features/schools/SchoolProfile/index.tsx"
      provides: "Composition of EditorialNarrative, TagsSection, SourcesSection, correction Link"
      contains: "EditorialNarrative"
    - path: "src/features/schools/SchoolProfile/constants.ts"
      provides: "schoolProfile label Perfil oficial"
      contains: "Perfil oficial"
  key_links:
    - from: "SchoolProfile header"
      to: "EditorialNarrative / TagsSection"
      via: "conditional render immediately after </header>"
      pattern: "perfilDaEscola|TagsSection"
    - from: "SchoolProfile"
      to: "/report-correction/?school="
      via: "Link after SourcesSection"
      pattern: "report-correction/\\?school="
    - from: "SchoolProfile"
      to: "collectCitedSources(school)"
      via: "existing call now returning tag-merged Fontes"
      pattern: "collectCitedSources\\(school\\)"
---

<objective>
Wire qualitative UI into SchoolProfile: rename factual label, insert editorial zone after header, keep Fontes merge, add correction CTA on every profile (D-01–D-06, D-04, D-18, all NARR-*).

Purpose: Make pilot success criteria observable on real profile pages without altering non-pilot structure beyond additive optional rendering.
Output: Green SchoolProfile suite; VALIDATION sampling map updated; phase UI complete.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-profile-ui-tags-narrative/15-CONTEXT.md
@.planning/phases/15-profile-ui-tags-narrative/15-UI-SPEC.md
@.planning/phases/15-profile-ui-tags-narrative/15-02-SUMMARY.md
@.planning/phases/15-profile-ui-tags-narrative/15-03-SUMMARY.md
@.planning/phases/15-profile-ui-tags-narrative/15-04-SUMMARY.md
@src/features/schools/SchoolProfile/index.tsx
@src/features/schools/SchoolProfile/constants.ts
@src/features/schools/EditorialNarrative/index.tsx
@src/features/schools/TagsSection/index.tsx

<interfaces>
Insert after `</header>`, before `PROFILE_SECTIONS.map` (D-01/D-02):
```tsx
{school.perfilDaEscola ? (
  <EditorialNarrative text={school.perfilDaEscola} />
) : null}
{school.tags && school.tags.length > 0 ? (
  <TagsSection tags={school.tags} sources={school.sources} />
) : null}
```

After `<SourcesSection ... />`:
```tsx
<Link
  href={`/report-correction/?school=${school.slug}`}
  className="inline-flex min-h-11 items-center text-sm font-medium text-blue-700 underline"
>
  Sugerir correção ou atualização
</Link>
```

constants.ts change (D-04):
```ts
schoolProfile: "Perfil oficial",
```
(was `"Perfil da escola"`)

Keep SchoolProfile as Server Component (no `"use client"`).
Omit `qualitativeLastReviewed` UI (discretion / RESEARCH A4).
Never render `school.qualitativeResearchNotes`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename Perfil oficial and compose editorial zone + correction link</name>
  <files>src/features/schools/SchoolProfile/constants.ts, src/features/schools/SchoolProfile/index.tsx</files>
  <read_first>
    - src/features/schools/SchoolProfile/index.tsx
    - src/features/schools/SchoolProfile/constants.ts
    - src/features/schools/SchoolProfile/index.test.tsx
    - src/features/schools/EditorialNarrative/index.tsx
    - src/features/schools/TagsSection/index.tsx
    - .planning/phases/15-profile-ui-tags-narrative/15-UI-SPEC.md
  </read_first>
  <behavior>
    - validTaggedSchool: Perfil da escola H2 before Identificação; Características present; Perfil oficial field label present; correction link href includes slug
    - validPerfilOnlySchool: narrative present; no Características
    - validDetailedPublicSchool: no editorial H2 / no Características; correction link still present
    - school with qualitativeResearchNotes: notes string not in document
  </behavior>
  <action>
    1. In `constants.ts`, set `profileFieldLabels.schoolProfile` to exact string `Perfil oficial`.

    2. In `index.tsx`:
       - Import `EditorialNarrative` from `@/features/schools/EditorialNarrative` and `TagsSection` from `@/features/schools/TagsSection`.
       - After `</header>`, insert the conditional editorial zone from interfaces (perfil then tags).
       - Keep `collectCitedSources(school)` unchanged at call site (Plan 02 already merged tags).
       - After `<SourcesSection groupedSources={groupedSources} />`, add the correction `Link` with exact CTA copy `Sugerir correção ou atualização` and href `/report-correction/?school=${school.slug}`.
       - Do not render `qualitativeResearchNotes` or `qualitativeLastReviewed`.
       - Do not add `"use client"`.
       - Do not change directory/compare surfaces.
  </action>
  <verify>
    <automated>pnpm test -- SchoolProfile</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'schoolProfile: "Perfil oficial"' src/features/schools/SchoolProfile/constants.ts` matches
    - `rg -n "EditorialNarrative|TagsSection|Sugerir correção ou atualização|report-correction/\\?school=" src/features/schools/SchoolProfile/index.tsx` matches
    - `rg -n "qualitativeResearchNotes" src/features/schools/SchoolProfile/index.tsx` returns no matches
    - `rg -n '"use client"' src/features/schools/SchoolProfile/index.tsx` returns no matches
    - `pnpm test -- SchoolProfile` exits 0
  </acceptance_criteria>
  <done>SchoolProfile composes qualitative UI + correction CTA; Wave 0 profile tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gate — full unit suite + VALIDATION update</name>
  <files>.planning/phases/15-profile-ui-tags-narrative/15-VALIDATION.md</files>
  <read_first>
    - .planning/phases/15-profile-ui-tags-narrative/15-VALIDATION.md
    - src/features/schools/SchoolProfile/index.test.tsx
  </read_first>
  <action>
    1. Run full `pnpm test` and `pnpm typecheck`. Fix any regressions caused by `citedBy` required field breaking other callers (grep `CitedSourceEntry` / hand-built groupedSources). If `pnpm build` is feasible in time budget, run it to catch Suspense/static-export issues on `/report-correction`.

    2. Update `15-VALIDATION.md`:
       - Mark Wave 0 requirement checkboxes complete
       - Set task Status column to ✅ for greened rows
       - Set `wave_0_complete: true`
       - Set `nyquist_compliant: true` only if all automated rows are green and sampling continuity holds; otherwise leave false and note remaining gaps
       - Map concrete Task IDs from plans 00–05 in the Per-Task Verification Map
  </action>
  <verify>
    <automated>pnpm test && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0
    - `pnpm typecheck` exits 0
    - `rg -n "wave_0_complete: true" .planning/phases/15-profile-ui-tags-narrative/15-VALIDATION.md` matches
    - `rg -n "Perfil oficial" src/features/schools/SchoolProfile/constants.ts` matches
  </acceptance_criteria>
  <done>Full unit/typecheck green; VALIDATION reflects Wave 0 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static school record → SchoolProfile HTML | Trusted SSG content |
| Profile → report-correction query | Public slug only in URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-01 | XSS | perfilDaEscola prose | mitigate | Pass string into EditorialNarrative React text node only |
| T-15-05-02 | I | correction Link href | mitigate | Use `school.slug` from typed School record (not request input) when building query |
| T-15-05-03 | E | non-pilot chrome | mitigate | Conditional render only — no empty shells; no coverage calculator changes |
</threat_model>

<verification>
`pnpm test` + `pnpm typecheck` green; SchoolProfile shows qualitative blocks on pilots only.
</verification>

<success_criteria>
- All five ROADMAP success criteria observable via unit tests + composition
- Non-pilots unchanged aside from additive correction link
- NARR-01, NARR-03, NARR-04, NARR-05, NARR-06 satisfied in wired profile
</success_criteria>

<output>
After completion, create `.planning/phases/15-profile-ui-tags-narrative/15-05-SUMMARY.md`
</output>
