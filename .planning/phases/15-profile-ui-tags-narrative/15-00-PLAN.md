---
phase: 15-profile-ui-tags-narrative
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/features/schools/tagTaxonomy/index.test.ts
  - src/features/schools/TagsSection/index.test.tsx
  - src/features/schools/TagsSection/index.tsx
  - src/features/schools/EditorialNarrative/index.test.tsx
  - src/features/schools/EditorialNarrative/index.tsx
  - src/features/schools/collectCitedSources/index.test.ts
  - src/features/schools/SourcesSection/index.test.tsx
  - src/features/schools/SchoolProfile/index.test.tsx
  - src/features/schools/ReportCorrection/index.test.tsx
  - src/features/schools/ReportCorrection/index.tsx
  - .planning/phases/15-profile-ui-tags-narrative/15-VALIDATION.md
autonomous: true
requirements:
  - NARR-01
  - NARR-03
  - NARR-04
  - NARR-05
  - NARR-06
must_haves:
  truths:
    - "Failing Jest stubs exist for TagsSection, EditorialNarrative, tag category helpers, Fontes tag merge, SchoolProfile qualitative chrome, and ReportCorrection"
    - "New-component stubs export null-returning components so imports resolve and assertions stay RED"
    - "15-VALIDATION.md Wave 0 checklist items are marked in progress / mapped to plan 00"
  artifacts:
    - path: "src/features/schools/TagsSection/index.test.tsx"
      provides: "NARR-01/03/04 RED coverage for category groups, confidence, expand, empty omit"
      contains: "Características da escola"
    - path: "src/features/schools/EditorialNarrative/index.test.tsx"
      provides: "NARR-04 RED coverage for Perfil da escola heading/body"
      contains: "Perfil da escola"
    - path: "src/features/schools/ReportCorrection/index.test.tsx"
      provides: "NARR-06 RED coverage for prefill + scope copy"
      contains: "Sugerir correção"
  key_links:
    - from: "15-VALIDATION.md Wave 0"
      to: "plan 00 test files"
      via: "Wave 0 Requirements checklist"
      pattern: "TagsSection/index.test.tsx"
---

<objective>
Create Wave 0 Nyquist RED test scaffolds (and minimal null stubs for brand-new modules) covering NARR-01/03–06 behaviors before any production UI lands.

Purpose: Guarantee every later plan has an automated verify target and sampling continuity per 15-VALIDATION.md.
Output: Failing targeted Jest suites + updated VALIDATION wave-0 tracking; zero real UI behavior.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-profile-ui-tags-narrative/15-CONTEXT.md
@.planning/phases/15-profile-ui-tags-narrative/15-UI-SPEC.md
@.planning/phases/15-profile-ui-tags-narrative/15-VALIDATION.md
@.planning/phases/15-profile-ui-tags-narrative/15-RESEARCH.md
@.planning/phases/15-profile-ui-tags-narrative/15-PATTERNS.md
@src/features/schools/school/fixtures.ts
@src/features/schools/SchoolComparison/index.test.tsx

<interfaces>
From src/features/schools/school/fixtures.ts (use in stubs — do not invent new fixtures unless needed):
```typescript
export const validTaggedSchool = { /* tags + perfilDaEscola + qualitativeLastReviewed */ };
export const validPerfilOnlySchool = { /* perfil only, no tags */ };
export const validDetailedPublicSchool = { /* non-pilot factual fixture */ };
export const validDirectoryOnlySchool = { /* directory non-pilot */ };
```

Locked UI-SPEC copy strings for assertions:
- "Perfil da escola", "Características da escola", "Perfil oficial"
- "Ver evidência", "Ocultar evidência", "Citado em:"
- "Sugerir correção ou atualização", "Sugerir correção"
- "Inclua problemas em dados factuais, características (tags) e no texto de Perfil da escola."
- "Não encontramos essa escola no link. Descreva o nome da escola no e-mail."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED stubs for tag taxonomy categories, TagsSection, EditorialNarrative</name>
  <files>src/features/schools/tagTaxonomy/index.test.ts, src/features/schools/TagsSection/index.tsx, src/features/schools/TagsSection/index.test.tsx, src/features/schools/EditorialNarrative/index.tsx, src/features/schools/EditorialNarrative/index.test.tsx</files>
  <read_first>
    - src/features/schools/tagTaxonomy/index.ts
    - src/features/schools/tagTaxonomy/index.test.ts
    - src/features/schools/school/fixtures.ts
    - src/features/schools/ComparisonEvidenceNote/index.tsx
    - .planning/phases/15-profile-ui-tags-narrative/15-UI-SPEC.md
    - .planning/phases/15-profile-ui-tags-narrative/15-VALIDATION.md
  </read_first>
  <behavior>
    - getTagCategory("stem-focus") === "academic_focus"; getTagCategory("bilingual-program") === "learning_model"; getTagCategory("inclusion-support") === "student_support"; getTagCategory("active-school-community") === "school_environment"
    - formatTagCategory("academic_focus") === "Foco acadêmico"; formatTagCategory("learning_model") === "Modelo de aprendizagem"; formatTagCategory("student_support") === "Apoio ao aluno"; formatTagCategory("school_environment") === "Ambiente escolar"
    - TAG_CATEGORY_ORDER equals ["academic_focus","learning_model","student_support","school_environment"]
    - TagsSection with validTaggedSchool.tags renders H2 "Características da escola", confidence label from formatTagConfidence, collapsed state has no "Fontes comunitárias trianguladas" / no "Comunidade" badge text
    - Clicking "Ver evidência" sets aria-expanded true and reveals formatSourceType label for the cited source
    - TagsSection with tags=[] returns null / no "Características da escola"
    - EditorialNarrative with text renders H2 "Perfil da escola" + body; without text / empty returns null
  </behavior>
  <action>
    1. Extend `tagTaxonomy/index.test.ts` with a new `describe("tag categories")` that imports `getTagCategory`, `formatTagCategory`, `TAG_CATEGORY_ORDER` from `.` and asserts the exact mappings above for all 10 taxonomy ids mapped to the four categories (use the RESEARCH map: stem/languages/arts-music → academic_focus; bilingual/special-pedagogical/all-day → learning_model; inclusion/transition → student_support; structured-learning/active-school-community → school_environment). Tests MUST fail until Plan 01 exports these symbols.

    2. Create `TagsSection/index.tsx` stub only:
    ```tsx
    "use client";
    export function TagsSection(_props: { tags: unknown; sources: unknown }) {
      return null;
    }
    ```

    3. Create `TagsSection/index.test.tsx` that renders `<TagsSection tags={validTaggedSchool.tags} sources={validTaggedSchool.sources} />` and asserts H2 "Características da escola", presence of `formatTagId("stem-focus")` ("Foco em STEM"), presence of `formatTagConfidence("confirmed_official")` ("Confirmado por fonte oficial"), queryByText("Comunidade") is null when collapsed, and after `userEvent.click` on button named "Ver evidência" the button has `aria-expanded="true"` and expanded region includes a source-type string from `formatSourceType`. Also assert `render(<TagsSection tags={[]} sources={[]} />)` yields `queryByRole("heading", { name: "Características da escola" })` null.

    4. Create `EditorialNarrative/index.tsx` stub: `export function EditorialNarrative(_props: { text?: string }) { return null; }`

    5. Create `EditorialNarrative/index.test.tsx`: with `text="Parágrafo sintético de teste."` expect heading level 2 "Perfil da escola" and the paragraph text; with no props / undefined text expect heading absent.
  </action>
  <verify>
    <automated>pnpm test -- tagTaxonomy TagsSection EditorialNarrative 2>&1 | tee /tmp/15-00-t1.txt; grep -E "FAIL|●|Cannot find|Tests:" /tmp/15-00-t1.txt | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "getTagCategory|formatTagCategory|TAG_CATEGORY_ORDER" src/features/schools/tagTaxonomy/index.test.ts` matches ≥1 each
    - `test -f src/features/schools/TagsSection/index.test.tsx` and file contains exact strings `Características da escola`, `Ver evidência`, `aria-expanded`
    - `test -f src/features/schools/EditorialNarrative/index.test.tsx` and file contains exact string `Perfil da escola`
    - Targeted Jest run exits non-zero (RED) — at least one failing assertion or missing export
  </acceptance_criteria>
  <done>Category + TagsSection + EditorialNarrative RED tests and null stubs exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED extensions for collectCitedSources and SourcesSection cite-back</name>
  <files>src/features/schools/collectCitedSources/index.test.ts, src/features/schools/SourcesSection/index.test.tsx</files>
  <read_first>
    - src/features/schools/collectCitedSources/index.ts
    - src/features/schools/collectCitedSources/types.ts
    - src/features/schools/collectCitedSources/index.test.ts
    - src/features/schools/SourcesSection/index.tsx
    - src/features/schools/SourcesSection/index.test.tsx
    - src/features/schools/school/fixtures.ts
  </read_first>
  <behavior>
    - Tag-only sourceId appears exactly once in collectCitedSources output when no field cites it
    - Same sourceId cited by a field and a tag appears once (dedupe) with citations length ≥1
    - citedBy includes formatTagId label for tag citations and profileFieldLabels label for field citations
    - SourcesSection renders "Citado em:" + joined labels when entry.citedBy is non-empty; omits "Citado em:" when citedBy is [] or missing
  </behavior>
  <action>
    1. Append tests to `collectCitedSources/index.test.ts`:
       - Build a school from `validDetailedPublicSchool` that adds a journalism (or school_website) source id only referenced from `tags: [{ id: "arts-music-focus", confidence: "confirmed_official", citations: [{ sourceId: "tag-only-source" }] }]` and `sources` includes that source — assert grouped entries contain that source id exactly once across all groups.
       - Build a school where one existing field citation sourceId is also cited by a tag — assert that source appears once and `entry.citedBy` includes both a field label (e.g. from `profileFieldLabels`) and `formatTagId(tag.id)`.
       - Assert every returned `CitedSourceEntry` has `citedBy` as an array (may be empty).

    2. Append tests to `SourcesSection/index.test.tsx`:
       - Render with a hand-built `groupedSources` containing one entry with `citedBy: ["Ganztag", "Foco em STEM"]` — expect text matching `/Citado em:/` and both labels.
       - Render with `citedBy: []` — expect `queryByText(/Citado em:/)` null.
  </action>
  <verify>
    <automated>pnpm test -- collectCitedSources SourcesSection 2>&1 | tee /tmp/15-00-t2.txt; grep -E "FAIL|●|Tests:" /tmp/15-00-t2.txt | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "citedBy|tag-only|Citado em" src/features/schools/collectCitedSources/index.test.ts src/features/schools/SourcesSection/index.test.tsx` shows new cases
    - Targeted Jest exits non-zero until Plan 02 greens (missing `citedBy` property or failing expect)
  </acceptance_criteria>
  <done>Fontes merge / cite-back RED tests appended.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED extensions for SchoolProfile qualitative chrome and ReportCorrection</name>
  <files>src/features/schools/SchoolProfile/index.test.tsx, src/features/schools/ReportCorrection/index.tsx, src/features/schools/ReportCorrection/index.test.tsx, .planning/phases/15-profile-ui-tags-narrative/15-VALIDATION.md</files>
  <read_first>
    - src/features/schools/SchoolProfile/index.test.tsx
    - src/features/schools/SchoolProfile/index.tsx
    - src/features/schools/SchoolComparison/index.test.tsx
    - .planning/phases/15-profile-ui-tags-narrative/15-UI-SPEC.md
    - .planning/phases/15-profile-ui-tags-narrative/15-VALIDATION.md
  </read_first>
  <behavior>
    - SchoolProfile with validTaggedSchool shows H2 "Perfil da escola" before "Identificação", shows "Características da escola", shows factual label "Perfil oficial" (not dual "Perfil da escola" for the field), link "Sugerir correção ou atualização" href `/report-correction/?school=${slug}`
    - SchoolProfile with validDetailedPublicSchool (no perfil/tags) has no "Características da escola" and no editorial "Perfil da escola" H2; still has correction link
    - SchoolProfile never renders qualitativeResearchNotes text when present on school
    - ReportCorrection with ?school=valid slug shows H1 "Sugerir correção", "Escola: {name}", scope line exact UI-SPEC string, mailto href containing encodeURIComponent school name
    - ReportCorrection with unknown school shows missing-school copy exact string
  </behavior>
  <action>
    1. Extend `SchoolProfile/index.test.tsx` with cases using `validTaggedSchool`, `validPerfilOnlySchool`, and `validDetailedPublicSchool` asserting the behaviors above. Assert document order: editorial H2 appears before Identificação when present (compare heading positions via `compareDocumentPosition` or DOM order of getAllByRole headings).

    2. Create `ReportCorrection/index.tsx` stub: `"use client"; export function ReportCorrection() { return null; }`

    3. Create `ReportCorrection/index.test.tsx` mirroring SchoolComparison's `jest.mock("next/navigation")` + mutable `URLSearchParams` pattern. Assert locked copy strings from UI-SPEC. For mailto, assert `href` starts with `mailto:` and includes the school slug in the subject or body (decoded).

    4. Update `15-VALIDATION.md`: set `wave_0_complete: false` still until greens, but check off Wave 0 file-creation bullets that now exist; fill Task ID column rows 15-W0-01…05 pointing at this plan; keep `nyquist_compliant: false` until phase end.
  </action>
  <verify>
    <automated>pnpm test -- SchoolProfile ReportCorrection 2>&1 | tee /tmp/15-00-t3.txt; grep -E "FAIL|●|Tests:" /tmp/15-00-t3.txt | head -40</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Sugerir correção ou atualização|Perfil oficial|qualitativeResearchNotes" src/features/schools/SchoolProfile/index.test.tsx` matches
    - `test -f src/features/schools/ReportCorrection/index.test.tsx` and contains `Inclua problemas em dados factuais, características (tags) e no texto de Perfil da escola.`
    - `rg -n "15-W0-01|TagsSection/index.test" .planning/phases/15-profile-ui-tags-narrative/15-VALIDATION.md` matches
    - Targeted Jest exits non-zero (RED)
  </acceptance_criteria>
  <done>Profile + correction RED tests and VALIDATION Wave 0 file tracking updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures → Jest DOM | Synthetic school data only; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-00-01 | I | ReportCorrection test fixtures | accept | Tests use public fixture slugs/names only — no secrets |
| T-15-00-02 | T | Wave 0 stubs | accept | Null stubs are non-production paths; real mitigations land in Plans 02–05 |
</threat_model>

<verification>
Targeted Jest suites for TagsSection, EditorialNarrative, tagTaxonomy, collectCitedSources, SourcesSection, SchoolProfile, and ReportCorrection exist and fail (RED) pending implementation plans.
</verification>

<success_criteria>
- All Wave 0 test files listed in 15-VALIDATION.md exist
- Brand-new modules have null stubs so imports resolve
- No production qualitative UI behavior yet (stubs return null / existing code unchanged except tests)
</success_criteria>

<output>
After completion, create `.planning/phases/15-profile-ui-tags-narrative/15-00-SUMMARY.md`
</output>
