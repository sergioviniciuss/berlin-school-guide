---
phase: 15-profile-ui-tags-narrative
plan: 02
type: execute
wave: 1
depends_on:
  - "00"
files_modified:
  - src/features/schools/collectCitedSources/types.ts
  - src/features/schools/collectCitedSources/index.ts
  - src/features/schools/collectCitedSources/index.test.ts
  - src/features/schools/SourcesSection/index.tsx
  - src/features/schools/SourcesSection/index.test.tsx
autonomous: true
requirements:
  - NARR-05
must_haves:
  truths:
    - "Fontes includes sources cited only by tags, deduped by source id with field citations"
    - "Each CitedSourceEntry exposes unique citedBy PT-BR labels for citing fields and tags"
    - "SourcesSection shows Citado em: only when citedBy is non-empty"
  artifacts:
    - path: "src/features/schools/collectCitedSources/types.ts"
      provides: "citedBy: string[] on CitedSourceEntry"
      contains: "citedBy"
    - path: "src/features/schools/collectCitedSources/index.ts"
      provides: "field ∪ tag citation merge with Map dedupe"
      contains: "school.tags"
    - path: "src/features/schools/SourcesSection/index.tsx"
      provides: "Citado em: line inside source articles"
      contains: "Citado em:"
  key_links:
    - from: "collectCitedSources"
      to: "school.tags[].citations"
      via: "second aggregation loop"
      pattern: "for \\(const tag of school\\.tags"
    - from: "SourcesSection article"
      to: "entry.citedBy"
      via: "conditional paragraph after date metadata"
      pattern: "Citado em:"
---

<objective>
Extend Fontes collection to merge field and tag citations (dedupe by source id) with cite-back labels, and render “Citado em:” in SourcesSection (D-16, D-17, NARR-05).

Purpose: Tag provenance appears in the single Fontes section without duplicates.
Output: Green collectCitedSources + SourcesSection cite-back; no perfil→sourceId invention (RESEARCH A2).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-profile-ui-tags-narrative/15-CONTEXT.md
@.planning/phases/15-profile-ui-tags-narrative/15-RESEARCH.md
@.planning/phases/15-profile-ui-tags-narrative/15-UI-SPEC.md
@.planning/phases/15-profile-ui-tags-narrative/15-PATTERNS.md
@src/features/schools/collectCitedSources/index.ts
@src/features/schools/collectCitedSources/types.ts
@src/features/schools/SourcesSection/index.tsx
@src/features/schools/SchoolProfile/constants.ts

<interfaces>
Current CitedSourceEntry (extend — do not remove fields):
```typescript
export type CitedSourceEntry = {
  source: Source;
  citations: Array<{ quote?: string; note?: string }>;
};
```

Target:
```typescript
export type CitedSourceEntry = {
  source: Source;
  citations: Array<{ quote?: string; note?: string }>;
  citedBy: string[]; // unique PT-BR labels; UI omits line when empty
};
```

Keep `SOURCE_TYPE_ORDER` and title sort unchanged.
Field labels: import `profileFieldLabels` from `@/features/schools/SchoolProfile/constants`.
Tag labels: import `formatTagId` from `@/features/schools/tagTaxonomy`.
Do NOT invent narrative citation associations for `perfilDaEscola` (no “Perfil da escola” in citedBy unless a real citation link exists — none today).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend collectCitedSources for tags + citedBy</name>
  <files>src/features/schools/collectCitedSources/types.ts, src/features/schools/collectCitedSources/index.ts</files>
  <read_first>
    - src/features/schools/collectCitedSources/types.ts
    - src/features/schools/collectCitedSources/index.ts
    - src/features/schools/collectCitedSources/index.test.ts
    - src/features/schools/SchoolProfile/constants.ts
    - src/features/schools/tagTaxonomy/index.ts
  </read_first>
  <behavior>
    - Tag-only source appears once
    - Field+tag same id dedupes to one entry
    - citedBy unique labels from profileFieldLabels[path] and formatTagId(tag.id)
  </behavior>
  <action>
    1. Add `citedBy: string[]` to `CitedSourceEntry` in `types.ts`.

    2. Rewrite aggregation in `index.ts` to track per sourceId:
       - `citationsBySourceId` (existing quote/note list)
       - `citedByBySourceId: Map<string, Set<string>>` (or array + uniqueness)

    3. Field loop (keep walking `importantSchoolFieldPathsDetailedV2`): on each citation, if source exists, push citation and add `profileFieldLabels[path]` to citedBy set.

    4. Tag loop after fields:
    ```ts
    for (const tag of school.tags ?? []) {
      for (const citation of tag.citations) {
        if (!sourceById.has(citation.sourceId)) continue;
        // push { quote, note } into citationsBySourceId
        // citedBy.add(formatTagId(tag.id))
      }
    }
    ```

    5. When building `CitedSourceEntry`, set `citedBy: [...set]` sorted with `localeCompare(..., "pt-BR")` for stable output.

    6. Preserve `SOURCE_TYPE_ORDER` grouping and title sort. Remove unused dead imports if any appear — do not change export surface beyond citedBy.
  </action>
  <verify>
    <automated>pnpm test -- collectCitedSources</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "citedBy" src/features/schools/collectCitedSources/types.ts src/features/schools/collectCitedSources/index.ts` matches
    - `rg -n "school\\.tags" src/features/schools/collectCitedSources/index.ts` matches
    - `rg -n "perfilDaEscola" src/features/schools/collectCitedSources/index.ts` returns no matches (no invented narrative cites)
    - `pnpm test -- collectCitedSources` exits 0
  </acceptance_criteria>
  <done>Field+tag Fontes merge with citedBy greens Wave 0 collectCitedSources tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render Citado em: in SourcesSection</name>
  <files>src/features/schools/SourcesSection/index.tsx, src/features/schools/SourcesSection/index.test.tsx</files>
  <read_first>
    - src/features/schools/SourcesSection/index.tsx
    - src/features/schools/SourcesSection/index.test.tsx
    - .planning/phases/15-profile-ui-tags-narrative/15-UI-SPEC.md
  </read_first>
  <action>
    Inside each source `<article>`, after the publisher/date metadata blocks and before the external URL link (UI-SPEC: after publisher/date metadata), insert:
    ```tsx
    {entry.citedBy.length > 0 ? (
      <p className="text-sm text-neutral-600">
        <span className="font-semibold">Citado em:</span>{" "}
        {entry.citedBy.join(", ")}
      </p>
    ) : null}
    ```
    Do not render empty “Citado em:”. Keep `id={`source-${entry.source.id}`}` and existing blue source-type chips unchanged (D-13). Remove unused imports (`calculateEvidenceCoverage`, `formatFieldStatus`, `getCoverageTierLabel`) if still present and unused.
  </action>
  <verify>
    <automated>pnpm test -- SourcesSection collectCitedSources</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Citado em:" src/features/schools/SourcesSection/index.tsx` matches
    - `rg -n "citedBy\\.length" src/features/schools/SourcesSection/index.tsx` matches
    - `pnpm test -- SourcesSection` exits 0
  </acceptance_criteria>
  <done>SourcesSection shows cite-back line; NARR-05 Fontes UI complete at component level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| School static data → Fontes HTML | Trusted build-time content rendered as React text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | T | collectCitedSources | mitigate | Skip unknown citation.sourceId via sourceById.has check (existing pattern) |
| T-15-02-02 | I | citedBy labels | accept | Labels are closed PT-BR maps from code, not raw user strings |
| T-15-02-03 | XSS | SourcesSection | mitigate | Render citedBy via React text nodes only — never dangerouslySetInnerHTML |
</threat_model>

<verification>
`pnpm test -- collectCitedSources SourcesSection` green.
</verification>

<success_criteria>
- Tag-only sources appear in Fontes once
- Field+tag overlapping sourceIds dedupe
- Citado em: visible iff citedBy non-empty
</success_criteria>

<output>
After completion, create `.planning/phases/15-profile-ui-tags-narrative/15-02-SUMMARY.md`
</output>
