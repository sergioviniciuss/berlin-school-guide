---
phase: 11-flagship-discovery-release
plan: 02
type: execute
wave: 1
depends_on: ["00"]
files_modified:
  - src/features/guides/GuidesHub/index.tsx
  - src/features/guides/GuidesHub/index.test.tsx
  - src/app/guides/page.test.tsx
autonomous: true
requirements: [DISC-02]
must_haves:
  truths:
    - "Entenda o sistema section stacks onboarding flagship card above Berlin card"
    - "Flagship card uses primary chrome and links to /guides/german-education-system"
    - "Berlin card is retitled Como funciona o sistema escolar público de Berlim with secondary chrome and CTA Ler guia do sistema escolar unchanged"
    - "First-steps hub body copy matches D-06 when-ready checklist role"
  artifacts:
    - path: "src/features/guides/GuidesHub/index.tsx"
      provides: "Flagship + Berlin stacked under Entenda o sistema"
      exports: ["GuidesHub"]
      contains: "Ler guia do sistema educacional"
    - path: "src/features/guides/GuidesHub/index.test.tsx"
      provides: "Assertions for flagship and Berlin CTAs"
      contains: "german-education-system"
    - path: "src/app/guides/page.test.tsx"
      provides: "Page-level flagship link assertion"
      contains: "Ler guia do sistema educacional"
  key_links:
    - from: "src/features/guides/GuidesHub/index.tsx"
      to: "/guides/german-education-system"
      via: "flagship Button Link"
      pattern: "href=\"/guides/german-education-system\""
    - from: "src/features/guides/GuidesHub/index.tsx"
      to: "/guides/berlin-school-system"
      via: "Berlin Button Link with label Ler guia do sistema escolar"
      pattern: "href=\"/guides/berlin-school-system\""
---

<objective>
Make `/guides` hub present the onboarding guide as the flagship card above the retitled Berlin complementary card (DISC-02 / D-05–D-08 hub).

Purpose: Hub reading order matches Germany-first → Berlin-in-practice → checklist.
Output: Updated `GuidesHub` + unit/page tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-flagship-discovery-release/11-CONTEXT.md
@.planning/phases/11-flagship-discovery-release/11-UI-SPEC.md
@.planning/phases/11-flagship-discovery-release/11-PATTERNS.md
@.planning/phases/11-flagship-discovery-release/11-00-SUMMARY.md
@AGENTS.md

<interfaces>
Keep section shell:
```tsx
<section aria-labelledby="guides-system-heading">…</section>
<section aria-labelledby="guides-checklists-heading">…</section>
```

Label collision guard (Pitfall 2):
- Flagship CTA accessible name MUST be `Ler guia do sistema educacional`
- Berlin CTA accessible name MUST remain exactly `Ler guia do sistema escolar`

Do not modify `src/app/guides/page.tsx` shell — only `GuidesHub` + tests.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend GuidesHub and guides page tests for flagship card</name>
  <files>src/features/guides/GuidesHub/index.test.tsx, src/app/guides/page.test.tsx</files>
  <read_first>
    - src/features/guides/GuidesHub/index.test.tsx
    - src/features/guides/GuidesHub/index.tsx
    - src/app/guides/page.test.tsx
    - .planning/phases/11-flagship-discovery-release/11-UI-SPEC.md
    - .planning/phases/11-flagship-discovery-release/11-PATTERNS.md
  </read_first>
  <behavior>
    - Flagship link `Ler guia do sistema educacional` → `/guides/german-education-system`
    - Berlin link `Ler guia do sistema escolar` → `/guides/berlin-school-system` (unchanged)
    - Heading `Sistema educacional na Alemanha` and retitled Berlin heading present
    - Flagship article has `border-l-primary`; Berlin article does not
  </behavior>
  <action>
    Update `src/features/guides/GuidesHub/index.test.tsx`:

    1. Keep existing assertion for `Ler guia do sistema escolar` → `/guides/berlin-school-system` and `Abrir checklist` → `/guides/first-steps`.
    2. Add assertion: `Ler guia do sistema educacional` → `/guides/german-education-system`.
    3. Assert headings: `Sistema educacional na Alemanha` and `Como funciona o sistema escolar público de Berlim`.
    4. Assert `querySelectorAll` under Entenda o sistema / overall articles: flagship `border-l-primary` is on the article closest to `Sistema educacional na Alemanha`.
    5. Optionally assert first-steps body contains `Quando estiver pronto para agir` (exact D-06 string substring).

    Update `src/app/guides/page.test.tsx` to assert the flagship link `Ler guia do sistema educacional` is present with href `/guides/german-education-system`, while keeping Berlin CTA assertion.

    Tests may be RED until Task 2.
  </action>
  <verify>
    <automated>pnpm test -- --testPathPattern="GuidesHub|guides/page.test" --no-coverage 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Ler guia do sistema educacional" src/features/guides/GuidesHub/index.test.tsx src/app/guides/page.test.tsx` matches both files
    - `rg -n "german-education-system" src/features/guides/GuidesHub/index.test.tsx` matches
    - `rg -n "Ler guia do sistema escolar" src/features/guides/GuidesHub/index.test.tsx` matches (Berlin label retained)
    - `rg -n "Como funciona o sistema escolar público de Berlim" src/features/guides/GuidesHub/index.test.tsx` matches
  </acceptance_criteria>
  <done>Hub tests encode flagship-above-Berlin and distinct CTA labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GuidesHub flagship stack and D-06/D-08 hub copy</name>
  <files>src/features/guides/GuidesHub/index.tsx</files>
  <read_first>
    - src/features/guides/GuidesHub/index.tsx
    - src/features/guides/GuidesHub/index.test.tsx
    - .planning/phases/11-flagship-discovery-release/11-UI-SPEC.md
  </read_first>
  <behavior>
    - Same two sections; Entenda o sistema has two articles (flagship then Berlin)
    - Checklists section structure unchanged; only first-steps body text changes
  </behavior>
  <action>
    Rewrite `GuidesHub` to match UI-SPEC Component Contracts (D-05–D-08, D-06 first-steps body):

    Under `Entenda o sistema`:
    1. Flagship article (primary chrome `border-l-4 border-l-primary bg-white p-6 md:p-8`):
       - h3: `Sistema educacional na Alemanha`
       - body: `Comece aqui. Entenda como funciona o sistema educacional na Alemanha antes de escolher uma escola.`
       - Button default: Link `Ler guia do sistema educacional` → `/guides/german-education-system`
    2. Berlin article (secondary chrome `bg-neutral-50 p-5 md:p-6`, outline Button):
       - h3: `Como funciona o sistema escolar público de Berlim`
       - body: `Depois, veja como essas regras se aplicam especificamente às escolas públicas de Berlim.`
       - Button outline: Link `Ler guia do sistema escolar` → `/guides/berlin-school-system` (**exact label — do not change**)

    Under `Checklists práticas` first-steps article, change **only** the paragraph body to:
    `Quando estiver pronto para agir, siga o checklist prático.`
    Keep title `Primeiros passos` and button `Abrir checklist` → `/guides/first-steps`.

    Do not add a third system card or new section wrappers.
  </action>
  <verify>
    <automated>pnpm test -- --testPathPattern="GuidesHub|guides/page.test" --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Ler guia do sistema educacional" src/features/guides/GuidesHub/index.tsx` matches
    - `rg -n "Ler guia do sistema escolar" src/features/guides/GuidesHub/index.tsx` matches
    - `rg -n "Como funciona o sistema escolar público de Berlim" src/features/guides/GuidesHub/index.tsx` matches
    - `rg -n "Comece aqui. Entenda como funciona" src/features/guides/GuidesHub/index.tsx` matches
    - `rg -n "Quando estiver pronto para agir, siga o checklist prático." src/features/guides/GuidesHub/index.tsx` matches
    - `rg -n "border-l-primary" src/features/guides/GuidesHub/index.tsx` matches exactly once
    - `pnpm test -- --testPathPattern="GuidesHub|guides/page.test" --no-coverage` exits 0
  </acceptance_criteria>
  <done>Guides hub shows onboarding flagship above retitled Berlin card with D-06 roles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static hub HTML → browser | Hardcoded internal guide links only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | T | GuidesHub Link hrefs | mitigate | Hardcode `/guides/german-education-system`, `/guides/berlin-school-system`, `/guides/first-steps` only |
| T-11-02-02 | I | Hub card copy | accept | Static editorial Portuguese; no user content |
</threat_model>

<verification>
GuidesHub + guides page unit tests green. Smoke Berlin CTA label still finds a unique link.
</verification>

<success_criteria>
- DISC-02 satisfied: onboarding is flagship above Berlin under Entenda o sistema
- Berlin CTA label unchanged for existing tests/smoke
</success_criteria>

<output>
After completion, create `.planning/phases/11-flagship-discovery-release/11-02-SUMMARY.md`
</output>
