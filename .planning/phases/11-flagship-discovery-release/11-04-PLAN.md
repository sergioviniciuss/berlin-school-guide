---
phase: 11-flagship-discovery-release
plan: 04
type: execute
wave: 2
depends_on: ["00"]
files_modified:
  - src/content/guides/german-education-system.mdx
  - src/content/guides/first-steps.mdx
  - e2e/onboarding.spec.ts
autonomous: true
requirements: [ONBD-06, DISC-03]
must_haves:
  truths:
    - "german-education-system.mdx has ## Próximos passos immediately before ## Glossário"
    - "Próximos passos has exactly three ordered exits: Berlin guide, /schools, first-steps — no /compare"
    - "first-steps.mdx opens with the italic Novo em Berlim? tip linking to onboarding"
  artifacts:
    - path: "src/content/guides/german-education-system.mdx"
      provides: "Próximos passos closing narrative (D-13–D-15)"
      contains: "## Próximos passos"
    - path: "src/content/guides/first-steps.mdx"
      provides: "Soft conditional tip (D-11)"
      contains: "Novo em Berlim?"
    - path: "e2e/onboarding.spec.ts"
      provides: "Optional assertion that Próximos passos and three exits render"
      contains: "Próximos passos"
  key_links:
    - from: "src/content/guides/german-education-system.mdx"
      to: "/guides/berlin-school-system"
      via: "Próximos passos item 1 MDX link"
      pattern: "\\(/guides/berlin-school-system\\)"
    - from: "src/content/guides/german-education-system.mdx"
      to: "/schools"
      via: "Próximos passos item 2 MDX link"
      pattern: "\\(/schools\\)"
    - from: "src/content/guides/first-steps.mdx"
      to: "/guides/german-education-system"
      via: "top tip MDX link"
      pattern: "\\(/guides/german-education-system\\)"
---

<objective>
Add onboarding closing **Próximos passos** (before Glossário) and the first-steps soft top tip with cross-links (ONBD-06, DISC-03, D-11, D-13–D-15).

Purpose: Complete the linear discovery path Germany → Berlin → schools → act, without `/compare` or directory friction.
Output: Updated MDX files + onboarding e2e assertions for the closing section.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-flagship-discovery-release/11-CONTEXT.md
@.planning/phases/11-flagship-discovery-release/11-UI-SPEC.md
@.planning/phases/11-flagship-discovery-release/11-PATTERNS.md
@.planning/phases/11-flagship-discovery-release/11-00-SUMMARY.md
@AGENTS.md

<interfaces>
Placement rules:
- Insert `## Próximos passos` block **immediately above** existing `## Glossário` in `german-education-system.mdx` (line ~239 today)
- Insert first-steps tip as the **first** content of `first-steps.mdx`, above `Use esta checklist como roteiro…`
- Format: plain MDX ordered list / italic paragraph — **no** new React components, **no** BerlinCallout, **no** mini cards
- Do **not** edit `/schools` intro (D-12)
- Do **not** add `/compare` to Próximos passos (D-14)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Próximos passos before Glossário in onboarding MDX</name>
  <files>src/content/guides/german-education-system.mdx, e2e/onboarding.spec.ts</files>
  <read_first>
    - src/content/guides/german-education-system.mdx
    - e2e/onboarding.spec.ts
    - .planning/phases/11-flagship-discovery-release/11-UI-SPEC.md
    - .planning/phases/11-flagship-discovery-release/11-CONTEXT.md
  </read_first>
  <action>
    In `src/content/guides/german-education-system.mdx`, insert **immediately before** the existing `## Glossário` heading (D-13–D-15 / UI-SPEC):

    ```mdx
    ## Próximos passos

    Agora que você entende como funciona o sistema educacional alemão:

    1. **Entenda como isso funciona em Berlim** — veja as regras específicas do
       sistema escolar público berlinense. [Como funciona o sistema escolar
       público de Berlim](/guides/berlin-school-system)
    2. **Explore as escolas** — veja escolas primárias em Lichtenberg com status
       de evidência em cada informação. [Ver escolas](/schools)
    3. **Confira os primeiros passos** — checklist prático do zero até a visita
       e a matrícula. [Primeiros passos](/guides/first-steps)

    ```

    Ensure `## Glossário` remains the next heading after this block (Glossário stays last).

    Hard rules:
    - Exactly three list items in this order
    - No `/compare` anywhere in the new section
    - No new React components — Markdown ordered list only

    Extend `e2e/onboarding.spec.ts` with a focused test (or assertions in an existing onboarding navigation test) that:
    1. `goto("/guides/german-education-system")`
    2. Assert heading `Próximos passos` level 2 is visible
    3. Assert links present: name matching Berlin guide title → berlin-school-system; `Ver escolas` → `/schools`; `Primeiros passos` → `/guides/first-steps`
    4. Assert no link with href containing `/compare` inside the page’s Próximos passos region (or document-wide check that the three hrefs above exist and `/compare` is not among Próximos passos anchors — prefer scoping to the section if easy)

    Keep existing onboarding e2e coverage intact.
  </action>
  <verify>
    <automated>rg -n "## Próximos passos|/compare|## Glossário" src/content/guides/german-education-system.mdx; rg -n "Próximos passos" e2e/onboarding.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "^## Próximos passos$" src/content/guides/german-education-system.mdx` matches
    - `rg -n "\\(/guides/berlin-school-system\\)" src/content/guides/german-education-system.mdx` matches within the new section (file contains the path)
    - `rg -n "\\[Ver escolas\\]\\(/schools\\)" src/content/guides/german-education-system.mdx` matches
    - `rg -n "\\[Primeiros passos\\]\\(/guides/first-steps\\)" src/content/guides/german-education-system.mdx` matches
    - `rg -n "/compare" src/content/guides/german-education-system.mdx` returns exit code 1 for any new compare exit (if `/compare` exists elsewhere in file historically, ensure Próximos passos block has zero `/compare` — grep the 20 lines after `## Próximos passos` and confirm no match)
    - Shell check: `python3 -c "from pathlib import Path; t=Path('src/content/guides/german-education-system.mdx').read_text(); i=t.index('## Próximos passos'); j=t.index('## Glossário'); assert i < j; chunk=t[i:j]; assert '/compare' not in chunk; assert '/guides/berlin-school-system' in chunk; assert '[Ver escolas](/schools)' in chunk; assert '[Primeiros passos](/guides/first-steps)' in chunk; print('ok')"`
    - `rg -n "Próximos passos" e2e/onboarding.spec.ts` matches
  </acceptance_criteria>
  <done>Próximos passos exists before Glossário with three exits and e2e coverage.</done>
</task>

<task type="auto">
  <name>Task 2: Add first-steps soft top tip linking to onboarding</name>
  <files>src/content/guides/first-steps.mdx</files>
  <read_first>
    - src/content/guides/first-steps.mdx
    - .planning/phases/11-flagship-discovery-release/11-UI-SPEC.md
    - .planning/phases/11-flagship-discovery-release/11-CONTEXT.md
  </read_first>
  <action>
    Prepend to `src/content/guides/first-steps.mdx` as the first lines (D-11 / UI-SPEC), **above** the existing lead paragraph starting with `Use esta checklist como roteiro`:

    ```mdx
    _Novo em Berlim? Se você ainda não conhece o sistema educacional alemão,
    recomendamos começar pelo guia [Como funciona o sistema educacional na
    Alemanha](/guides/german-education-system) antes deste checklist._

    ```

    Rules:
    - Plain italic MDX paragraph only — do **not** wrap in BerlinCallout, border, or background
    - Soft phrasing: keep `recomendamos começar` (never “obrigatório”)
    - Do not change `first-steps/page.tsx` GuideIntro
    - Do not change `/schools` directory copy
  </action>
  <verify>
    <automated>rg -n "Novo em Berlim\?|german-education-system|BerlinCallout" src/content/guides/first-steps.mdx; python3 - <<'PY'
from pathlib import Path
lines=Path("src/content/guides/first-steps.mdx").read_text().strip().splitlines()
assert "Novo em Berlim?" in lines[0] or "Novo em Berlim?" in "\n".join(lines[:3])
assert "/guides/german-education-system" in "\n".join(lines[:5])
print("ok")
PY</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Novo em Berlim\?" src/content/guides/first-steps.mdx` matches
    - `rg -n "\\(/guides/german-education-system\\)" src/content/guides/first-steps.mdx` matches
    - `rg -n "BerlinCallout" src/content/guides/first-steps.mdx` returns exit code 1
    - `rg -n "obrigatório" src/content/guides/first-steps.mdx` returns exit code 1 for the tip lines (soft tip only)
    - File still contains `Use esta checklist como roteiro` after the tip
  </acceptance_criteria>
  <done>First-steps opens with soft conditional tip linking to onboarding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Author MDX → static HTML | Compiled at build time; links are static markdown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | T | MDX markdown links | mitigate | Hardcode internal paths only (`/guides/berlin-school-system`, `/schools`, `/guides/first-steps`, `/guides/german-education-system`) |
| T-11-04-02 | I | Editorial tip copy | accept | Static Portuguese content; no user-submitted MDX |
</threat_model>

<verification>
Structural python/rg checks pass; onboarding e2e assertions reference Próximos passos. Full e2e green after Plans 01–03 also land.
</verification>

<success_criteria>
- ONBD-06: clear next steps before glossary
- DISC-03: cross-links include first-steps ↔ onboarding and onboarding → directory/Berlin
</success_criteria>

<output>
After completion, create `.planning/phases/11-flagship-discovery-release/11-04-SUMMARY.md`
</output>
