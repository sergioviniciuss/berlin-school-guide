---
phase: 01-data-evidence-audit
plan: 01-01
type: refactor
wave: 1
depends_on: []
requirements: [DATA-01, DATA-02]
files_modified:
  - src/content/schools/real/lichtenbergPrimarySchools/inferredFrom/index.ts
  - src/content/schools/real/lichtenbergPrimarySchools/inferredFrom/index.test.ts
  - src/content/schools/real/lichtenbergPrimarySchools/inferredFrom/types.ts
  - src/content/schools/real/lichtenbergPrimarySchools/index.ts
autonomous: true
must_haves:
  truths:
    - No school field is marked verified when evidence comes from an unrelated field's citation
    - afterSchoolCare inferred from ganztag uses not_confirmed status with an explicit note
    - bilingualPrograms and internationalPrograms default to missing unless explicitly verified
    - inspectionAvailability for directory-only schools is missing not not_confirmed with portrait citation
  artifacts:
    - src/content/schools/real/lichtenbergPrimarySchools/inferredFrom/index.ts
    - src/content/schools/real/lichtenbergPrimarySchools/index.ts
  key_links:
    - primarySchool() imports inferredFrom from ./inferredFrom
    - All 10 schools in realLichtenbergPrimarySchools export valid School objects passing validate:data
---

# Plan 01-01: Evidence Data Integrity — inferredFrom + School Audit

<objective>
Replace implicit cross-field verification fallbacks in `primarySchool()` with an explicit `inferredFrom()` helper, then audit all 10 Lichtenberg school records field-by-field so verified status reflects independent confirmation only.
</objective>

<context>
@.planning/phases/01-data-evidence-audit/01-CONTEXT.md
@.planning/phases/01-data-evidence-audit/01-RESEARCH.md
@.planning/phases/01-data-evidence-audit/01-PATTERNS.md
@docs/DATA_MODEL.md
@docs/RESEARCH.md
@docs/research/REAL_SCHOOL_RESEARCH_SPIKE.md
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-01-01 | Inferred field displayed as verified misleads parents | high | inferredFrom() always returns not_confirmed; audit grep for cross-field verified reuse |
| T-01-02 | Incorrect school facts published with verified badge | high | Field-by-field audit against portrait URLs; validate:data gate |
</threat_model>

<verification>
pnpm test -- --testPathPattern=inferredFrom
pnpm validate:data
pnpm typecheck
</verification>

<task id="01-01-01" type="auto">
  <name>Create inferredFrom helper with tests</name>
  <read_first>
    - src/content/schools/real/lichtenbergPrimarySchools/index.ts
    - src/features/evidence/fieldEvidence/types.ts (or fieldEvidence index)
    - .planning/phases/01-data-evidence-audit/01-CONTEXT.md (D-01 through D-08)
  </read_first>
  <action>
    Create directory `src/content/schools/real/lichtenbergPrimarySchools/inferredFrom/` with:

    **types.ts** — export `InferredFromInput` with fields:
    - `sourceField: "ganztag" | "offers" | "schoolProfile"`
    - `sourceEvidence: FieldEvidence`
    - `note: string` (required, non-empty)

    **index.ts** — export function `inferredFrom(input: InferredFromInput): FieldEvidence` returning:
    ```typescript
    {
      status: "not_confirmed",
      citations: input.sourceEvidence.citations,
      note: input.note,
      lastChecked: input.sourceEvidence.lastChecked,
    }
    ```
    Throw if `note` is empty string.

    **index.test.ts** — test cases:
    1. Returns status `"not_confirmed"` (not `"verified"`)
    2. Preserves citations from sourceEvidence
    3. Includes provided note verbatim
    4. Throws on empty note
  </action>
  <acceptance_criteria>
    - `src/content/schools/real/lichtenbergPrimarySchools/inferredFrom/index.ts` exports `inferredFrom`
    - `rg 'status: "verified"' src/content/schools/real/lichtenbergPrimarySchools/inferredFrom/` returns 0 matches
    - `pnpm test -- --testPathPattern=inferredFrom` exits 0
  </acceptance_criteria>
  <automated>pnpm test -- --testPathPattern=inferredFrom</automated>
</task>

<task id="01-01-02" type="auto">
  <name>Refactor primarySchool() to use explicit inference rules</name>
  <read_first>
    - src/content/schools/real/lichtenbergPrimarySchools/index.ts
    - src/content/schools/real/lichtenbergPrimarySchools/inferredFrom/index.ts
    - .planning/phases/01-data-evidence-audit/01-CONTEXT.md (D-06, D-07, D-26, D-27, D-28)
  </read_first>
  <action>
    Refactor `primarySchool()` in `lichtenbergPrimarySchools/index.ts`:

    1. **afterSchoolCare** — Remove ganztag→verified fallback (lines 144-148). New logic:
       - If `input.afterSchoolCare` provided: use as-is
       - Else if `input.ganztag`: `field(input.ganztag, inferredFrom({ sourceField: "ganztag", sourceEvidence: directoryEvidence, note: "Valor inferido a partir do modelo Ganztag; contraturno não foi confirmado de forma independente." }))`
       - Else: `field(null, missingResearch)`

    2. **schoolProfile / pedagogyFocus** — When derived from `input.offers`:
       - Use `inferredFrom({ sourceField: "offers", sourceEvidence: directoryEvidence, note: "Perfil inferido a partir das ofertas listadas no retrato oficial; não confirmado de forma independente." })`
       - Never use `evidence(portrait.id)` directly for these inferred fields

    3. **bilingualPrograms / internationalPrograms** — Change defaults from `notApplicable(...)` to `field(null, missingResearch)` when not explicitly provided in input

    4. **inspectionAvailability** — For schools without explicit `input.inspectionAvailability`:
       - If `input.researchStatus === "directory_only"` OR coverageLevel defaults to directory: `field(null, missing("Inspeção oficial não pesquisada para escola com perfil básico."))`
       - Remove default `not_confirmed` with portrait citation for directory-only schools

    5. Import `inferredFrom` from `./inferredFrom`
  </action>
  <acceptance_criteria>
    - `rg 'input\.ganztag\s*\?\s*field\(input\.ganztag,\s*directoryEvidence\)' src/content/schools/real/lichtenbergPrimarySchools/index.ts` returns 0 matches (old afterSchoolCare fallback gone)
    - `rg 'notApplicable\(' src/content/schools/real/lichtenbergPrimarySchools/index.ts` returns 0 matches in primarySchool defaults for bilingualPrograms/internationalPrograms
    - `rg 'inferredFrom' src/content/schools/real/lichtenbergPrimarySchools/index.ts` returns at least 1 match
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <automated>pnpm typecheck</automated>
</task>

<task id="01-01-03" type="auto">
  <name>Audit all 10 Lichtenberg school records field-by-field</name>
  <read_first>
    - src/content/schools/real/lichtenbergPrimarySchools/index.ts (full file)
    - docs/research/REAL_SCHOOL_RESEARCH_SPIKE.md
    - .planning/phases/01-data-evidence-audit/01-CONTEXT.md (D-05, D-19)
    - .planning/PROJECT.md (school research status table)
  </read_first>
  <action>
    Review each school in `realLichtenbergPrimarySchools` array against its cited sources:

    **For all 10 schools:**
    - Confirm portrait-sourced fields (name, address, ganztag, languages) retain `verified` with portrait citation
    - Confirm afterSchoolCare: only `verified` when independent source exists (Lew-Tolstoi: lew-tolstoi-ganztag; Richard-Wagner: richard-wagner-ganztag); all others should be `not_confirmed` (inferred) or `missing`
    - Confirm offers-derived schoolProfile/pedagogyFocus on directory-only schools use `not_confirmed` via inferredFrom, not verified

    **Detailed schools (Adam-Ries, Lew-Tolstoi, Richard-Wagner):**
    - Verify each `verified` field cites a source that actually discusses that field topic
    - Adam-Ries: keep detailed if ≥8 independently verified detailed-level fields after corrections; else downgrade `coverageLevel` to `"directory"` and `researchStatus` to `"directory_only"`
    - Lew-Tolstoi / Richard-Wagner: same reclassification rule (D-19)

    **Directory-only schools (remaining 7):**
    - Set explicit `researchStatus: "directory_only"` and `coverageLevel: "directory"` where not already set
    - Remove any detailed-level verified claims not supported by portrait-only research

    **Lew-Tolstoi welcomeClasses:** keep `missing` (already correct)

    Run `pnpm validate:data` after all edits.
  </action>
  <acceptance_criteria>
    - `realLichtenbergPrimarySchools` array length is 10 (`rg -c 'primarySchool\(' src/content/schools/real/lichtenbergPrimarySchools/index.ts` returns 10)
    - `pnpm validate:data` exits 0
    - For directory-only schools without explicit afterSchoolCare override: `afterSchoolCare.evidence.status` is `"not_confirmed"` or `"missing"`, never `"verified"` unless independent citation exists
    - No school has `bilingualPrograms.evidence.status === "not_applicable"` unless explicit negative evidence note cites portrait stating absence
  </acceptance_criteria>
  <automated>pnpm validate:data</automated>
</task>

## PLANNING COMPLETE
