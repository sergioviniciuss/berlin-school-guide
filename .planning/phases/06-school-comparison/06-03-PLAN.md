---
phase: 06-school-comparison
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - src/features/schools/SchoolComparison/index.tsx
  - src/features/schools/SchoolComparison/index.test.tsx
  - src/features/schools/ComparisonTable/index.tsx
  - src/features/schools/ComparisonLimitations/index.tsx
  - src/app/compare/page.tsx
  - src/features/navigation/SiteHeader/constants.ts
  - src/features/navigation/SiteFooter/index.tsx
  - e2e/smoke.spec.ts
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04]
must_haves:
  truths:
    - /compare renders heading "Comparar" and restores schools from ?schools= slug list (D-05, D-17)
    - Criteria table rows follow PROFILE_SECTIONS groups with school columns — no composite score or winner styling (D-06, D-09, D-11)
    - Each cell shows formatted value + StatusBadge; column headers show tier label + coverage % (D-07, D-08, D-10, D-03)
    - Always-visible Portuguese limitations block above table links to /methodology (D-13, D-14, COMP-04)
    - Invalid slugs show notice; fewer than 2 valid schools shows empty state linking to /schools (D-18, D-16)
    - Mobile table scrolls horizontally with sticky criteria column (D-19)
    - Comparar appears in SiteHeader nav and SiteFooter when route ships (D-15)
    - E2E covers select → compare → URL restore journey (COMP-01)
  artifacts:
    - path: src/app/compare/page.tsx
      provides: Static compare route with Suspense + metadata
      contains: "SchoolComparison"
    - path: src/features/schools/SchoolComparison/index.tsx
      provides: Compare page shell, empty states, skipped-slug notice
      exports: ["SchoolComparison"]
    - path: src/features/schools/ComparisonTable/index.tsx
      provides: PROFILE_SECTIONS criteria table
      exports: ["ComparisonTable"]
    - path: src/features/schools/ComparisonLimitations/index.tsx
      provides: Always-visible limitations copy
      exports: ["ComparisonLimitations"]
    - path: src/features/navigation/SiteHeader/constants.ts
      provides: Comparar nav item
      contains: "/compare"
  key_links:
    - from: src/features/schools/SchoolComparison/index.tsx
      to: src/features/schools/parseCompareSlugs
      via: parseCompareSlugs(searchParams.get("schools"))
      pattern: "COMPARE_PAGE_PARAM|parseCompareSlugs"
    - from: src/features/schools/ComparisonTable/index.tsx
      to: src/features/schools/SchoolProfile/constants
      via: PROFILE_SECTIONS + profileFieldLabels
      pattern: "PROFILE_SECTIONS"
    - from: src/features/schools/ComparisonTable/index.tsx
      to: src/features/schools/formatProfileFieldValue
      via: cell value rendering + StatusBadge
      pattern: "formatProfileFieldValue"
---

# Plan 06-03: Comparison Page, Navigation, and E2E

<objective>
Ship `/compare` as a trust-first criteria table with limitations copy, shareable URLs, mobile-friendly layout, site navigation discoverability, and end-to-end test coverage.
</objective>

<context>
@.planning/phases/06-school-comparison/06-CONTEXT.md
@.planning/phases/06-school-comparison/06-RESEARCH.md
@docs/COMPARISON.md
@docs/INFORMATION_ARCHITECTURE.md
@src/features/schools/SchoolProfile/constants.ts
@src/app/schools/page.tsx
@src/features/navigation/SiteHeader/constants.ts
@src/features/navigation/SiteFooter/index.tsx
@e2e/smoke.spec.ts

<interfaces>
From plan 06-01:
```typescript
export const COMPARE_PAGE_PARAM = "schools";
export function parseCompareSlugs(raw: string | null): string[];
export function resolveCompareSchools(slugs: string[]): { schools: School[]; skippedSlugs: string[] };
export function formatProfileFieldValue(fieldPath: string, field: FieldValue<unknown>): string;
```

From `src/features/schools/SchoolProfile/constants.ts`:
```typescript
export const PROFILE_SECTIONS: ProfileSectionConfig[];
export const profileFieldLabels: Record<ImportantSchoolFieldPathDetailedV2, string>;
```

From evidence layer:
```typescript
export function calculateEvidenceCoverage(school: School): EvidenceCoverage;
export function getCoverageTierLabel(level: CoverageLevel): string;
export function getSchoolFieldByPath(school: School, fieldPath: string): FieldValue<unknown> | undefined;
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ?schools= URL → table cells | Slugs resolved to bundled School records only |
| Limitations copy → user trust | Static PT content; no dynamic ranking claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Tampering | Skipped slug notice | mitigate | Display skipped slugs as plain text list — React escaping, no HTML injection |
| T-06-09 | Spoofing | Empty-state links | mitigate | Hardcode href="/schools" and href="/methodology" only |
| T-06-10 | Tampering | Ranking via UI styling | mitigate | No conditional green/red cell backgrounds; no sort-by-coverage controls (D-09) |
| T-06-11 | Information Disclosure | Coverage % in headers | accept | Coverage = research completeness per product policy; limitations block explains ≠ quality (D-13) |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ComparisonLimitations and ComparisonTable</name>
  <files>
    src/features/schools/ComparisonLimitations/index.tsx
    src/features/schools/ComparisonTable/index.tsx
  </files>
  <behavior>
    - ComparisonLimitations renders always-visible block (not collapsible) with PT copy covering: no ranking, coverage ≠ quality, missing fields mean ask on visit; Link to /methodology (D-13, D-14)
    - ComparisonTable renders PROFILE_SECTIONS as section header rows + field rows (D-11)
    - Column headers: school name, tier label, coverage % via calculateEvidenceCoverage + getCoverageTierLabel (D-08)
    - Cells: formatProfileFieldValue + StatusBadge per field; missing field shows badge not blank (D-07, COMP-03)
    - inspectionAvailability uses availability-only formatting (D-12)
    - No sort controls, no winner highlight classes, no composite score (D-09)
    - Table wrapper overflow-x-auto; criteria column sticky left-0 z-10 bg-white below lg (D-19)
  </behavior>
  <action>
    Create `ComparisonLimitations/` with static Brazilian Portuguese prose aligned to `docs/COMPARISON.md` philosophy. Include inline Link to `/methodology`.

    Create `ComparisonTable/` accepting `schools: School[]`. Use semantic `<table>`. Map PROFILE_SECTIONS → profileFieldLabels for row labels. For each cell, use `getSchoolFieldByPath` — if undefined render em dash with missing StatusBadge treatment.

    Column header secondary line: `{getCoverageTierLabel(...)} · {percentage}%` with neutral text-xs styling — same semantics as directory cards, not quality.

    Per discretion A2: add per-column remove (X icon) that links to `/schools?compare=` with remaining slugs OR omits removed slug — URL remains source of truth. Minimum: link "Editar seleção" back to `/schools?compare={current slugs}`.

    Do NOT add card-per-school mobile layout (D-19 locked).
  </action>
  <verify>
    <automated>pnpm test -- --testPathPattern="ComparisonLimitations|ComparisonTable" --no-coverage</automated>
  </verify>
  <done>Limitations always visible; table mirrors profile sections; cells show value + badge; mobile sticky column works</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SchoolComparison shell, route, and empty states</name>
  <files>
    src/features/schools/SchoolComparison/index.tsx
    src/features/schools/SchoolComparison/index.test.tsx
    src/app/compare/page.tsx
  </files>
  <behavior>
    - Reads ?schools= via useSearchParams inside client component
    - resolveCompareSchools produces table data; skippedSlugs render visible notice (D-18)
    - fewer than 2 valid schools → empty state with heading + link to /schools (D-16, D-18)
    - 2–4 valid schools → limitations + table in slug URL order
    - Page heading "Comparar" (D-05)
    - /compare?schools=a,b restores same view (D-17)
  </behavior>
  <action>
    Create `SchoolComparison/` client component: parse `COMPARE_PAGE_PARAM` from searchParams, resolve schools, branch empty vs table view.

    Skipped slug notice example: "Não encontramos: {slugs}. Essas escolas foram ignoradas." (neutral, no error throw).

    Empty state copy directs user to `/schools` to pick schools (D-16). CTA uses Button or Link to `/schools`.

    Create `src/app/compare/page.tsx` mirroring `src/app/schools/page.tsx`: metadata title "Comparar", PT description, Suspense fallback "Carregando comparação...", wrap SchoolComparison.

    Tests: limitations present; empty state when 0–1 valid schools; table renders section headings Identificação/Inspeção; skipped slug notice when mixed valid/invalid.
  </action>
  <verify>
    <automated>pnpm test -- --testPathPattern="SchoolComparison" --no-coverage</automated>
  </verify>
  <done>/compare route works statically; URL restore and empty/skip paths covered by tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Comparar navigation and compare E2E journey</name>
  <files>
    src/features/navigation/SiteHeader/constants.ts
    src/features/navigation/SiteFooter/index.tsx
    e2e/smoke.spec.ts
  </files>
  <behavior>
    - NAV_ITEMS includes { href: "/compare", label: "Comparar" } between Escolas and Metodologia (D-15)
    - SiteFooter adds Comparar link alongside Metodologia (D-15, discretion A4)
    - E2E: select 2 schools via compare checkbox → click Comparar escolas → see Comparar heading and both school names in table
    - E2E: direct /compare?schools=lew-tolstoi-schule,adam-ries-schule restores comparison
    - E2E: invalid slug in URL shows notice or empty state without crash
  </behavior>
  <action>
    Insert Comparar into `NAV_ITEMS` — SiteHeader and mobile sheet consume this constant automatically (Phase 3 pattern).

    Add footer Link to `/compare` with label "Comparar".

    Extend `e2e/smoke.spec.ts` with compare journey using real Lichtenberg slugs (e.g. lew-tolstoi-schule, adam-ries-schule). Use compare checkbox aria-labels from SchoolCard. Assert URL contains `schools=` after navigation.

    Run full phase gate: `pnpm test && pnpm build` before marking complete.
  </action>
  <verify>
    <automated>pnpm e2e -- e2e/smoke.spec.ts -g compare</automated>
  </verify>
  <done>Comparar in header/footer; e2e compare flow passes; build succeeds</done>
</task>

</tasks>

<verification>
pnpm test -- --testPathPattern="SchoolComparison|ComparisonTable|ComparisonLimitations" --no-coverage && pnpm build && pnpm e2e -- e2e/smoke.spec.ts -g compare
</verification>

<success_criteria>
- /compare delivers side-by-side criteria comparison without ranking signals
- Shareable URLs restore comparison in fresh session
- Limitations and evidence coverage visible per COMP-02–COMP-04
- Comparar discoverable from site chrome; e2e validates core journey
</success_criteria>

<output>
After completion, create `.planning/phases/06-school-comparison/06-03-SUMMARY.md`
</output>
