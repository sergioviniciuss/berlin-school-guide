---
phase: 08-release-readiness
plan: 03
type: execute
wave: 2
depends_on: [08-01]
requirements: [REL-05, REL-06]
files_modified:
  - package.json
  - e2e/a11y.spec.ts
  - e2e/a11y-utils.ts
  - src/features/navigation/SiteHeader/index.tsx
  - src/features/schools/SchoolFilters/index.tsx
  - src/components/ui/Sheet/index.tsx
  - .planning/phases/08-release-readiness/08-HUMAN-UAT.md
autonomous: true
must_haves:
  truths:
    - Axe scans core journey routes with zero violations policy (D-07, D-08, D-09)
    - Core routes covered: /, /schools, sample profile, /compare, /guides, one guide, /methodology
    - Violations found are fixed in source (not suppressed without justification)
    - Human UAT doc prepared for keyboard/focus + mobile spot-check (D-07)
  artifacts:
    - path: e2e/a11y.spec.ts
      provides: Automated accessibility gate
    - path: .planning/phases/08-release-readiness/08-HUMAN-UAT.md
      provides: Manual a11y/metadata UAT checklist
  key_links:
    - from: e2e/a11y.spec.ts
      to: @axe-core/playwright
      via: AxeBuilder.analyze()
      pattern: "AxeBuilder"
---

# Plan 08-03: Accessibility Automation & Fixes

<objective>
Add `@axe-core/playwright` scans on core journey routes, fix all reported violations to meet zero-violations policy, and prepare human UAT for keyboard/focus review.
</objective>

<context>
@.planning/phases/08-release-readiness/08-CONTEXT.md
@.planning/phases/08-release-readiness/08-RESEARCH.md
@.planning/phases/08-release-readiness/08-PATTERNS.md
@.planning/codebase/CONCERNS.md
@e2e/smoke.spec.ts
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Axe rules | Automated only; manual UAT catches keyboard flow gaps axe misses |

## Threats

| Threat | Severity | Mitigation |
|--------|----------|------------|
| False positive axe rules | low | Fix root cause; document any disabled rule with justification in test file |
| Color contrast failures | medium | Adjust Tailwind tokens if axe reports contrast issues |

Public static pages only.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Axe Playwright setup</name>
  <action>
    - Add `@axe-core/playwright` devDependency
    - Create `e2e/a11y-utils.ts` with helper to run axe and format violations
    - Create `e2e/a11y.spec.ts` visiting scoped routes from D-08 (include valid compare URL)
    - Wire a11y.spec.ts into e2e-full.yml (main) and optionally PR smoke if fast enough
  </action>
</task>

<task type="auto">
  <name>Task 2: Fix axe violations</name>
  <action>
    - Run a11y suite locally, fix violations in components (common: missing labels, contrast, landmark structure, button names)
    - Likely touch areas: SiteHeader mobile sheet, SchoolFilters fieldsets, form controls, skip links if missing
    - Re-run until zero violations on all scoped routes
  </action>
</task>

<task type="auto">
  <name>Task 3: Human UAT template</name>
  <action>
    - Create `08-HUMAN-UAT.md` with 3–4 manual tests:
      - Keyboard nav through header → directory → profile
      - Focus visible on filters and compare toggle
      - Mobile 390px core journeys (no horizontal scroll)
      - OG/Twitter preview spot-check (cross-ref D-13 from 08-02)
    - Do NOT execute UAT in this plan — template only for /gsd-verify-work 8
  </action>
</task>

</tasks>

<verification>
pnpm e2e:ci -- e2e/a11y.spec.ts
pnpm test
pnpm build
</verification>

<success_criteria>
- a11y.spec.ts passes with zero axe violations on all D-08 routes
- No eslint-disable or axe rule disables without comment
- 08-HUMAN-UAT.md exists with pending tests
</success_criteria>
