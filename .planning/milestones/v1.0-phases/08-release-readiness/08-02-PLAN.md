---
phase: 08-release-readiness
plan: 02
type: execute
wave: 2
depends_on: [08-01]
requirements: [REL-04]
files_modified:
  - src/features/siteMetadata/index.ts
  - src/features/siteMetadata/index.test.ts
  - src/features/siteMetadata/constants.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/schools/page.tsx
  - src/app/compare/page.tsx
  - src/app/methodology/page.tsx
  - src/app/guides/page.tsx
  - src/app/guides/berlin-school-system/page.tsx
  - src/app/guides/first-steps/page.tsx
  - src/app/schools/[slug]/page.tsx
  - public/og-default.png
  - e2e/metadata.spec.ts
autonomous: true
must_haves:
  truths:
    - Every public page has unique PT title and description (D-10)
    - Root layout sets metadataBase, openGraph locale pt_BR, Twitter cards, default OG image (D-10, D-12)
    - Each page has canonical URL via alternates.canonical (D-10)
    - School profiles override metadata via generateMetadata (D-11)
    - Automated metadata checks on core routes (D-13)
  artifacts:
    - path: src/features/siteMetadata/index.ts
      provides: buildPageMetadata helper
      exports: ["buildPageMetadata", "siteMetadataDefaults"]
    - path: public/og-default.png
      provides: Default social share image
    - path: e2e/metadata.spec.ts
      provides: Automated title/canonical/OG assertions
  key_links:
    - from: src/app/layout.tsx
      to: src/features/siteMetadata
      via: metadataBase and default openGraph/twitter
      pattern: "metadataBase"
---

# Plan 08-02: SEO Metadata & Social Cards

<objective>
Implement complete V1 metadata (title, description, canonical, Open Graph, Twitter, pt_BR locale, default share image) across all public routes with automated verification.
</objective>

<context>
@.planning/phases/08-release-readiness/08-CONTEXT.md
@.planning/phases/08-release-readiness/08-RESEARCH.md
@.planning/phases/08-release-readiness/08-PATTERNS.md
@src/app/layout.tsx
@src/app/schools/[slug]/page.tsx
@docs/INFORMATION_ARCHITECTURE.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| metadataBase URL | Must use env-configurable production URL; document NEXT_PUBLIC_SITE_URL in DEPLOYMENT.md |

## Threats

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Wrong canonical URLs in staging | low | Env var with sensible default for local dev |

Static metadata only.
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Site metadata helper + default OG image</name>
  <action>
    - Create `src/features/siteMetadata/` with constants (SITE_URL, DEFAULT_OG_IMAGE) and `buildPageMetadata({ title, description, path })`
    - Add `public/og-default.png` — simple branded 1200×630 static image (Berlin School Guide + tagline in PT)
    - Unit tests: helper returns openGraph, twitter, alternates.canonical with pt_BR locale
  </action>
</task>

<task type="auto">
  <name>Task 2: Apply metadata to all routes</name>
  <action>
    - Update `src/app/layout.tsx`: metadataBase, default OG/Twitter, locale
    - Refactor each `page.tsx` metadata export to use helper (home, schools, compare, methodology, guides/*)
    - Extend `generateMetadata` in schools/[slug] for OG/Twitter/canonical while keeping dynamic title/description
    - Use NEXT_PUBLIC_SITE_URL env with fallback documented for local dev
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Metadata e2e smoke</name>
  <action>
    - Add `e2e/metadata.spec.ts` — visit core routes, assert document title and meta og:title, link rel=canonical present
    - Wire into main fuller workflow (08-01); optional fast subset in PR if time permits
  </action>
</task>

</tasks>

<verification>
pnpm test -- src/features/siteMetadata
pnpm build
pnpm e2e -- e2e/metadata.spec.ts
</verification>

<success_criteria>
- All 8 route types export distinct Portuguese titles
- buildPageMetadata tested for OG/Twitter/canonical fields
- Default OG image exists in public/
</success_criteria>
