---
phase: 08-release-readiness
plan: 04
type: execute
wave: 3
depends_on: [08-01, 08-02, 08-03]
requirements: [REL-01, REL-02, REL-07]
files_modified:
  - docs/DEPLOYMENT.md
  - .env.example
  - README.md
  - .github/workflows/ci.yml
  - .planning/phases/08-release-readiness/08-VERIFICATION.md
autonomous: true
must_haves:
  truths:
    - docs/DEPLOYMENT.md covers Vercel primary + generic static host (D-14)
    - Documents out/ directory, pnpm build, 404/fallback for SPA routes (D-15)
    - Recommends essential security headers (D-16)
    - Documents NEXT_PUBLIC_SITE_URL for canonical/OG URLs
    - Phase verification confirms REL-01–REL-07 evidence
  artifacts:
    - path: docs/DEPLOYMENT.md
      provides: Production deployment guide
  key_links:
    - from: docs/DEPLOYMENT.md
      to: next.config.mjs
      via: output export reference
      pattern: "output: \"export\""
---

# Plan 08-04: Deployment Docs & Release Verification

<objective>
Document Vercel and generic static deployment paths, environment variables, recommended headers, and run final release verification gate for all REL requirements.
</objective>

<context>
@.planning/phases/08-release-readiness/08-CONTEXT.md
@.planning/phases/08-release-readiness/08-RESEARCH.md
@docs/DECISIONS.md
@docs/PRODUCT.md
@next.config.mjs
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deployment docs | No secrets in repo; document env var names only |

## Threats

| Threat | Severity | Mitigation |
|--------|----------|------------|
| Missing 404 on generic hosts | medium | Document fallback to index.html or host-specific rewrite rules |

Documentation only.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: DEPLOYMENT.md</name>
  <action>
    - Create `docs/DEPLOYMENT.md` in Brazilian Portuguese (user-facing doc convention) OR English for ops — match docs/ style (check existing docs language: English in docs/)
    - Sections:
      - Prerequisites (Node 22, pnpm, Corepack)
      - Build: `pnpm install && pnpm build` → `out/`
      - Vercel deployment (connect repo, framework preset Next.js, output static)
      - Generic static host (upload out/, 404 handling for client routes)
      - Environment: NEXT_PUBLIC_SITE_URL
      - Recommended headers table (Cache-Control, X-Content-Type-Options, Referrer-Policy, Permissions-Policy)
      - Optional future: CSP, HSTS
      - CI overview (what gates run on PR)
  </action>
</task>

<task type="auto">
  <name>Task 2: Env example + README link</name>
  <action>
    - Add `.env.example` with NEXT_PUBLIC_SITE_URL
    - Link DEPLOYMENT.md from README if deployment section missing
  </action>
</task>

<task type="auto">
  <name>Task 3: Release verification</name>
  <action>
    - Run full regression: pnpm lint, typecheck, test, validate:data, build, e2e:ci (smoke + a11y + metadata)
    - Create `08-VERIFICATION.md` mapping REL-01–REL-07 to evidence
    - Update ROADMAP Phase 8 plans checklist
  </action>
</task>

</tasks>

<verification>
pnpm lint && pnpm typecheck && pnpm test && pnpm validate:data && pnpm build
pnpm e2e:ci
</verification>

<success_criteria>
- docs/DEPLOYMENT.md exists with both Vercel and generic paths
- Full regression gate passes locally
- 08-VERIFICATION.md scores 7/7 REL requirements
</success_criteria>
