---
phase: 05-directory-ux-discovery
plan: 02
type: execute
wave: 2
depends_on: [05-01]
requirements: [DISC-02, DISC-04, DISC-06]
files_modified:
  - src/features/schools/SchoolFilters/index.tsx
  - src/features/schools/SchoolFilters/types.ts
  - src/features/schools/SchoolFilters/index.test.tsx
  - src/features/schools/MobileFilterSheet/index.tsx
  - src/features/schools/SchoolDirectorySort/index.tsx
  - src/features/schools/SchoolDirectory/index.tsx
  - src/features/schools/SchoolDirectory/index.test.tsx
  - src/features/schools/countActiveFilters/index.ts
  - src/features/schools/countActiveFilters/index.test.ts
autonomous: true
must_haves:
  truths:
    - Filter groups split into Essenciais (always visible) and Avançados (collapsed by default) (D-21, D-23)
    - Ganztag and Willkommensklasse have inline help links to /methodology (D-22)
    - Mobile filters open in right-side Sheet matching SiteHeader pattern — not inline expand (D-09, D-12)
    - Hybrid apply — search and chip removal instant; drawer toggles batch until Apply or close (D-10)
    - Sticky mobile filter bar shows "Filtros (N ativos)" and opens drawer (D-11)
    - Desktop keeps sticky left sidebar filters at lg+ with no Sheet (D-12)
    - Mobile drawer has fixed header, scrollable body, fixed footer Apply/Clear (D-24)
    - Sort control visible above results beside count — native select with Portuguese labels (D-07)
    - Sort changes update URL immediately without debounce (D-08)
  artifacts:
    - path: src/features/schools/MobileFilterSheet/index.tsx
      provides: Sheet drawer with draft filter state and apply-on-close
      exports: ["MobileFilterSheet"]
    - path: src/features/schools/SchoolDirectorySort/index.tsx
      provides: Ordenar por native select
      exports: ["SchoolDirectorySort"]
    - path: src/features/schools/SchoolFilters/index.tsx
      provides: Tiered Essenciais/Avançados filter panel
      contains: "Filtros essenciais"
    - path: src/features/schools/countActiveFilters/index.ts
      provides: Active filter count for sticky bar
      exports: ["countActiveFilters"]
  key_links:
    - from: src/features/schools/SchoolDirectory/index.tsx
      to: src/features/schools/MobileFilterSheet
      via: controlled open state + draftFilters on mobile
      pattern: "MobileFilterSheet"
    - from: src/features/schools/MobileFilterSheet/index.tsx
      to: src/components/ui/Sheet
      via: side="right" SheetContent
      pattern: 'side="right"'
    - from: src/features/schools/SchoolDirectory/index.tsx
      to: src/features/schools/SchoolDirectorySort
      via: sortKey + onSortChange updates URL
      pattern: "SchoolDirectorySort"
---

# Plan 05-02: Filter Tiers, Mobile Sheet, and Sort Control

<objective>
Restructure filters into Essenciais/Avançados tiers with methodology help links, replace inline mobile filter expand with a Sheet drawer using hybrid draft apply, add sticky mobile filter bar, and expose sort control beside the result count row.
</objective>

<context>
@.planning/phases/05-directory-ux-discovery/05-CONTEXT.md
@.planning/phases/05-directory-ux-discovery/05-UI-SPEC.md
@.planning/phases/05-directory-ux-discovery/05-RESEARCH.md
@.planning/phases/05-directory-ux-discovery/05-PATTERNS.md
@src/features/navigation/SiteHeader/index.tsx
@src/components/ui/Sheet/index.tsx
@src/features/schools/SchoolDirectory/index.tsx
@src/features/schools/SchoolFilters/index.tsx

<interfaces>
From plan 05-01 (must be complete):
```typescript
export type DirectorySortKey = "name" | "coverage" | "tier";
export function sortFromSearchParams(params: URLSearchParams): DirectorySortKey;
// SchoolDirectory exposes updateFilters, sortKey, sortedSchools pipeline
```

From `SiteHeader` Sheet pattern:
```typescript
<Sheet open={open} onOpenChange={setOpen}>
  <SheetContent side="right" id="mobile-nav" className="w-[280px] max-w-[85vw]">
    <SheetHeader><SheetTitle>Navegação</SheetTitle></SheetHeader>
  </SheetContent>
</Sheet>
```

From `SchoolFilters/types.ts`:
```typescript
export type SchoolFiltersProps = {
  filters: DirectoryFilterState;
  options: DirectoryFilterOptions;
  onToggle: (key: keyof DirectoryFilterState, value: string) => void;
};
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mobile Sheet draft state → URL | Batched filter toggles committed to shareable query string |
| Filter help links → /methodology | Internal static route only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | Draft filter toggles | mitigate | Commit only via updateFilters with validated checkbox option sets from getDirectoryFilterOptions |
| T-05-06 | Information Disclosure | Sheet focus trap | accept | Radix modal behavior expected — search remains outside drawer per D-10 |
| T-05-07 | Spoofing | Help links | mitigate | Link href hardcoded to `/methodology` — no user-controlled URLs (D-22) |
</threat_model>

<verification>
pnpm test -- --testPathPattern="SchoolFilters|MobileFilterSheet|SchoolDirectorySort|countActiveFilters|SchoolDirectory" --no-coverage
pnpm typecheck
</verification>

<success_criteria>
- Essenciais contains Distrito, Bairro, Tipo, Ganztag, Bilíngue, Willkommensklasse (D-21)
- Avançados contains Idiomas, Foco educacional, Contraturno, Inspeção oficial, Cobertura da pesquisa — collapsed by default (D-21, D-23)
- Mobile Sheet apply-on-close commits draft filters to URL (D-10)
- Drawer checkbox toggles do NOT call router.replace until Apply or close (D-10)
- Chip removal and search still apply instantly via existing ActiveFilterSummary path (D-10)
- Sticky bar visible only below lg; desktop sidebar unchanged at lg+ (D-11, D-12)
- Sort select changes URL immediately; labels match UI-SPEC (D-07, DISC-03)
- No dedicated tier filter checkbox (D-20)
</success_criteria>

<output>
After completion, create `.planning/phases/05-directory-ux-discovery/05-02-SUMMARY.md`
</output>

<tasks>

<task id="05-02-01" type="auto" tdd="true">
  <name>Tiered SchoolFilters with methodology help links</name>
  <files>
    src/features/schools/SchoolFilters/index.tsx
    src/features/schools/SchoolFilters/types.ts
    src/features/schools/SchoolFilters/index.test.tsx
  </files>
  <read_first>
    - .planning/phases/05-directory-ux-discovery/05-UI-SPEC.md (filter tiers, help link copy/style)
    - .planning/phases/05-directory-ux-discovery/05-CONTEXT.md (D-21, D-22, D-23)
    - src/features/schools/SchoolFilters/index.tsx
  </read_first>
  <behavior>
    - Test 1: "Filtros essenciais" section heading visible
    - Test 2: Essenciais includes Distrito, Bairro, Tipo, Ganztag, Bilíngue, Willkommensklasse groups
    - Test 3: Avançados section inside details element, closed by default
    - Test 4: Avançados includes Idiomas, Foco educacional, Contraturno, Inspeção oficial, Cobertura da pesquisa
    - Test 5: Ganztag group has link "O que é Ganztag?" href="/methodology"
    - Test 6: Willkommensklasse group has link "O que é Willkommensklasse?" href="/methodology"
    - Test 7: Checkbox toggle still calls onToggle with correct key
  </behavior>
  <action>
    1. Restructure `SchoolFilters` per RESEARCH Pattern 4 and UI-SPEC:
    - Add section label `Filtros essenciais` (`text-sm font-semibold`) wrapping Essenciais groups
    - Essenciais order: Distrito, Bairro, Tipo, Ganztag, Bilíngue, Willkommensklasse (Ganztag in Essenciais per RESEARCH A1)
    - Wrap Avançados in native `<details>` with summary `Filtros avançados`, default closed
    - Avançados: Idiomas, Foco educacional, Cuidado no contraturno, Inspeção oficial, Cobertura da pesquisa (D-23)

    2. Extend `FilterGroup` to accept optional `helpLink?: { href: string; label: string }` for legend row:
    - Ganztag → `{ href: "/methodology", label: "O que é Ganztag?" }` (D-22)
    - Willkommensklasse → `{ href: "/methodology", label: "O que é Willkommensklasse?" }` (D-22)
    - Style: `text-sm text-blue-700 underline` below or beside legend per UI-SPEC

    3. Do NOT link to `/guides` — methodology interim per Phase 3 D-06 / deferred guides.

    4. Do NOT add tier-only filter checkbox (D-20).

    5. Write/extend tests in `index.test.tsx`.
  </action>
  <acceptance_criteria>
    - `rg "Filtros essenciais" src/features/schools/SchoolFilters/index.tsx` returns at least 1 match
    - `rg "Filtros avançados" src/features/schools/SchoolFilters/index.tsx` returns at least 1 match
    - `rg "O que é Ganztag" src/features/schools/SchoolFilters/index.tsx` returns at least 1 match
    - `rg "O que é Willkommensklasse" src/features/schools/SchoolFilters/index.tsx` returns at least 1 match
    - `rg 'href="/methodology"' src/features/schools/SchoolFilters/index.tsx` returns at least 2 matches
    - `rg "<details" src/features/schools/SchoolFilters/index.tsx` returns at least 1 match
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/features/schools/SchoolFilters/index.test.tsx --no-coverage</automated>
  </verify>
  <done>SchoolFilters renders tiered sections with methodology help links; Avançados collapsed by default</done>
</task>

<task id="05-02-02" type="auto" tdd="true">
  <name>countActiveFilters utility and MobileFilterSheet drawer</name>
  <files>
    src/features/schools/countActiveFilters/index.ts
    src/features/schools/countActiveFilters/index.test.ts
    src/features/schools/MobileFilterSheet/index.tsx
  </files>
  <read_first>
    - .planning/phases/05-directory-ux-discovery/05-RESEARCH.md (Pattern 3 mobile Sheet, countActiveFilters)
    - .planning/phases/05-directory-ux-discovery/05-PATTERNS.md (MobileFilterSheet analog)
    - .planning/phases/05-directory-ux-discovery/05-CONTEXT.md (D-09, D-10, D-11, D-12, D-24)
    - src/features/navigation/SiteHeader/index.tsx
    - src/components/ui/Sheet/index.tsx
  </read_first>
  <behavior>
    - Test 1: countActiveFilters counts query as 1 when non-empty
    - Test 2: countActiveFilters sums all selected array filter values
    - Test 3: countActiveFilters(defaultDirectoryFilters) returns 0
    - Test 4: MobileFilterSheet renders SheetTitle "Filtros"
    - Test 5: Toggling checkbox inside sheet calls onDraftToggle — not onApply
    - Test 6: "Aplicar filtros" calls onApply with draft state
    - Test 7: "Limpar filtros" footer resets draft to defaultDirectoryFilters then applies in one action (UI-SPEC)
  </behavior>
  <action>
    1. Create `countActiveFilters/index.ts`:
    ```typescript
    export function countActiveFilters(filters: DirectoryFilterState): number {
      let n = filters.query ? 1 : 0;
      for (const key of FILTER_ARRAY_KEYS) n += filters[key].length;
      return n;
    }
    ```
    Export FILTER_ARRAY_KEYS constant mirroring non-query keys. Colocate unit tests.

    2. Create `MobileFilterSheet/index.tsx` — client component:
    - Props: `open`, `onOpenChange`, `filters` (applied), `draftFilters`, `onDraftChange`, `onApply`, `onClearDraft`, `filterOptions`, `onDraftToggle`
    - Structure per UI-SPEC and D-24:
      - `SheetContent side="right" id="mobile-school-filters-sheet" className="flex h-full w-[min(100%,20rem)] flex-col p-0"`
      - Fixed header: `SheetTitle` "Filtros"
      - Scrollable body: `min-h-0 flex-1 overflow-y-auto px-6 py-4` with `SchoolFilters` bound to `draftFilters`
      - Fixed footer: primary "Aplicar filtros", secondary "Limpar filtros"
    - Footer "Limpar filtros": reset draft to `defaultDirectoryFilters` AND call onApply immediately (clear + apply one action per UI-SPEC)
    - Use `Button` from `@/components/ui/Button` for footer actions; min-h-11 for touch targets (44px)

    3. Apply-on-close handled by parent `SchoolDirectory` in next task — sheet exposes `onOpenChange` callback pattern.

    4. Match SiteHeader Sheet side="right" (D-09).
  </action>
  <acceptance_criteria>
    - `test -f src/features/schools/MobileFilterSheet/index.tsx` exits 0
    - `rg "export function countActiveFilters" src/features/schools/countActiveFilters/index.ts` returns 1 match
    - `rg 'side="right"' src/features/schools/MobileFilterSheet/index.tsx` returns at least 1 match
    - `rg "Aplicar filtros" src/features/schools/MobileFilterSheet/index.tsx` returns at least 1 match
    - `rg "overflow-y-auto" src/features/schools/MobileFilterSheet/index.tsx` returns at least 1 match
    - `rg "mobile-school-filters-sheet" src/features/schools/MobileFilterSheet/index.tsx` returns at least 1 match
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/features/schools/countActiveFilters/index.test.ts --no-coverage</automated>
  </verify>
  <done>countActiveFilters utility tested; MobileFilterSheet renders fixed header/footer with scrollable filter body</done>
</task>

<task id="05-02-03" type="auto" tdd="true">
  <name>SchoolDirectorySort, sticky bar, hybrid apply wiring</name>
  <files>
    src/features/schools/SchoolDirectorySort/index.tsx
    src/features/schools/SchoolDirectory/index.tsx
    src/features/schools/SchoolDirectory/index.test.tsx
  </files>
  <read_first>
    - .planning/phases/05-directory-ux-discovery/05-UI-SPEC.md (sort row layout, mobile sticky bar copy)
    - .planning/phases/05-directory-ux-discovery/05-CONTEXT.md (D-07, D-08, D-09–D-12, D-24)
    - src/features/schools/SchoolDirectory/index.tsx (post 05-01)
    - src/features/schools/SchoolSearch/index.tsx (input styling reference)
  </read_first>
  <behavior>
    - Test 1: SchoolDirectorySort renders label "Ordenar por" and select with three options
    - Test 2: Changing sort to coverage calls router.replace with sort=coverage immediately (no debounce)
    - Test 3: Sticky mobile button "Filtros" visible in document (lg:hidden class)
    - Test 4: Sticky button shows "Filtros (2 ativos)" when two filters active
    - Test 5: Opening sheet and toggling district checkbox does NOT call replace until Apply
    - Test 6: Closing sheet applies draft filters (apply-on-close D-10)
    - Test 7: Desktop aside still renders SchoolFilters at lg+ — no Sheet trigger in aside
    - Test 8: Remove inline mobileFiltersOpen expand panel entirely (D-09)
  </behavior>
  <action>
    1. Create `SchoolDirectorySort/index.tsx`:
    - Props: `value: DirectorySortKey`, `onChange: (key: DirectorySortKey) => void`
    - Native `<select>` styled like SchoolSearch input borders
    - Visible label `Ordenar por`; select `aria-label="Ordenar resultados do diretório"`
    - Options: `Nome (A–Z)` value name, `Cobertura da pesquisa` value coverage, `Perfil detalhado primeiro` value tier (D-07)
    - Place in flex row with result count: `flex flex-wrap items-center justify-between gap-3`

    2. Wire sort in `SchoolDirectory`:
    - `setSortKey(key)` → `router.replace` with extended `toQueryString(filters, key)` immediately
    - Omit `sort` param when key is `"name"` (D-08)

    3. Replace mobile inline expand (lines 137-159) with:
    - Sticky bar `lg:hidden sticky top-0 z-10 bg-white border-b py-3`:
      - Button opens sheet: `Filtros` or `Filtros ({n} ativos)` using countActiveFilters (D-11)
      - `aria-expanded`, `aria-controls="mobile-school-filters-sheet"`
    - State: `sheetOpen`, `draftFilters`
    - On sheet open: sync draftFilters from applied filters
    - `onDraftToggle`: mutate draftFilters only — NO router.replace (D-10)
    - Apply button + apply-on-close: `updateFilters(draftFilters)` (D-10)
    - Search and ActiveFilterSummary chip paths unchanged — instant apply (D-10)

    4. Desktop sidebar (`hidden lg:block sticky top-6`) keeps live `SchoolFilters` with instant toggleFilter — no draft state at lg+ (D-12).

    5. Remove `mobileFiltersOpen` state and inline panel completely.

    6. Extend SchoolDirectory tests with sheet open/apply and sort URL cases. Use fake timers only for search tests inherited from 05-01.

    7. Do NOT modify profile pages or comparison.
  </action>
  <acceptance_criteria>
    - `test -f src/features/schools/SchoolDirectorySort/index.tsx` exits 0
    - `rg "SchoolDirectorySort" src/features/schools/SchoolDirectory/index.tsx` returns at least 1 match
    - `rg "MobileFilterSheet" src/features/schools/SchoolDirectory/index.tsx` returns at least 1 match
    - `rg "mobileFiltersOpen" src/features/schools/SchoolDirectory/index.tsx` returns 0 matches
    - `rg "Abrir filtros" src/features/schools/SchoolDirectory/index.tsx` returns 0 matches
    - `rg "sticky" src/features/schools/SchoolDirectory/index.tsx` returns at least 1 match
    - `rg "Ordenar por" src/features/schools/SchoolDirectorySort/index.tsx` returns at least 1 match
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/features/schools/SchoolDirectory/index.test.tsx --no-coverage</automated>
  </verify>
  <done>Mobile Sheet replaces inline filters; sticky bar and sort control wired; hybrid apply verified in tests</done>
</task>

</tasks>
