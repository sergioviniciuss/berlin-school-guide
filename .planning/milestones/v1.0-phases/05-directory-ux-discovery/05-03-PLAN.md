---
phase: 05-directory-ux-discovery
plan: 03
type: execute
wave: 3
depends_on: [05-02]
requirements: [DISC-04, DISC-05, DISC-07, DISC-08]
files_modified:
  - src/features/schools/ActiveFilterSummary/utils.ts
  - src/features/schools/ActiveFilterSummary/index.tsx
  - src/features/schools/SchoolResults/index.tsx
  - src/features/schools/SchoolResults/index.test.tsx
  - src/features/schools/SchoolCard/index.tsx
  - src/features/schools/SchoolCard/index.test.tsx
  - src/features/schools/SchoolDirectory/index.tsx
  - src/features/schools/filterSchools/index.ts
  - docs/INFORMATION_ARCHITECTURE.md
  - e2e/smoke.spec.ts
autonomous: true
must_haves:
  truths:
    - Zero results lists active filters/search causing empty set with removal suggestions (D-13)
    - Zero-result state has "Limpar todos os filtros" button plus methodology link (D-14)
    - Search-only miss shows "Nenhuma escola com esse nome" with clear-search action (D-16)
    - Few results (1–2 schools) shows tip below count (D-15)
    - SchoolCard keeps coverage block with tier label and %; placement polish only (D-17)
    - Tier labels have ? help affordance explaining básica vs detalhada (D-18)
    - Shared URL with filters, q, and sort restores directory view in fresh session (DISC-04)
    - filterSchools documents client-side scaling approach for future Berlin-wide import (DISC-07)
  artifacts:
    - path: src/features/schools/SchoolResults/index.tsx
      provides: Contextual empty and search-miss states
      contains: "Nenhuma escola com esse nome"
    - path: src/features/schools/SchoolCard/index.tsx
      provides: Tier help affordance on coverage block
      contains: "aria-label"
    - path: src/features/schools/ActiveFilterSummary/utils.ts
      provides: Shared chip formatter for empty states
      exports: ["getActiveFilterChips", "formatChipValue"]
    - path: src/features/schools/filterSchools/index.ts
      provides: DISC-07 scaling comment
    - path: e2e/smoke.spec.ts
      provides: URL share restore test with sort param
  key_links:
    - from: src/features/schools/SchoolResults/index.tsx
      to: src/features/schools/ActiveFilterSummary/utils.ts
      via: getActiveFilterChips for bullet list
      pattern: "getActiveFilterChips"
    - from: src/features/schools/SchoolDirectory/index.tsx
      to: src/features/schools/SchoolResults
      via: passes filters, totalCount, onClearSearch, onResetFilters
      pattern: "onResetFilters"
    - from: e2e/smoke.spec.ts
      to: /schools
      via: goto shared URL with q and sort params
      pattern: "sort=coverage"
---

# Plan 05-03: Empty States, Card Help, E2E, and Scaling Note

<objective>
Deliver contextual empty and low-result UX, polish SchoolCard tier help, document client-side filter scaling, and extend e2e coverage for shareable filter/search/sort URLs.
</objective>

<context>
@.planning/phases/05-directory-ux-discovery/05-CONTEXT.md
@.planning/phases/05-directory-ux-discovery/05-UI-SPEC.md
@.planning/phases/05-directory-ux-discovery/05-RESEARCH.md
@.planning/phases/05-directory-ux-discovery/05-PATTERNS.md
@src/features/schools/SchoolResults/index.tsx
@src/features/schools/SchoolResults/index.test.tsx
@src/features/schools/SchoolCard/index.tsx
@src/features/schools/ActiveFilterSummary/index.tsx
@src/features/schools/SchoolDirectory/index.tsx
@e2e/smoke.spec.ts
@docs/INFORMATION_ARCHITECTURE.md

<interfaces>
From `SchoolResults` (extend):
```typescript
type SchoolResultsProps = {
  schools: SchoolDirectoryItem[];
  filters: DirectoryFilterState;
  totalCount: number;
  onClearSearch: () => void;
  onResetFilters: () => void;
};
```

From UI-SPEC empty state table (D-13–D-16):
| Scenario | h2 | Actions |
| Filters + zero | Nenhuma escola encontrada | Limpar todos os filtros + methodology link |
| Search-only miss | Nenhuma escola com esse nome | Limpar busca |
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Search query in empty state body | User-supplied `q` interpolated in Portuguese sentence |
| External links | None new — methodology is internal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | Empty state query display | mitigate | Render `{filters.query}` as React text in quotes — no HTML injection |
| T-05-09 | Spoofing | Methodology CTA | mitigate | Hardcoded `/methodology` href; existing static page |
</threat_model>

<verification>
pnpm test -- --testPathPattern="SchoolResults|SchoolCard|ActiveFilterSummary|filterSchools" --no-coverage
pnpm e2e -- e2e/smoke.spec.ts
pnpm typecheck
</verification>

<success_criteria>
- Empty state lists active filter chips as bullets when filters cause zero results (DISC-05, D-13)
- Search-only empty (query set, all array filters empty) shows dedicated heading and Limpar busca (D-16)
- Combined empty prefers search-specific copy when query set and no array filters (UI-SPEC combined rule)
- Few-results tip appears when 1 ≤ result count ≤ 2 (D-15)
- SchoolCard ? button has aria-label and title per tier (D-18); coverage % still visible (D-17)
- e2e opens fresh page with `?q=...&sort=coverage` and asserts restored view (DISC-04)
- filterSchools has scaling comment for DISC-07; INFORMATION_ARCHITECTURE documents sort param
- Profile pages (`src/app/schools/[slug]/`) and comparison untouched
</success_criteria>

<output>
After completion, create `.planning/phases/05-directory-ux-discovery/05-03-SUMMARY.md`
</output>

<tasks>

<task id="05-03-01" type="auto" tdd="true">
  <name>Shared chip formatter and contextual SchoolResults empty states</name>
  <files>
    src/features/schools/ActiveFilterSummary/utils.ts
    src/features/schools/ActiveFilterSummary/index.tsx
    src/features/schools/SchoolResults/index.tsx
    src/features/schools/SchoolResults/index.test.tsx
    src/features/schools/SchoolDirectory/index.tsx
  </files>
  <read_first>
    - .planning/phases/05-directory-ux-discovery/05-UI-SPEC.md (empty state copy table)
    - .planning/phases/05-directory-ux-discovery/05-CONTEXT.md (D-13, D-14, D-16)
    - .planning/phases/05-directory-ux-discovery/05-PATTERNS.md (ActiveFilterSummary chip extraction)
    - src/features/schools/ActiveFilterSummary/index.tsx
    - src/features/schools/SchoolResults/index.test.tsx
  </read_first>
  <behavior>
    - Test 1: getActiveFilterChips returns same labels as ActiveFilterSummary for sample filters
    - Test 2: Zero results with district filter shows h2 "Nenhuma escola encontrada" and bullet listing active filters (D-13)
    - Test 3: Zero results shows button "Limpar todos os filtros" calling onResetFilters (D-14)
    - Test 4: Zero results shows link "Como funciona nossa pesquisa?" href /methodology (D-14)
    - Test 5: Search-only miss (query="xyz", arrays empty) shows h2 "Nenhuma escola com esse nome" (D-16)
    - Test 6: Search-only miss shows body with quoted query and button "Limpar busca" calling onClearSearch (D-16)
    - Test 7: Search-only takes precedence over generic filter empty when query set and no array filters (UI-SPEC combined rule)
  </behavior>
  <action>
    1. Extract from `ActiveFilterSummary/index.tsx` into `utils.ts`:
    - `export function formatChipValue(value: string): string`
    - `export function getActiveFilterChips(filters: DirectoryFilterState)` — move getChips logic
    - Re-export or import in ActiveFilterSummary — no behavior change to chips

    2. Extend `SchoolResults` props per RESEARCH Pattern 5:
    ```typescript
    type SchoolResultsProps = {
      schools: SchoolDirectoryItem[];
      filters: DirectoryFilterState;
      totalCount: number;
      onClearSearch: () => void;
      onResetFilters: () => void;
    };
    ```

    3. Empty state logic:
    - Detect search-only miss: `filters.query` non-empty AND all array filter arrays empty → D-16 variant
    - Else zero results → D-13 variant: h2 "Nenhuma escola encontrada", ul of getActiveFilterChips labels as bullets, suggest removing filters in prose
    - Both variants include D-14 actions where applicable (search-only gets Limpar busca; filter variant gets Limpar todos os filtros + methodology link)

    4. Wire `SchoolDirectory` to pass props:
    - `filters`, `totalCount={schools.length}`, `onClearSearch={() => updateFilters({ ...filters, query: "" })}` + sync searchInput
    - `onResetFilters={resetFilters}`

    5. Use `<button type="button">` for actions with focus rings — not div click handlers (UI-SPEC a11y).

    6. Do NOT use dangerouslySetInnerHTML for query display.
  </action>
  <acceptance_criteria>
    - `test -f src/features/schools/ActiveFilterSummary/utils.ts` exits 0
    - `rg "export function getActiveFilterChips" src/features/schools/ActiveFilterSummary/utils.ts` returns 1 match
    - `rg "Nenhuma escola com esse nome" src/features/schools/SchoolResults/index.tsx` returns at least 1 match
    - `rg "Limpar todos os filtros" src/features/schools/SchoolResults/index.tsx` returns at least 1 match
    - `rg "Limpar busca" src/features/schools/SchoolResults/index.tsx` returns at least 1 match
    - `rg "getActiveFilterChips" src/features/schools/SchoolResults/index.tsx` returns at least 1 match
    - `rg "dangerouslySetInnerHTML" src/features/schools/SchoolResults/` returns 0 matches
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/features/schools/SchoolResults/index.test.tsx --no-coverage</automated>
  </verify>
  <done>SchoolResults renders contextual empty states; chip formatter shared with ActiveFilterSummary</done>
</task>

<task id="05-03-02" type="auto" tdd="true">
  <name>SchoolCard tier help and few-results tip</name>
  <files>
    src/features/schools/SchoolCard/index.tsx
    src/features/schools/SchoolCard/index.test.tsx
    src/features/schools/SchoolDirectory/index.tsx
  </files>
  <read_first>
    - .planning/phases/05-directory-ux-discovery/05-UI-SPEC.md (SchoolCard tier help copy, few-results tip)
    - .planning/phases/05-directory-ux-discovery/05-CONTEXT.md (D-15, D-17, D-18)
    - src/features/schools/SchoolCard/index.tsx
  </read_first>
  <behavior>
    - Test 1: Coverage block still shows tier label and percentage (D-17)
    - Test 2: directory-level school shows ? help with aria-label for pesquisa básica (D-18)
    - Test 3: detailed-level school shows ? help with aria-label for pesquisa detalhada (D-18)
    - Test 4: Help button has title attribute with tier explanation text (D-18)
    - Test 5: SchoolDirectory shows "Poucos resultados — tente remover filtros" when sortedSchools.length is 1 or 2 (D-15)
    - Test 6: Tip NOT shown when 0 or 3+ results
  </behavior>
  <action>
    1. Polish SchoolCard coverage block (D-17) — keep structure and % visible; optional spacing `mt-4` consistency:
    - Keep "Cobertura da pesquisa" heading, `{coverageTierLabel} · {percentage}%` line, disclaimer sentence

    2. Add tier help affordance beside tier label (D-18):
    - `button type="button"` with `?` text or HelpCircle icon from lucide-react
    - `title` attribute with UI-SPEC tooltip copy for básica vs detalhada
    - `aria-label="O que significa pesquisa básica"` or detalhada variant based on `school.coverageLevel`
    - No new Popover primitive — title + aria-label sufficient per RESEARCH

    3. Add few-results tip in `SchoolDirectory` count row area (D-15):
    - When `sortedSchools.length >= 1 && sortedSchools.length <= 2`, render below count/sort row:
      `Poucos resultados — tente remover filtros` in subtle `text-sm text-neutral-600`
    - Do not show at 0 (empty state handles) or 3+

    4. Extend SchoolCard tests; add SchoolDirectory test for tip visibility.

    5. Do NOT modify `src/app/schools/[slug]/` profile pages.
  </action>
  <acceptance_criteria>
    - `rg "Cobertura da pesquisa" src/features/schools/SchoolCard/index.tsx` returns at least 1 match
    - `rg "evidenceCoverage.percentage" src/features/schools/SchoolCard/index.tsx` returns at least 1 match
    - `rg "aria-label" src/features/schools/SchoolCard/index.tsx` returns at least 1 match
    - `rg "Poucos resultados" src/features/schools/SchoolDirectory/index.tsx` returns at least 1 match
    - `rg "Popover" src/features/schools/SchoolCard/` returns 0 matches
    - `rg "src/app/schools/\\[slug\\]" src/features/schools/SchoolCard/` returns 0 matches
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- src/features/schools/SchoolCard/index.test.tsx src/features/schools/SchoolDirectory/index.test.tsx --no-coverage</automated>
  </verify>
  <done>SchoolCard tier help added; few-results tip shows for 1–2 matches</done>
</task>

<task id="05-03-03" type="auto" tdd="true">
  <name>DISC-07 scaling note, IA docs, and e2e URL share test</name>
  <files>
    src/features/schools/filterSchools/index.ts
    docs/INFORMATION_ARCHITECTURE.md
    e2e/smoke.spec.ts
  </files>
  <read_first>
    - .planning/phases/05-directory-ux-discovery/05-RESEARCH.md (Performance Notes DISC-07, scaling comment)
    - .planning/phases/05-directory-ux-discovery/05-CONTEXT.md (D-04, D-08)
    - docs/INFORMATION_ARCHITECTURE.md (query param section)
    - e2e/smoke.spec.ts (directory flow — update debounce timing)
  </read_first>
  <behavior>
    - Test 1: e2e navigates to `/schools?q=Lew-Tolstoi&sort=coverage` cold load — shows 1 result and coverage sort applied
    - Test 2: e2e existing search test still passes after debounce (Playwright auto-wait or explicit wait)
    - Test 3: filterSchools scaling comment present in source
  </behavior>
  <action>
    1. Add DISC-07 scaling comment atop `filterSchools` function per RESEARCH:
    ```typescript
    // Directory filter runs client-side on the full school list passed from static props.
    // V1 Lichtenberg (~10 schools): memoized filter → sort is sufficient.
    // If Berlin-wide import (500+ schools): consider pre-normalized search index,
    // virtualized list rendering, and/or Web Worker for filter — out of V1 scope.
    ```
    Do NOT add virtualization or workers — documentation only.

    2. Update `docs/INFORMATION_ARCHITECTURE.md` query param section:
    - Add `sort=name|coverage|tier` with note: omit when default name (D-08)
    - Example: `/schools?q=example&district=Mitte&sort=coverage`

    3. Extend `e2e/smoke.spec.ts`:
    - Update existing search fill test — Playwright `fill` + `expect` auto-wait should tolerate 300ms debounce; if flaky add `page.waitForTimeout(350)` before count assertion
    - Add test `restores directory view from shared URL with search and sort`:
      - `page.goto("/schools?q=Lew-Tolstoi&sort=coverage")`
      - Assert `1 de 10 escolas encontradas`
      - Assert Lew-Tolstoi card visible
      - Assert sort select value coverage (or URL contains sort=coverage)

    4. Optional: mobile viewport Sheet smoke — if time permits add `page.setViewportSize({ width: 375, height: 812 })` open Filtros assert SheetTitle; not blocking if omitted.

    5. Do NOT modify profile or comparison e2e tests beyond debounce tolerance fixes in directory test.
  </action>
  <acceptance_criteria>
    - `rg "Berlin-wide import" src/features/schools/filterSchools/index.ts` returns at least 1 match
    - `rg "sort=" docs/INFORMATION_ARCHITECTURE.md` returns at least 1 match
    - `rg "sort=coverage" e2e/smoke.spec.ts` returns at least 1 match
    - `rg "shared URL" e2e/smoke.spec.ts` returns at least 1 match OR `rg "sort=coverage" e2e/smoke.spec.ts` returns at least 1 match
  </acceptance_criteria>
  <verify>
    <automated>pnpm e2e -- e2e/smoke.spec.ts</automated>
  </verify>
  <done>Scaling documented; IA lists sort param; e2e verifies shareable URL restores filter and sort</done>
</task>

</tasks>
