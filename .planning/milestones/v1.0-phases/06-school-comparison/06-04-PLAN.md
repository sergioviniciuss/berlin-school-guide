---
phase: 06-school-comparison
plan: 04
type: execute
wave: 2.5
gap_closure: true
depends_on: [06-02]
blocks: [06-03]
requirements: [COMP-01]
files_modified:
  - src/features/schools/compareSelection/constants.ts
  - src/features/schools/compareSelection/index.ts
  - src/features/schools/compareSelection/index.test.ts
  - src/features/schools/CompareToggleButton/index.tsx
  - src/features/schools/CompareToggleButton/index.test.tsx
  - src/features/schools/parseCompareSlugs/index.ts
  - src/features/schools/parseCompareSlugs/index.test.ts
  - src/features/schools/SchoolCard/index.tsx
  - src/features/schools/SchoolCard/index.test.tsx
  - src/features/schools/SchoolDirectory/index.tsx
  - src/features/schools/SchoolDirectory/index.test.tsx
  - src/features/schools/SchoolProfile/index.tsx
  - src/features/schools/SchoolProfile/index.test.tsx
  - src/app/schools/[slug]/page.tsx
autonomous: true
must_haves:
  truths:
    - Max comparison selection is 3 schools, not 4 (D-03)
    - SchoolCard uses toggle button "Adicionar à comparação" / "Na comparação", not checkbox (D-01)
    - Profile header includes same toggle with max-3 rules (D-08, D-09)
    - Shared selection state via sessionStorage mirror + URL sync on directory (D-09)
    - Profile link separate from compare action (D-02)
  artifacts:
    - path: src/features/schools/CompareToggleButton/index.tsx
      provides: Shared compare toggle control
      exports: ["CompareToggleButton"]
    - path: src/features/schools/compareSelection/index.ts
      provides: sessionStorage + toggle helpers
      exports: ["MAX_COMPARE_SCHOOLS", "readStoredCompareSlugs", "writeStoredCompareSlugs", "toggleCompareSlug"]
---

# Plan 06-04: Gap Closure — Selection UX, Max 3, Profile Compare

<objective>
Correct Waves 1–2 to match revised CONTEXT: max 3 schools, toggle-button selection UX, shared selection state, and profile-header compare action.
</objective>

<context>
@.planning/phases/06-school-comparison/06-CONTEXT.md
@.planning/phases/06-school-comparison/06-DISCUSSION-LOG.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: compareSelection module and max-3 cap</name>
  <action>
    - Export `MAX_COMPARE_SCHOOLS = 3` from `compareSelection/constants.ts`
    - `parseCompareSlugs` uses this constant (remove local max 4)
    - `compareSelection/index.ts`: read/write sessionStorage ordered slug list; `toggleCompareSlug(slugs, slug)` add/remove with max cap preserving order
    - Update all tests expecting max 4 → max 3
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CompareToggleButton and SchoolCard refactor</name>
  <action>
    - Create `CompareToggleButton` with labels "Adicionar à comparação" / "Na comparação", min-h-11, disabled when selection full
    - Replace checkbox in SchoolCard with footer toggle button outside profile Link
    - Update SchoolCard tests
  </action>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Directory sync + SchoolProfile header action</name>
  <action>
    - SchoolDirectory: sync URL compare param ↔ sessionStorage on change; use MAX_COMPARE_SCHOOLS = 3
    - Add client wrapper or hook for profile page compare toggle reading/writing sessionStorage
    - SchoolProfile header: render CompareToggleButton for current school slug
    - Tests for profile toggle and directory max-3 gating
  </action>
</task>

</tasks>

<verification>
pnpm test -- --testPathPattern="compareSelection|CompareToggleButton|parseCompareSlugs|SchoolCard|SchoolDirectory|SchoolProfile" --no-coverage
</verification>
