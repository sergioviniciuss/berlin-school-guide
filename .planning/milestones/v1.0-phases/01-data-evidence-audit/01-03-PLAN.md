---
phase: 01-data-evidence-audit
plan: 01-03
type: feature
wave: 3
depends_on: [01-02]
requirements: [DATA-04, DATA-05, DATA-06]
files_modified:
  - src/features/schools/SchoolCard/index.tsx
  - src/features/schools/SchoolCard/index.test.tsx
  - src/features/schools/SchoolCard/utils.ts
  - src/features/schools/SchoolDirectory/index.tsx
  - src/features/schools/schoolDirectoryData/index.ts
  - src/features/schools/schoolDirectoryData/index.test.ts
  - src/app/schools/page.tsx
  - src/app/page.tsx
autonomous: true
must_haves:
  truths:
    - Every SchoolCard shows Perfil básico or Perfil detalhado badge
    - Coverage display includes Pesquisa básica or Pesquisa detalhada tier label
    - No sintética/synthetic/M2 wording in production pages or directory header
    - getSyntheticSchools is not exported from production data module
    - Homepage links to /schools without M2 eyebrow
  artifacts:
    - src/features/schools/SchoolCard/index.tsx
    - src/features/schools/SchoolDirectory/index.tsx
    - src/app/page.tsx
  key_links:
    - src/app/schools/page.tsx imports getSchoolDirectoryItems only (not getSyntheticSchools)
    - SchoolCard reads coverageLevel and coverageTierLabel from SchoolDirectoryItem
---

# Plan 01-03: Trust UX — Cards, Copy, Synthetic Isolation

<objective>
Make research depth visible on directory cards, replace misleading synthetic/M2 copy with honest Berlin School Guide messaging, and isolate synthetic test fixtures from production data paths.
</objective>

<context>
@.planning/phases/01-data-evidence-audit/01-CONTEXT.md (D-04, D-13 through D-25)
@.planning/phases/01-data-evidence-audit/01-RESEARCH.md
@.planning/codebase/CONCERNS.md
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| T-01-03 | Synthetic test data reachable from production pages | medium | Unexport getSyntheticSchools; grep CI guard; test asserts no import in src/app |
</threat_model>

<verification>
pnpm test -- --testPathPattern="SchoolCard|schoolDirectoryData"
rg -i "sintétic|synthetic|M2" src/app src/features/schools/SchoolDirectory src/features/schools/SchoolCard
rg "getSyntheticSchools" src/app
pnpm typecheck
</verification>

<task id="01-03-01" type="auto">
  <name>Update SchoolCard with badges, tier label, and field visibility rules</name>
  <read_first>
    - src/features/schools/SchoolCard/index.tsx
    - src/features/schools/SchoolCard/utils.ts
    - src/features/schools/filterSchools/types.ts
    - .planning/phases/01-data-evidence-audit/01-CONTEXT.md (D-04, D-15, D-16, D-17)
  </read_first>
  <action>
    Update `SchoolCard/index.tsx`:

    1. **Research level badge** — Below school name, render badge:
       - `coverageLevel === "directory"` → `<span>Perfil básico</span>`
       - `coverageLevel === "detailed"` → `<span>Perfil detalhado</span>`
       Use existing neutral/blue Tailwind classes consistent with directory header (`text-blue-700` for emphasis)

    2. **Coverage tier label** — In coverage box, change text to:
       `{coverageTierLabel} · {percentage}% · mede completude da pesquisa neste nível, não qualidade da escola.`
       (D-14 disclaimer with level-aware context)

    3. **Hide unsupported fields (D-16)** — Only render `<Fact>` when:
       - Field value is non-null AND status is not `"missing"`, OR
       - Status is `"not_confirmed"` with a displayable inferred value (show with muted tone via existing `getStatusTone`)
       Skip Facts entirely when status is `"missing"` and value is null

    4. **Inferred visual distinction (D-04)** — Ensure `not_confirmed` facts use `getStatusTone("not_confirmed")` (verify utils.ts handles this; add if missing)

    5. **Profile CTA (D-17)** — Add below coverage box:
       `<p className="text-sm text-neutral-600">Perfil completo em breve — estamos expandindo as páginas de detalhe.</p>`
       Do NOT link to `/schools/[slug]` yet (Phase 4); honest non-link framing

    Add/update `SchoolCard/index.test.tsx`:
    - Renders "Perfil básico" for directory-level fixture item
    - Renders "Perfil detalhado" for detailed-level fixture item
    - Renders "Pesquisa básica" or "Pesquisa detalhada" in coverage section
    - Does not render Fact for missing welcomeClasses on directory fixture
  </action>
  <acceptance_criteria>
    - `rg 'Perfil básico|Perfil detalhado' src/features/schools/SchoolCard/index.tsx` returns at least 2 matches
    - `rg 'Pesquisa básica|Pesquisa detalhada' src/features/schools/SchoolCard/index.tsx` returns at least 1 match
    - `rg 'em breve' src/features/schools/SchoolCard/index.tsx` returns at least 1 match
    - `pnpm test -- --testPathPattern=SchoolCard` exits 0
  </acceptance_criteria>
  <automated>pnpm test -- --testPathPattern=SchoolCard</automated>
</task>

<task id="01-03-02" type="auto">
  <name>Replace synthetic/M2 copy in directory, schools page, and homepage</name>
  <read_first>
    - src/features/schools/SchoolDirectory/index.tsx (header section lines 103-112)
    - src/app/schools/page.tsx
    - src/app/page.tsx
    - .planning/phases/01-data-evidence-audit/01-CONTEXT.md (D-20 through D-24)
  </read_first>
  <action>
    **SchoolDirectory header** — Replace lines 104-112 with:
    - Eyebrow: `Berlin School Guide` (not "Diretório estático")
    - Title: keep `Escolas`
    - Description: `Explore escolas primárias em Berlim com informações verificadas e transparência sobre o que ainda não confirmamos. Começamos por Lichtenberg — mais distritos em breve. A cobertura da pesquisa mede completude dos dados neste nível, não qualidade da escola.`

    **src/app/schools/page.tsx metadata** — Replace description with:
    `Diretório de escolas primárias em Berlim com pesquisa parcial em Lichtenberg. Cada fato indica seu status de evidência.`

    **src/app/page.tsx minimal fix (D-23):**
    - Remove `eyebrow="M2"` from GuideIntro (omit eyebrow prop or set to `Berlin School Guide`)
    - Change description to: `Guia de escolas primárias em Berlim para famílias brasileiras — com transparência sobre fontes e limites da pesquisa.`
    - Replace link: `<Link href="/schools">Ver escolas em Lichtenberg</Link>` (remove `/guides/m2-smoke` link)
  </action>
  <acceptance_criteria>
    - `rg -i "sintétic|synthetic" src/features/schools/SchoolDirectory/index.tsx src/app/schools/page.tsx src/app/page.tsx` returns 0 matches
    - `rg 'M2' src/app/page.tsx` returns 0 matches
    - `rg 'href="/schools"' src/app/page.tsx` returns at least 1 match
    - `rg 'Lichtenberg' src/features/schools/SchoolDirectory/index.tsx` returns at least 1 match
  </acceptance_criteria>
  <automated>rg -i "sintétic|synthetic|M2" src/app src/features/schools/SchoolDirectory</automated>
</task>

<task id="01-03-03" type="auto">
  <name>Isolate synthetic fixtures from production exports</name>
  <read_first>
    - src/features/schools/schoolDirectoryData/index.ts
    - src/features/schools/schoolDirectoryData/index.test.ts
    - src/features/schools/school/fixtures.ts
    - .planning/phases/01-data-evidence-audit/01-CONTEXT.md (D-25)
  </read_first>
  <action>
    1. **Remove `getSyntheticSchools` export** from `schoolDirectoryData/index.ts`:
       - Delete `syntheticSchoolFixtures` array and `getSyntheticSchools` function from this file
       - Keep `getRealSchools`, `getSchoolDirectoryItems`, `toSchoolDirectoryItem`

    2. **Update test** `schoolDirectoryData/index.test.ts`:
       - Remove test importing `getSyntheticSchools`
       - Add test: `getRealSchools()` returns length 10
       - Import fixtures directly from `@/features/schools/school/fixtures` if shape regression test needed

    3. **Fix production name fallback** in `toSchoolDirectoryItem`:
       - Replace `"Escola sintética sem nome"` with throw or empty string:
         `name: school.name.value ?? ""` 
       - Add dev-only assertion comment if value is empty

    4. Verify no production page imports synthetic path:
       - Confirm `src/app/schools/page.tsx` uses only `getSchoolDirectoryItems`
  </action>
  <acceptance_criteria>
    - `rg 'getSyntheticSchools' src/features/schools/schoolDirectoryData/index.ts` returns 0 matches
    - `rg 'getSyntheticSchools' src/app` returns 0 matches
    - `rg 'Escola sintética' src/` returns 0 matches
    - `pnpm test -- --testPathPattern=schoolDirectoryData` exits 0
  </acceptance_criteria>
  <automated>pnpm test -- --testPathPattern=schoolDirectoryData && rg "getSyntheticSchools" src/app</automated>
</task>

## PLANNING COMPLETE
