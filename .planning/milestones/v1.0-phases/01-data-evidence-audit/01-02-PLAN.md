---
phase: 01-data-evidence-audit
plan: 01-02
type: refactor
wave: 2
depends_on: [01-01]
requirements: [DATA-03]
files_modified:
  - src/features/schools/school/constants.ts
  - src/features/evidence/calculateEvidenceCoverage/index.ts
  - src/features/evidence/calculateEvidenceCoverage/index.test.ts
  - src/features/evidence/calculateEvidenceCoverage/types.ts
  - src/features/schools/filterSchools/types.ts
  - src/features/schools/schoolDirectoryData/index.ts
  - src/features/schools/getCoverageTierLabel/index.ts
autonomous: true
must_haves:
  truths:
    - evidenceCoverageVersion is v2
    - Directory schools measured against smaller field set than detailed schools
    - Coverage numerator counts only verified fields; not_confirmed never counts
    - SchoolDirectoryItem includes coverageLevel and coverageTierLabel
  artifacts:
    - src/features/schools/school/constants.ts
    - src/features/evidence/calculateEvidenceCoverage/index.ts
  key_links:
    - calculateEvidenceCoverage reads school.research.coverageLevel to select field path array
    - toSchoolDirectoryItem maps coverageTierLabel from getCoverageTierLabel helper
---

# Plan 01-02: Evidence Coverage v2 — Level-Aware Scoring

<objective>
Implement coverage v2 with level-aware denominators so directory-only schools are not penalized for missing detailed-level fields, and coverage percentages reflect only independently verified research.
</objective>

<context>
@.planning/phases/01-data-evidence-audit/01-CONTEXT.md (D-09 through D-14)
@.planning/phases/01-data-evidence-audit/01-RESEARCH.md
@docs/DATA_MODEL.md
@docs/DECISIONS.md
</context>

<threat_model>
| ID | Threat | Severity | Mitigation |
|----|--------|----------|------------|
| — | Static data miscalculation | low | Unit tests with fixture schools at both coverage levels |
</threat_model>

<verification>
pnpm test -- --testPathPattern="calculateEvidenceCoverage|getCoverageTierLabel|schoolDirectoryData"
pnpm typecheck
</verification>

<task id="01-02-01" type="auto">
  <name>Define v2 field paths and bump coverage version</name>
  <read_first>
    - src/features/schools/school/constants.ts
    - docs/DATA_MODEL.md (Minimum School Fields, V1 Important Fields)
    - .planning/phases/01-data-evidence-audit/01-CONTEXT.md (D-11)
  </read_first>
  <action>
    Update `src/features/schools/school/constants.ts`:

    1. Change `evidenceCoverageVersion` from `"v1"` to `"v2"`

    2. Rename `importantSchoolFieldPathsV1` → keep as alias OR rename to `importantSchoolFieldPathsDetailedV2` containing same 23 paths

    3. Add `importantSchoolFieldPathsDirectoryV2` with these paths exactly:
       ```
       "name", "schoolNumber", "website", "classification", "level",
       "location.district", "location.neighbourhood", "location.address",
       "gradesServed", "ganztag", "languages"
       ```
       (11 fields — directory-appropriate set from official portrait research)

    4. Export type `ImportantSchoolFieldPathDirectoryV2` and `ImportantSchoolFieldPathDetailedV2`

    5. Export helper `getImportantFieldPathsForCoverage(coverageLevel: "directory" | "detailed")` returning the appropriate array
  </action>
  <acceptance_criteria>
    - `rg 'evidenceCoverageVersion = "v2"' src/features/schools/school/constants.ts` returns 1 match
    - `rg 'importantSchoolFieldPathsDirectoryV2' src/features/schools/school/constants.ts` returns at least 1 match
    - `importantSchoolFieldPathsDirectoryV2` has length 11
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <automated>pnpm typecheck</automated>
</task>

<task id="01-02-02" type="auto">
  <name>Update calculateEvidenceCoverage for level-aware v2</name>
  <read_first>
    - src/features/evidence/calculateEvidenceCoverage/index.ts
    - src/features/evidence/calculateEvidenceCoverage/index.test.ts
    - src/features/schools/school/constants.ts
    - src/features/schools/school/fixtures.ts (validDirectoryOnlySchool, validDetailedPublicSchool)
  </read_first>
  <action>
    Update `calculateEvidenceCoverage/index.ts`:

    1. Import `getImportantFieldPathsForCoverage` from constants
    2. Select field paths: `getImportantFieldPathsForCoverage(school.research.coverageLevel)`
    3. Keep numerator logic: count fields where `evidence.status === "verified"`
    4. Keep exclusion: `not_applicable` excluded from denominator
    5. Ensure `not_confirmed` is NOT counted in numerator (only verified counts)

    Update `index.test.ts`:
    - Directory fixture: expect `version: "v2"`, `total: 11` (or actual verified count after Plan 01-01 audit adjustments — document expected values in test)
    - Detailed fixture: expect `version: "v2"`, `total: 23`
    - Add test: school with `not_confirmed` field does not increment verified count
    - Add test: directory school has lower total denominator than same school would if detailed

    Update `types.ts` if version literal needs widening for `"v2"`.
  </action>
  <acceptance_criteria>
    - `calculateEvidenceCoverage(validDirectoryOnlySchool).version` equals `"v2"` in test output
    - `calculateEvidenceCoverage(validDirectoryOnlySchool).total` equals 11
    - `calculateEvidenceCoverage(validDetailedPublicSchool).total` equals 23
    - `pnpm test -- --testPathPattern=calculateEvidenceCoverage` exits 0
  </acceptance_criteria>
  <automated>pnpm test -- --testPathPattern=calculateEvidenceCoverage</automated>
</task>

<task id="01-02-03" type="auto">
  <name>Add coverage tier label and extend SchoolDirectoryItem</name>
  <read_first>
    - src/features/schools/schoolDirectoryData/index.ts
    - src/features/schools/filterSchools/types.ts
    - .planning/phases/01-data-evidence-audit/01-CONTEXT.md (D-13)
  </read_first>
  <action>
    1. Create `src/features/schools/getCoverageTierLabel/index.ts`:
       ```typescript
       export function getCoverageTierLabel(coverageLevel: "directory" | "detailed"): "Pesquisa básica" | "Pesquisa detalhada" {
         return coverageLevel === "directory" ? "Pesquisa básica" : "Pesquisa detalhada";
       }
       ```
       Add `index.test.ts` with 2 cases.

    2. Extend `SchoolDirectoryItem` in `filterSchools/types.ts`:
       ```typescript
       coverageLevel: "directory" | "detailed";
       researchStatus: "directory_only" | "in_research" | "published";
       coverageTierLabel: "Pesquisa básica" | "Pesquisa detalhada";
       ```

    3. Update `toSchoolDirectoryItem()` in `schoolDirectoryData/index.ts` to map:
       - `coverageLevel: school.research.coverageLevel`
       - `researchStatus: school.research.status`
       - `coverageTierLabel: getCoverageTierLabel(school.research.coverageLevel)`

    4. Update `schoolDirectoryData/index.test.ts` if it asserts directory item shape.
  </action>
  <acceptance_criteria>
    - `SchoolDirectoryItem` type includes `coverageLevel`, `researchStatus`, `coverageTierLabel`
    - `getCoverageTierLabel("directory")` returns `"Pesquisa básica"`
    - `getCoverageTierLabel("detailed")` returns `"Pesquisa detalhada"`
    - `pnpm test -- --testPathPattern="getCoverageTierLabel|schoolDirectoryData"` exits 0
  </acceptance_criteria>
  <automated>pnpm test -- --testPathPattern="getCoverageTierLabel|schoolDirectoryData"</automated>
</task>

## PLANNING COMPLETE
