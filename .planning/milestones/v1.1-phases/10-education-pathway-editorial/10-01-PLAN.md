---
phase: 10-education-pathway-editorial
plan: 01
type: execute
wave: 1
depends_on: ["00"]
files_modified:
  - src/features/guides/GlossaryTerm/index.tsx
  - src/features/guides/EducationTimeline/constants.ts
  - src/features/guides/EducationTimeline/fixtures.ts
autonomous: true
requirements: [ONBD-05, ONBD-01]
must_haves:
  truths:
    - "GlossaryTerm root dl exposes id=slugifyHeading(term) and scroll-mt-20 so narrative #term links land"
    - "Timeline Ver seção completa targets real chapter hashes (no placeholder #o-caminho-da-kita-a-universidade)"
    - "Timeline summaries have no Fase 10 placeholder phrasing"
    - "Secondary Berlin tracks share #os-caminhos-possiveis; Depois da 6ª série has no timeline anchor"
  artifacts:
    - path: "src/features/guides/GlossaryTerm/index.tsx"
      provides: "GlossaryTerm with slugifyHeading id"
      exports: ["GlossaryTerm"]
      contains: "slugifyHeading"
    - path: "src/features/guides/EducationTimeline/constants.ts"
      provides: "Real anchorHref + final summaries + berlinNote on Grundschule"
      contains: "antes-da-escola"
    - path: "src/features/guides/EducationTimeline/fixtures.ts"
      provides: "Fixture hash no longer uses placeholder"
      contains: "ensino-primario"
  key_links:
    - from: "src/features/guides/GlossaryTerm/index.tsx"
      to: "src/features/guides/mdxUtils/index.ts"
      via: "import { slugifyHeading }"
      pattern: "slugifyHeading\\(term\\)"
    - from: "src/features/guides/EducationTimeline/constants.ts"
      to: "MDX H2 ids (Plan 02/03)"
      via: "anchorHref hashes matching slugifyHeading of locked H2s"
      pattern: "#antes-da-escola|#ensino-primario|#os-caminhos-possiveis|#formacao-profissional|#ensino-superior"
---

<objective>
Green Wave 0 tests: implement GlossaryTerm deep-link ids (D-14 / UI-SPEC) and sync EducationTimeline `constants.ts` + `fixtures.ts` to the locked H2 anchor map with real summaries and optional Grundschule `berlinNote` (D-04, D-09).

Purpose: Unblock MDX narrative glossary links and make timeline “Ver seção completa” resolve once chapters ship.
Output: Passing GlossaryTerm + constants tests; production timeline data without Phase 10 stubs.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-education-pathway-editorial/10-CONTEXT.md
@.planning/phases/10-education-pathway-editorial/10-UI-SPEC.md
@.planning/phases/10-education-pathway-editorial/10-PATTERNS.md
@.planning/phases/10-education-pathway-editorial/10-00-SUMMARY.md
@AGENTS.md

<interfaces>
From src/features/guides/EducationTimeline/types.ts:
```typescript
export type TimelineStage = {
  id: string;
  name: string;
  grades: string;
  ageRange: string;
  summary: string;
  anchorHref?: string;
  berlinNote?: string;
};
```

Discretion locked for this phase (research open Q1):
- Shared secondary hash `#os-caminhos-possiveis` for Gymnasium, ISS, Gemeinschaftsschule — do **not** modify `src/mdx-components.tsx` h3 mapping.
- Do **not** change DEMO_TRUNK / primaryBranches / germanyWideContext structure or equal visual weight (Phase 9 D-07 / ONBD-03).
- Do **not** change timeline region aria-label or page.tsx GuideIntro H1 (e2e contract).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement GlossaryTerm id + scroll-mt-20</name>
  <files>src/features/guides/GlossaryTerm/index.tsx</files>
  <read_first>
    - src/features/guides/GlossaryTerm/index.tsx
    - src/features/guides/GlossaryTerm/index.test.tsx
    - src/features/guides/mdxUtils/index.ts
    - src/mdx-components.tsx
    - .planning/phases/10-education-pathway-editorial/10-UI-SPEC.md
  </read_first>
  <behavior>
    - Root `dl` has `id={slugifyHeading(term)}`
    - Root `dl` className includes existing chrome plus `scroll-mt-20`
    - Visual tokens unchanged: `rounded-lg border border-neutral-200 bg-neutral-50 p-4`; dt/dd classes unchanged
  </behavior>
  <action>
    Update `src/features/guides/GlossaryTerm/index.tsx` per D-14 / UI-SPEC:

    ```tsx
    import type { ReactNode } from "react";
    import { slugifyHeading } from "@/features/guides/mdxUtils";

    type GlossaryTermProps = {
      term: string;
      children: ReactNode;
    };

    export function GlossaryTerm({ term, children }: GlossaryTermProps) {
      return (
        <dl
          id={slugifyHeading(term)}
          className="scroll-mt-20 rounded-lg border border-neutral-200 bg-neutral-50 p-4"
        >
          <dt className="text-xl font-semibold text-neutral-950">{term}</dt>
          <dd className="mt-1 text-base leading-7 text-neutral-700">{children}</dd>
        </dl>
      );
    }
    ```

    Do not add `types.ts`. Do not change dt/dd semantics. Run tests until GREEN.
  </action>
  <verify>
    <automated>pnpm test -- GlossaryTerm</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "slugifyHeading\\(term\\)" src/features/guides/GlossaryTerm/index.tsx` matches
    - `rg -n "scroll-mt-20" src/features/guides/GlossaryTerm/index.tsx` matches
    - `pnpm test -- GlossaryTerm` exits 0
  </acceptance_criteria>
  <done>GlossaryTerm deep-link ids work; Wave 0 GlossaryTerm tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sync timeline constants and fixtures to locked anchors</name>
  <files>src/features/guides/EducationTimeline/constants.ts, src/features/guides/EducationTimeline/fixtures.ts</files>
  <read_first>
    - src/features/guides/EducationTimeline/constants.ts
    - src/features/guides/EducationTimeline/fixtures.ts
    - src/features/guides/EducationTimeline/constants.test.ts
    - .planning/phases/10-education-pathway-editorial/10-UI-SPEC.md
    - .planning/phases/10-education-pathway-editorial/10-CONTEXT.md
  </read_first>
  <behavior>
    - Exact `anchorHref` values per locked map in Plan 00 / UI-SPEC
    - GYMNASIUM, ISS, GEMEINSCHAFTSSCHULE each set `anchorHref: "#os-caminhos-possiveis"`
    - AUSBILDUNG sets `anchorHref: "#formacao-profissional"`
    - GRUNDSCHULE sets `berlinNote: "dura 6 anos, até a 6ª série"`
    - Every `summary` is 2–3 Brazilian Portuguese sentences with zero `Fase 10` / `Conteúdo completo chega` / placeholder hash text
    - `fixtures.ts` `grundschuleFixture.anchorHref` becomes `#ensino-primario` (not the placeholder)
    - Export structure DEMO_TRUNK / DEMO_SECONDARY_DECISION / DEMO_OUTCOMES unchanged
  </behavior>
  <action>
    Per D-04 and UI-SPEC data contract, update `constants.ts`:

    | id | anchorHref | notes |
    |----|------------|-------|
    | kita | `#antes-da-escola` | rewrite summary; remove Fase 10 |
    | grundschule | `#ensino-primario` | add `berlinNote: "dura 6 anos, até a 6ª série"`; concept-level Berlin only — no Einzugsgebiet/Anmeldung procedure copy (D-11) |
    | gymnasium | `#os-caminhos-possiveis` | add anchor; peer-weight summary (not “única via”) |
    | iss | `#os-caminhos-possiveis` | add anchor |
    | gemeinschaftsschule | `#os-caminhos-possiveis` | add anchor |
    | ausbildung | `#formacao-profissional` | add anchor |
    | ensino-superior | `#ensino-superior` | rewrite summary |
    | realschule / hauptschule | (no required new anchors) | rewrite summaries only; keep germanyWideContext |

    Also update `fixtures.ts` `grundschuleFixture.anchorHref` from `#o-caminho-da-kita-a-universidade` to `#ensino-primario`.

    Do not reorder primaryBranches. Do not edit TimelineNode UI or aria-labels.
  </action>
  <verify>
    <automated>pnpm test -- EducationTimeline/constants EducationTimeline GlossaryTerm</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "o-caminho-da-kita-a-universidade|Fase 10|Conteúdo completo chega" src/features/guides/EducationTimeline/constants.ts` returns no matches
    - `rg -n "anchorHref: \"#antes-da-escola\"" src/features/guides/EducationTimeline/constants.ts` matches
    - `rg -n "anchorHref: \"#os-caminhos-possiveis\"" src/features/guides/EducationTimeline/constants.ts` has ≥3 matches
    - `rg -n "berlinNote: \"dura 6 anos, até a 6ª série\"" src/features/guides/EducationTimeline/constants.ts` matches
    - `rg -n "anchorHref: \"#ensino-primario\"" src/features/guides/EducationTimeline/fixtures.ts` matches
    - `pnpm test -- EducationTimeline/constants` exits 0
  </acceptance_criteria>
  <done>Timeline data matches locked H2 hashes; Wave 0 constants tests pass; no Fase 10 stubs remain in constants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static component → browser | GlossaryTerm ids are author-controlled German term strings |
| Timeline → in-page hash | anchorHref values are hardcoded relative hashes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Spoofing | GlossaryTerm `id` | accept | Ids derived from author `term` prop via slugifyHeading; no user HTML |
| T-10-01-02 | Tampering | constants `anchorHref` | mitigate | Only relative `#…` hashes; never external URLs in timeline links |
| T-10-01-03 | Elevation of Privilege | N/A | accept | Public static content; no auth |
</threat_model>

<verification>
`pnpm test -- GlossaryTerm EducationTimeline` green. Grep confirms no placeholder hashes or Fase 10 strings in constants.
</verification>

<success_criteria>
- GlossaryTerm id enhancement shipped per UI-SPEC
- All locked timeline anchors set; summaries production-ready
- Wave 0 RED tests are GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/10-education-pathway-editorial/10-01-SUMMARY.md`
</output>
