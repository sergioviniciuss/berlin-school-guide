---
phase: 11-flagship-discovery-release
plan: 05
type: execute
wave: 3
depends_on: ["01", "02", "03", "04"]
files_modified:
  - .planning/phases/11-flagship-discovery-release/11-VALIDATION.md
autonomous: false
requirements: [REL-01, REL-02, REL-03, DISC-01, DISC-02, DISC-03, DISC-04, ONBD-06]
must_haves:
  truths:
    - "Full Jest suite passes after flagship discovery changes"
    - "Playwright metadata, a11y, and smoke suites pass including homepage → onboarding and onboarding axe"
    - "Human confirms Start here visual hierarchy and complementary/editorial tone"
  artifacts:
    - path: ".planning/phases/11-flagship-discovery-release/11-VALIDATION.md"
      provides: "Validation sign-off updated after green gates"
      contains: "nyquist_compliant"
  key_links:
    - from: "e2e/smoke.spec.ts"
      to: "/guides/german-education-system"
      via: "homepage start-here CTA"
      pattern: "Começar pelo guia do sistema"
    - from: "e2e/a11y.spec.ts"
      to: "/guides/german-education-system"
      via: "CORE_A11Y_ROUTES axe run"
      pattern: "german-education-system"
---

<objective>
Phase gate: run full automated suites and human-verify homepage hierarchy + complementary editorial tone against success criteria.

Purpose: Confirm REL/DISC/ONBD outcomes are observable end-to-end before `/gsd-verify-work`.
Output: Green `pnpm test && pnpm e2e:ci` (intentional phase gate; waves 01–04 keep fast unit/`rg` verifies) and validated checkpoint; update 11-VALIDATION.md sign-off flags.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-flagship-discovery-release/11-CONTEXT.md
@.planning/phases/11-flagship-discovery-release/11-UI-SPEC.md
@.planning/phases/11-flagship-discovery-release/11-VALIDATION.md
@.planning/phases/11-flagship-discovery-release/11-01-SUMMARY.md
@.planning/phases/11-flagship-discovery-release/11-02-SUMMARY.md
@.planning/phases/11-flagship-discovery-release/11-03-SUMMARY.md
@.planning/phases/11-flagship-discovery-release/11-04-SUMMARY.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full Jest + Playwright CI suites and mark validation</name>
  <files>.planning/phases/11-flagship-discovery-release/11-VALIDATION.md</files>
  <read_first>
    - .planning/phases/11-flagship-discovery-release/11-VALIDATION.md
    - package.json
    - e2e/smoke.spec.ts
    - e2e/metadata.spec.ts
    - e2e/a11y.spec.ts
  </read_first>
  <action>
    1. Run `pnpm test` (full Jest). Must exit 0.
    2. Run `pnpm e2e:ci` only (CI Playwright config — do not fall back to `pnpm e2e`). Must exit 0.
       Note: Full Playwright is an intentional phase gate (>30s). Earlier waves (01–04) retain fast unit/`rg` verifies; this task is the end-of-phase green bar only.
    3. Confirm these greps remain true in the tree:
       - `Começar pelo guia do sistema` in `HomeJourneyCards/index.tsx` and `e2e/smoke.spec.ts`
       - `"/guides/german-education-system"` in `e2e/metadata.spec.ts` and `e2e/a11y.spec.ts`
       - `## Próximos passos` before `## Glossário` in onboarding MDX
       - No `Ver guias para famílias` in `HomeJourneyCards/index.tsx`
    4. Update `11-VALIDATION.md` frontmatter: set `nyquist_compliant: true` and `wave_0_complete: true`. Check off Wave 0 Requirements boxes and Validation Sign-Off automated items that are now true.
  </action>
  <verify>
    <automated>pnpm test && pnpm e2e:ci</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0
    - `pnpm e2e:ci` exits 0
    - `rg -n "nyquist_compliant: true" .planning/phases/11-flagship-discovery-release/11-VALIDATION.md` matches
    - `rg -n "wave_0_complete: true" .planning/phases/11-flagship-discovery-release/11-VALIDATION.md` matches
  </acceptance_criteria>
  <done>Automated unit + e2e gates green; VALIDATION.md Nyquist flags updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify homepage hierarchy and complementary copy</name>
  <files>.planning/phases/11-flagship-discovery-release/11-VALIDATION.md</files>
  <read_first>
    - .planning/phases/11-flagship-discovery-release/11-UI-SPEC.md
    - .planning/phases/11-flagship-discovery-release/11-VALIDATION.md
  </read_first>
  <action>
    Pause for human verification of discovery hierarchy and complementary copy against 11-UI-SPEC and 11-VALIDATION Manual-Only Verifications. Do not edit production files unless the user reports issues; if issues are reported, fix them in a follow-up commit before closing the plan. When approved, mark the two Manual-Only Verification rows in 11-VALIDATION.md as done.
  </action>
  <what-built>
    Two-card homepage (Start here primary → onboarding; Explore schools secondary), guides hub flagship stack, complementary intros with inline sibling links, Próximos passos before Glossário, first-steps soft tip, Portuguese metadata + e2e coverage.
  </what-built>
  <how-to-verify>
    1. Open `/` — confirm exactly two journey cards; Start here has stronger accent and links to onboarding; schools is secondary but visible; no methodology/guides-hub cards above the fold.
    2. Open `/guides` — onboarding flagship above Berlin retitled card; first-steps under Checklists práticas.
    3. Open `/guides/german-education-system` and `/guides/berlin-school-system` — intros state complementary relationship with clickable sibling titles; Próximos passos has three exits and sits before Glossário.
    4. Open `/guides/first-steps` — soft italic tip at top (not a warning box).
    5. Open `/schools` — directory intro unchanged (no “read the guide first” friction).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <verify>
    <automated>rg -n "Começar pelo guia do sistema|## Próximos passos|Sistema escolar público em Berlim|Novo em Berlim\?" src/features/home/HomeJourneyCards/index.tsx src/content/guides/german-education-system.mdx src/app/guides/berlin-school-system/page.tsx src/content/guides/first-steps.mdx</automated>
  </verify>
  <acceptance_criteria>
    - User replies with `approved`, or lists concrete copy/visual defects to fix
    - Pre-check: `rg -n "Começar pelo guia do sistema" src/features/home/HomeJourneyCards/index.tsx` matches
    - Pre-check: `rg -n "^## Próximos passos$" src/content/guides/german-education-system.mdx` matches
  </acceptance_criteria>
  <done>Human confirms visual hierarchy and complementary editorial tone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI browser → public static routes | Read-only verification; no new attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | I | Validation doc updates | accept | Planning metadata only; no runtime secrets |
| T-11-05-02 | D | e2e gate failure | mitigate | Block phase completion until `pnpm test` and e2e exit 0 |
</threat_model>

<verification>
Phase success criteria 1–5 observable via automated green + human checkpoint.
</verification>

<success_criteria>
- All phase requirements covered and verified
- Homepage → onboarding smoke green; axe green on onboarding; metadata green
- Human approved discovery hierarchy and complementary copy
</success_criteria>

<output>
After completion, create `.planning/phases/11-flagship-discovery-release/11-05-SUMMARY.md`
</output>
